## 1. Field lifecycle

- [x] 1.1 Implement wrap omission and simple style removal using the existing marker; verify native square/none/absence across supported owners, restoration, no-op bytes, surrounding XML/non-target ZIP and retained authority/removal guards in the shared lifecycle experiment.

## 2. Public contract

- [x] 2.1 Update schema/Help/registry/text reference and coverage/backlog, regenerate reference/matrix, and run preview input/capability, portability/reference sync and strict OpenSpec checks with exact scope recorded.

Verification (2026-09-10): related body/placeholder/table-text native selection passed 54/54, zero skipped (SDK 8.0.128, repository TMPDIR, single-process build), including 7 new wrap cases and retained authority/other-field deletion guards. Preview input/capability, Skill maintenance, matrix, portability (255 files), reference sync (333 files) and strict OpenSpec passed. No wire change, NativeAOT rebuild or host wrapping acceptance. Other deletion/inheritance gaps remain; the known broader table failure was not rerun.
