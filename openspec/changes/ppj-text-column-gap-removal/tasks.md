## 1. Field lifecycle

- [x] 1.1 Implement source omission and simple-style deletion with NoColumnSpacing; verify native spacing, zero/fraction/max restoration across supported owners, columns/direction preservation, compact table restoration, no-op bytes, XML/non-target ZIP and retained guards in the shared fixture.

## 2. Public contract

- [x] 2.1 Update schema/Help/registry/text reference and coverage/backlog, regenerate reference/matrix, and run preview input/capability, portability/reference sync and strict OpenSpec checks with exact scope recorded.

Verification (2026-09-10): related native body/placeholder/table-text selection passed 82/82, zero skipped (SDK 8.0.128, repository TMPDIR, single-process build), including 7 columnGap cases with 0/12.5/10000 point restoration, exact EMU values, columns/direction preservation and existing authority/removal guards. Preview input/capability, Skill maintenance, matrix, portability (255 files), reference sync (333 files) and strict OpenSpec passed. No wire revision, NativeAOT rebuild or host column-layout acceptance; other deletion/inheritance gaps remain and the known broader table failure was not rerun.
