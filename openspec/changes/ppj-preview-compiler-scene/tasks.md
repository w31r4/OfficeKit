## 1. Scene contract and bounded evidence

Production entry closing recheck (2026-09-10): after the registry/diagnostic wording update, final bIwSP1 repeats all ten entry and seven gradient cases with unchanged eleven fatal deletion failures; no other failure array entries. Matching-package presentation gate passes 4/4 (tliOkO). The diagnostic suite also passes WITHOUT preload, retaining independent fresh-process lazy-import checks. Both generated checks, gate-policy, strict OpenSpec and diff whitespace pass. Public styled-component PNG inspected by Agent only. Default package update, remaining factual mappings, native deletion repairs and full goal are still unfinished; no completion checkbox changes.

Tasks 3.3/3.4/4.1 production entry integration (2026-09-10): renderPpjToSvg now requests includePreviewScene, calls the shared native painter with mandatory input assessment, and uses scene-bound publication. Canonical component/chart/table guessing code is deleted, not retained as fallback. Public JSON carries compact scene identity, never protobuf BigInts. Native-scene renderer identity includes painter/adapter/validator digests; visible warnings retain actual diagnostic addresses. Synthetic factual families, nested page identities, malformed native channels, asset snapshots and pre-publication missing/version/digest/candidate rejection were migrated to explicit native receipts. Exact c8b0d324 report DUtafR passes ten real-compile/public-function cases: plain/styled nested pairs, line/heatmap encoded/explicit pairs, source no-op and text edit. Entire content raster below the 24px review banner matches previously asserted native pixels; actual warning pixels, scene/candidate hashes, mandatory assessment, JSON serialization, returned/persisted equality and input/source bytes are checked. Two encoded cases retain failed factual status; no blanket rule retirement. First xmbPm5 stopped on Buffer-versus-Uint8Array type comparison; equal-type exact-byte assertions now pass. Actual CLI child-process authored/source/non-overwrite tests pass with test-only preload selecting the same real package (isytO3). Default installed package was tried and fails preview.scene.missing; it has not been replaced, so default-package delivery remains required. Full DUtafR remains FAILED/exit 1 on eleven native deletions, not a passing overall integration. No checkbox promotion or current native rebuild; 10/15 and full goal remain open.

Task 3.3/3.4 linear gradient progress (2026-09-10): shape and table cells share direct RGB/alpha ordered stops and scaled=false physical-angle mapping. Duplicate stops, alpha zero and unique SVG IDs survive. Synthetic 0/45/90/180/270 direction and invalid stop/angle/alpha/conflicting-fill cases pass; out-of-range alpha first exposed clamping and now rejects explicitly. Exact c8b0d324 runtime PfrKIJ records seven 0/45/90 authored/source no-op and shape-only 90->0 source-edit cases, with real plateau/transparent pixels, unchanged table angle, fresh projection and only slide1.xml changed. Original source/program remain unchanged; 45-degree PNG inspected by Agent only. Presentation 4/4 and generated checks pass. Full integration still FAILS on four transform and seven spacing deletion regressions; current summary now records all eleven. No native rebuild, radial/background/chart/text-gradient support or task completion claimed; 10/15 and full goal remain open.

Task 3.3 character bullet progress (2026-09-10): internal painter consumes direct character/font/RGB/point size with explicit left-aligned hanging indent. Bullet uses paragraphLeft+indent, all text lines use paragraphLeft, continuation lines do not repeat the marker; direct alpha and noBullet=true retain meaning. Missing direct paint, percent sizing, non-hanging and non-left-aligned cases fail with field-addressed limitations; exact glyph metrics/tab clearance, numbering/picture bullets/inheritance/deletion remain open. Synthetic positive first failed, then passes with five conservative negatives and immutable scene. Exact c8b0d324 runtime BPCuPP passes authored/source no-op, source character+RGB edit and independent authored comparison: orange circle 155 solid pixels becomes blue square 210, unchanged text positions and no continuation marker, complete text-region raster equality, fresh projected character and only slide1.xml delta. Source/program bytes remain unchanged. Edited PNG inspected by Agent, not human calibration. Presentation 4/4 and generated checks pass; full integration still FAILS on eleven deletion cases, exit 1. No native rebuild or production switch; 3.3/full goal remain open, 10/15.

Task 4.1 line factual mapping progress (2026-09-10): registry isolatedLinePoints requires all owner bindings to completed native LINE transforms with actual visible isolated-point captures and exact literal category/series/name/value/missing-index agreement. Dataset interpretation is excluded. It resolves legacy series-type inheritance errors and individually captured isolated-point errors; hidden, transparent, outside-range, mismatched, smooth-unavailable and mixed-owner cases retain failures. Captures publish only after complete line SVG construction. Exact c8b0d324 nPwMZz records four real authored/source-noop/table-move/value-edit cases, each retiring two series-type-not-inherited errors, with capture removal restoring them and missing registry rejecting. Existing marker/gap/zero pixels, source fidelity and combined unrelated diagnostics remain asserted. Real accepted inputs have no series.chartType, so isolated-error retirement has synthetic boundary evidence only, not a real-trigger claim. Initial explicit chartType fixture jiMcTB was compiler-rejected and restored without compiler changes. smg21U caught an old connector test expecting the newly resolved line rules unchanged; tests now verify these mapped families separately and retain all remaining diagnostics. Full integration still FAILS on eleven deletion regressions, exit 1. No native rebuild, default-profile/production switch or grade promotion; 4.1/full goal remain open, 10/15.

Task 3.4 static media poster progress (2026-09-10): internal media route uses verified posterAssetId image bytes and the writer's stretch frame, retaining transparency/outer transforms with a visible STATIC AUDIO/VIDEO POSTER label. Static-only diagnostics retain playback/timing/payload limits. Synthetic audio/video, transform, empty poster and undeclared asset rejection preserve scene bytes. Exact existing c8b0d324 runtime rnrCq1 passes two distinct authored poster colors, transparent background pixels, warning pixels, immutable inputs/assets and snapshot-free source no-op bytes. Candidate-import media remains opaque with no fabricated poster; opaque descriptors currently carry no preview asset. The media fixture is a container header, not playback evidence. Initial h5iVdP fixture omitted required asset accessibility metadata; corrected without validator changes. Presentation 4/4, generated summaries and gate-policy pass; authored PNG inspected by Agent only. Full integration still FAILS on eleven existing deletion regressions, exit 1. No native rebuild, imported/OLE poster claim or production switch; 3.4/full goal remain open, 10/15.

Task 3.3 capitalization display progress (2026-09-10): internal fontCaps all/none respects direct/default/fallback precedence and changes display text only. Native language guides Unicode casing while full language/shaping remains limited; small and unknown tokens fail explicitly rather than simulating small-cap glyphs. Synthetic regression first failed, then passes precedence, immutability, Turkish casing, Unicode expansion, explicit newline and escaping. Exact existing c8b0d324 runtime je2i3l records five real cases: all/none authored/source no-op and all->none source leaf edit. Raw glyph-region equality with independently authored literal-text references, original literal Hello abc in scene/reprojection, unchanged source/program and only slide1.xml delta pass. Full integration still FAILS on four transform and seven paragraph-spacing deletions, exit 1. No native rebuild, small-caps/deletion/host-font acceptance or production switch; 3.3/full goal stay open, 10/15.

Task 3.3 paragraph-spacing deletion investigation (2026-09-10): seven independent requests freshly project the original snapshot-free source and delete every nonempty combination of line/before/after multipliers. Exact existing c8b0d324 package rsK9nS rejects all seven with ppj.source.unsupportedMutation before candidate creation. Source plus seven request files and fatal failure records are retained; original source hash remains unchanged. No XML deletion/reprojection/pixel success is claimed. Current ApplyRichText masks do not implement paragraph-spacing removal, while lower PptxParagraphSpacingCodec.Apply already supports No*Spacing. A lower-codec deletion test is not PPJ acceptance. Repair needs scoped PPJ capability/diff/native mapping work beyond the read-only scene change; do not bypass PPJ or permit arbitrary rich-style edits. Full report FAILS on these seven rejections and four existing transform deletions, exit 1; independent regressions execute. Presentation 4/4, syntax and gate-policy pass. No production native change/rebuild or checkbox promotion; 10/15 and full goal remain open.

Task 3.3 before/after multiplier progress (2026-09-10): internal painter consumes spaceBeforeMultiplier/spaceAfterMultiplier using adjacent first/last effective logical line heights. Explicit zero, fixed points and absent behavior remain; overridden defaults do not inflate spacing. Synthetic 0/0.5/1/2, invalid negative/infinite, scene preservation and mixed 20pt/10pt versus overridden 80pt default pass. Initial new regression failed before implementation; mixed-case schema import omission was corrected and rerun. Exact existing c8b0d324 runtime HIDS0u records ten real cases: 10/20pt x 0/0.5 x authored/source no-op, plus two source edits zeroing both slots. Actual glyph-start geometry equals independent fixed-point authored expectations; fresh projection retains explicit zero, only slide1.xml changes and original source/program remain unchanged. Full integration still FAILS on four known transform deletions, exit 1. No native rebuild, formal-route switch, exact host font metrics or inheritance/deletion acceptance. Task 3.3 and full goal remain open, 10/15.

Task 3.3 line-spacing multiplier progress (2026-09-10): internal painter consumes native lineSpacingMultiplier against the existing logical baseline advance; first-line position, fixed-point spacing and absent defaults remain unchanged. Exact font metrics remain explicitly limited. New synthetic regression fails before implementation and passes afterward; 0.5/1/1.5/2, input preservation and invalid zero/negative/infinite multipliers are covered. Exact existing c8b0d324 runtime report 0PGFXu records seven cases: 1/1.5/2 authored plus snapshot-free source no-op and original-source 1->2 leaf edit. Three 20pt lines have actual ink-start deltas 24/36/48px matching independent fixed-point authored expectations. Fresh projection, unchanged original source/program and only slide1.xml changed are asserted. Presentation 4/4 and both generated checks pass. Full integration still FAILS on four known transform deletions, exit 1. No native rebuild, full text-metric/inheritance/deletion or production-route claim. Task 3.3 and full goal remain open, 10/15.

Task 4.1 direct connector endpoint mapping (2026-09-10): registry connectorEndpoints selects all-owner-direct-endpoints-painted. Painter captures only constructed straight/elbow paths; assessment additionally requires completed outer transform, all owner bindings to painted native connectors, no target binding for the endpoint, and exact safe-EMU equality to pure input x/y. Retire each from/to factual error independently; do not recreate object-anchor semantics. Synthetic straight/elbow positives pass, while mismatches, hidden/curved placeholders, mixed owner kinds and object anchors retain failure. Exact existing c8b0d324 runtime GjnuAZ verifies six straight/elbow authored/no-op/candidate cases with existing geometry/arrow/RGB assertions, missing capture restores both errors, missing registry rejects, unrelated input/paint diagnostics survive combined assessment and candidate bytes remain unchanged. Presentation 4/4 and both generated summary checks pass. Full integration still FAILS on four known frame-transform deletions, exit 1. No native rebuild, grade promotion or production switch; object anchors/imported route provenance and remaining factual mappings keep 4.1/full goal open, 10/15.

Task 3.3 source text-anchor lifecycle / refreshed runtime (2026-09-10): six independent original-source requests set middle/top, set bottom=0, delete anchor, delete bottom, or delete both. Old runtime-slot-fixed IcwdcL passes setters but retains deleted fields in all three deletion cases; failures remain recorded, not relabeled expected passes. Current native source already contains removal semantics. Exported c8b0d324561bc9d390f9f71aa108fad05bd9344b, verified 3847 Git blobs/symlinks (one unexpanded legacy submodule), and used checked-in build with SDK 8.0.128 to produce tmp/preview-current-native-iv6UMg/runtime, 7 files/106076144 bytes. PPJ SHA-256 d1a1dd53bbda25e0146058799e86ca718c030cb0a4e4ae45496a1e96536622d6; Office 1a4b88211b813ba388c06031e7e616d1e1fed4de055b265ffc37472a95e2cb3f; manifest 7ae2fe9f6d277dae531e497e90d22d4d2eacd3cf69ff0a6ea2612032b6131cfb. Real vAmZ1g passes all six edits, actual XML/native/fresh-projection presence, explicit top/0 versus removal, unchanged source/request/non-target ZIP members (only slide1.xml changes), and raw glyph-region equality with independent authored expectations. Existing 12 anchor/no-op cases remain. Same frozen source passes preview+margin 52/52 and separate body-enum lifecycle 30/30, zero skipped, TRX under snapshot tmp/test-results. Presentation 4/4 passes. Four frame-transform deletion regressions still fail on THIS new runtime; overall integration exits 1. No native production change, installed package replacement, new double-build reproducibility, full text metrics/complex inheritance or production routing claimed. This runtime supersedes runtime-slot-fixed for future regression, not for retroactive attribution of historical results. Tasks 3.3/5.3/5.4 and full goal stay open, 10/15.

Task 3.3 direct text-anchor progress (2026-09-10): internal top/center/bottom aligns the complete explicit-line block within top/bottom insets using existing logical line metrics; explicit zero inset is retained. Nine synthetic positive cases cover three anchors, bottom 0/10/30 and two lines; three negative cases require field-addressable unavailable for unknown anchors and unresolved overflow. Exact runtime-slot-fixed 6r8OMm records 12 authored/snapshot-free source no-op cases with native values, actual glyph bounds/counts, source/program immutability and byte-identical no-op candidates. Center/bottom shifts relative to top are 16/32px at bottom=10 and 6/12px at bottom=30; every glyph count is 911. Presentation 4/4 passes. Full integration still FAILS on four known transform deletions. The text-layout limitation remains: this is logical-block review alignment, not font-metric accuracy, AutoFit/wrapping, distributed/overflow anchoring, source anchor edit/delete or production routing. No native rebuild or grade promotion; 3.3 and full goal remain open, 10/15.

Task 4.1 text-body field classification (2026-09-10): replace blanket bodyProperties-unmapped reporting with descriptor-driven child-field assessment. Only actually consumed leftInsetEmu/rightInsetEmu/topInsetEmu are handled; bottom inset/reset/other fields remain specific limitations. Six synthetic alignment/inset cases verify coordinates, input immutability and retained bottomInsetEmu diagnostics. Existing runtime-slot-fixed PgPOma adds four authored cases: right inset 0->30 shifts right-aligned glyph bounds -30px and centered bounds -15px with identical ink counts, correct SVG anchors and no false consumed-inset diagnostic. Production alignment math itself was already implemented; this fixes evidence granularity rather than claiming new paragraph-right-margin support. Presentation 4/4 passes; full integration still FAILS on four known transform deletions. No native build, full text-layout/vertical/AutoFit claim or production route switch. 4.1 and full goal remain open, 10/15.

Task 3.3 effective run-size baseline fix (2026-09-10): nonempty lines previously took max(default size, actual run sizes), displacing explicitly small runs by an overridden default. Baseline advance now uses resolved run sizes; empty lines retain default/fallback size. Synthetic 8pt-versus-32pt-default assertion first failed, then passed; default-sized/empty-line and immutability cases remain. Real runtime-slot-fixed oYWAyO verifies authored and snapshot-free source no-op 8pt HHHH: baseline 111.6, actual upper-region ink, no ink in the old displaced region, source/no-op bytes preserved. Full integration remains FAILED on four transform deletions; prior pairs/paragraphs pass. Presentation 4/4 passes. Font metrics/wrapping/AutoFit and production route remain limited; no native rebuild or support-grade promotion. 3.3/G-02 and full goal remain open, 10/15.

Task 3.3 tiled-image false-display fix (2026-09-10): internal painter previously rejected crop+tile but drew uncropped native tiled=true as a single stretched image. All tiled inputs now retain field-addressed unavailable/failed evidence and a visible object placeholder instead of inventing coverage. Three synthetic crop-presence variants pass; real runtime-slot-fixed jHmDS0 exercises cropped/uncropped authored plus snapshot-free source no-op (4 cases), native tiled flag, no image markup, precise tile diagnostic, retained source bytes and exact no-op candidate. This closes misleading stretch output only, not actual tile rendering. Current scene carries a parameter-free tiled boolean without resolved intrinsic tile/DPI state; PptxPictureCodec accepts useLocalDpi 0/1 without exposing it on PresentationImage. Full tile semantics still need native read/transport scope and actual repeat pixels. No native change/rebuild, grade promotion or production switch. Full integration remains FAILED on four transform deletions; 3.3/G-05 and full goal remain open, 10/15.

Task 5.3 real scene-budget recovery (2026-09-10): runtime-slot-fixed report AperkA exercises repeated-20 in one actual NativeAOT process with maxUncompressedBytes=4096. It returns exactly preview_scene_budget_exceeded, no candidate bytes and no scene; restoring default limits in the same process succeeds with 41 bindings, validated scene digest and the ordinary candidate hash. Input hash remains unchanged, client retires in finally. Result is persisted in performance.json and bound by the verified integration SHA-256. This is a real authored byte-budget failure/recovery, not a generic compile rejection counted as scene evidence; node/depth bounds retain managed unit coverage only. Full integration remains FAILED on four known source-transform deletions; independent pairs and resource checks pass. No runtime rebuild or production change. Peak retained-object accounting and full 5.3/G-16 remain open, 10/15.

Task 5.3 source-candidate release (2026-09-10): added four managed weak-reference cases for source no-op, text, frame and semantic-fill compile paths using the existing source fixture with assets and an opaque sibling. While source bytes, request and projection remain alive, returned scene/result/presentation/slides/top-level node payloads/bindings/asset references become collectible after the non-inlined helper returns and forced GC runs. Candidate scene equals an independent fresh import; no-op retains source identity, edit candidates differ; subsequent scene-disabled compile has no scene and identical candidate bytes. Source and restored request bytes remain unchanged. Same isolated 4b7cd9c6-plus-two-fix source copy with authored/source release test patches passes preview 47/47, zero skipped, SDK 8.0.128; TRX tmp/preview-transform-diagnostic-jqYdBe/tmp/candidate-release-results/preview-candidate-release.trx. No native production change or NativeAOT rebuild. This extends managed reachability evidence, not arbitrary imported topology, native-host lifetime/allocation or retained-data peak proof. 5.3 remains open, 10/15; unrelated JS transform deletion failures remain unresolved.

Task 5.3 authored response release (2026-09-10): added ReleasedPreviewResponseDoesNotRemainRootedByLiveRequest for minimum/canonical authored inputs through both managed codec entrypoints (4 cases). A non-inlined helper returns only weak references to response/result/scene/presentation/slides/elements/shape/group/chart/bindings; the original request and assets stay live across forced GC. All observed objects become collectible. A subsequent scene-disabled compile on that same request has no scene, identical candidate bytes, and unchanged restored request bytes. Isolated 4b7cd9c6-plus-two-fix source copy, with this test patch, passes full preview filter 43/43, zero skipped using SDK 8.0.128; TRX: tmp/preview-transform-diagnostic-jqYdBe/tmp/response-release-results/preview-response-release.trx. This is managed authored response reachability, not NativeAOT/source-candidate release, zero allocation or peak retained-data evidence. No production native code or package rebuilt. Existing JS deletion failures are not retested or closed. 5.3 remains open, 10/15.

Task 5.3 isolated native memory evidence (2026-09-10): fullWireCompile measures fresh PPJ processes for one compile after handshake. performance.json records Linux procfs VmRSS/VmHWM before/after, PID, startup/invoke time and wire size for both modes on repeated-20 and source-candidate; each child retires in finally. Non-Linux explicitly reports unavailable, not zero. Same-PID/high-water monotonicity, scene-off absence, candidate identity and source/program immutability are asserted. Final FfHlHq binds performance.json by verified SHA-256; all four process measurements complete. These are OS residency observations (includeNodeMap=true), not managed live-object retention/release proof. Full integration remains FAILED/exit 1 on four known transform deletions; paired cases pass. Presentation 4/4, syntax and gate-policy pass. No C# rebuild or threshold claim; scene-disabled allocation/retained-data and resource-limit acceptance keep 5.3 open, 10/15.

Task 5.3 warm timing/response baseline (2026-09-10): actual runtime-slot-fixed integration DsJ91a writes performance.json for repeated-20 (41 scene bindings) and an actual source text-edit candidate. Both modes warm up, then run three samples each: raw native response bytes/invoke wall time, public compile wall time, actual SVG/PNG time/bytes and explicitly labeled JS before/after snapshots. Each measured compile issues one native request; scene-off has no scene, scene-on candidate bytes equal the off candidate in each round, original program/source hashes stay unchanged. Median off/on compile ms: repeated 10.34/14.71, source 25.79/31.15; responses 28234/48044 and 43021/46054 bytes. Scene-on median SVG/PNG ms: 3.54/18.18 and 1.05/8.93. These are same-host warm observations, not thresholds, cold-start or native/retained-memory peaks. Initial hMfwb5 input exceeded the PPTX page-size limit (4400pt); corrected to legal 4000pt with 3800pt repeat frame, preserving 20 instances. No compiler limits changed. Full DsJ91a remains FAILED on four known transform deletions; independent paired/benchmark cases pass. Presentation 4/4 passes. Native peak/retention, scene-disabled allocation and resource-limit coverage still keep 5.3 open; 10/15 unchanged.

Task 5.1 completed (2026-09-10): report bt1cbj against unchanged runtime-slot-fixed passes all required paired categories: nested components/repeats/slots (plain and named-style variant), standalone named-style/grammar, and dataset/explicit inputs through both native line and vector heatmap. Every pair retains missing/zero risk, actual paint and authoritative owners. Heatmap asserts six generated cells, fixed geometry and RGB pixels, green missing versus black zero, separate scene addresses, full typed group and raw raster equality, input preservation and scene-on/off candidate identity. All three pair failure arrays are empty. This completes the specified paired-fixture task, not all chart variants or full renderer acceptance. Overall integration remains FAILED/exit 1 on four native source-transform deletions; these remain fatal and cannot be offset by pair results. Presentation 4/4 passes; no new C# build or production switch. Progress 10/15; 3.3/3.4/4.1/5.3/5.4 and full goal remain open. Earlier 9/15 entries are historical.

Task 5.1 combined nested-style case verified (2026-09-10): retain plain nested-repeat pair and add styled variant composing the two fixed geometry/style fixtures. Inner tile uses named shape style; definition-supplied repeated slot uses named text style, grammar color/font/size and explicit false override. Independent explicit side gets fixed resolved styles, not compiler-derived output. Existing runtime-slot-fixed report x2Cj1e passes both variants: complete typed visual/raster equality, each repeated blue glyph region, font/size/false, source definition/slot paths, distinct instances/scene paths, frame/orange/blank pixels, missing/zero and scene-on/off candidate identity. No pair failures; standalone style and dataset still pass. Full integration remains FAILED on four known source-transform deletions, exit 1. Presentation 4/4, syntax and gate-policy pass. No new native build, production route or full text overflow claim. Vector/native chart pairs and remaining tasks keep 5.1 and full goal open, 9/15.

Task 5.1 named-style/grammar pair verified (2026-09-10): fixed preview-style-grammar-equivalence.json independently specifies named shape/text styles with grammar color/font/size tokens versus explicit resolved values, inline false overriding named true, and shared [1,null,0] risk. Exact existing runtime-slot-fixed report BhbhbY has stylePairFailures=[] and styleGrammarPair passed: ordered typed visual payload and full raw raster equality, font/size/false, orange shape and blue glyph pixels, owner/frame checks, missing/zero/isolated observations, immutable inputs and each side's scene-on/off candidate byte equality. Fixture hash joins before/after identity checks. First lgFqET run caught a fixture collision with base theme ink; Catalog correctly preferred the theme. Renamed the grammar token label-ink without changing semantics or weakening assertions. Nested/dataset pairs still pass. Full integration remains FAILED on the four known source-transform deletion assertions, exit 1. Presentation 4/4, syntax, gate-policy and strict OpenSpec pass. No C# rebuild, production switch, full theme/source-style lifecycle or human acceptance. Other vector/native and combined paired cases remain; 5.1 and full goal open, 9/15.

Task 5.1 authored dataset pair verified (2026-09-10): fixed preview-dataset-equivalence.json independently specifies interleaved Alpha/Beta data, array/object rows, name/index channel references and explicit series [1,null,0]/[0,4,5]. Exact runtime-slot-fixed report PZD4Ht has datasetPairFailures=[] and datasetPair passed: native names/order/frame/values/missing indexes, two isolated Alpha observations, one continuous Beta segment, complete typed chart payload and full raw raster equality, original owner/input retention and scene-on/off candidate byte identity on both sides. Fixture digest is checked before/after execution. Nested-repeat pair still passes. Full integration remains FAILED only on the four known native transform deletion cases, exit 1. No native changes/rebuild, JS dataset interpretation, workbook/source-edit or host/human acceptance claimed. Remaining named-style/grammar and other paired cases keep 5.1 and full goal open, 9/15.

Task 5.1 bounded nested-repeat/slot pair verified (2026-09-10): fixed repository JSON and evidence contract independently specify two-level components, repeated instances, definition-supplied literal-zero slot, explicit coordinates and [1,null,0] line risk. Initial runtime-fixed UO1dDt failed on the component side: diagnostic NativeAOT probe identified reflection-based JsonSerializer.Serialize(slot.Key) in PpjPreviewOrigins.Index. JsonEncodedText with explicit JSON delimiters preserves the same provenance path without reflection; isolated snapshot preview tests pass 39/39, zero skipped. Checked-in build produced independent runtime-slot-fixed: PPJ SHA-256 ca442bdfcef428e9d0a6444f661fd77f63ad1a593beda02d406fbffdebb0b9a6, manifest a37862d1b45875a150bcecab933a972595c4f626ab727a8e8bade4ac7f070223. Final SxRCLN report has nestedPairFailures=[] and nestedRepeatSlotPair passed: ordered typed visual payload bytes and full raw page raster equal, definition/slot owners and distinct instance/scene identities checked, missing/zero preserved, inputs immutable, scene-on/off candidate bytes equal on both sides. Fixture hash is included in before/after identities. The full suite still FAILS on four known source transform deletions, exit 1; both sides and independent regressions execute, failed pairs cannot report success. Runtime remains 4b7cd9c6 plus the two bounded AOT fixes, not current whole-worktree C#; no new double-build reproducibility or installed package update. No component expansion semantics changed. Named-style/grammar/dataset/vector/native-chart and remaining paired cases keep 5.1 open, 9/15.

Task 4.1 group-coordinate mapping (2026-09-10): registry maps groupCoordinates to all-owner-group-frames-painted. Retire only the old group-coordinates-ignored factual error when every owner binding is a successfully drawn native group and all eight outer/child frame values exactly match safe EMU conversions. Synthetic mismatched frame, hidden and zero-child-extent cases retain failure. Exact existing runtime-fixed integration KEpYTd records four successful groupCoordinate cases: two nested groups, nonzero child origin, different outer child widths, authored plus snapshot-free source no-op, orange center (150,136) versus (125,136), blank alternate location, two owner diagnostics, missing capture retention, missing registry rejection and immutable/no-op source bytes. Existing transform cases now explicitly assert the group rule's removal and restoration rather than treating it as an unchanged unrelated rule. Presentation 4/4, both generated checks and gate-policy pass. Full integration remains FAILED with the four known native transform deletion assertions, exit 1; no C# change/rebuild, source childFrame edit/delete, complete component coverage or production route. 4.1 and full goal remain open, 9/15.

Task 4.1 optional combined assessment (2026-09-10): internal paintPpjSceneSvg(receipt, { assessInput: true }) completes actual drawing for all pages before assessing canonical input with the validated native profile. A semantic-path trie maps each input diagnostic to its deepest owner bindings, retaining original input paths and all generated-node scene paths; unmatched constraints remain page/global, and final banners/publication consume the combined tree. Exact verified MIME/hash payload mapping handles different public/native asset IDs without IO. First real attempt N66dGG incorrectly keyed assets by receipt ID and failed publication; corrected mapping and rerun ewzfZH verifies both authored and source no-op/text-edit combined outputs, original input/paint diagnostic retention, returned/persisted assessment equality, real warning pixels and immutable candidate/program bytes. minimum remains requires-review; the other three remain factual failed, not false passes. Synthetic tests cover shared generated owners, retained transform failures, unknown global fields, immutable scenes and different public IDs/uppercase digests. Full real suite still FAILS on the four previously recorded source-transform deletion assertions; all combined cases pass and the failures remain fatal. The option stays off by default and production CLI is unchanged. No native semantic change or rebuild; remaining mappings/attribution cases/production integration keep 4.1 and full goal open, 9/15.

Task 4.1 source-transform lifecycle investigation (2026-09-10): new real cases independently reset or delete transform fields from each original source projection. Final tmp/officekit-native-scene-paint-REM5Lk/integration.json is FAILED, exit 1: four explicit 0/false resets pass, four deletions fail because fresh projected fields remain explicitly 0/false. Candidate XML confirms group xfrm rot="0"/flipH="0"/flipV="0". All edit cases reach correct reset/background pixels, unchanged ZIP inventory, only slide1.xml delta, full readingOrder and unchanged child frames before presence assertions; original authored/no-op eight cases and independent regressions still run. Failed cases retain requested/observed frames, hashes, PPTX and reprojected PPJ, then raise AggregateError. Current TryCollectFrameLeafMutations compares effective values and token-splices zero rather than removing attributes; candidate scene accurately reflects those bytes. Native removal semantics require a separate/expanded planning-scope decision beyond this read-only scene change, so no compiler implementation was changed. First fixture passed a string where workspace requires Uint8Array; fixed to bytes without relaxing assertions. Same bounded runtime-fixed PPJ 6b2c3f0c65624ef9f9fc81dc8e9fba721edbbd0ea9a674f05b172469ee3202b4; presentation 4/4 passes but does not cover new native deletion cases. This does not reopen the repaired AOT projection failure; G-03, 4.1 and full goal remain open, 9/15.

Task 4.1 transform AOT verification (2026-09-10): the earlier four source-group failures below are now resolved on the bounded fixed runtime. Diagnostic NativeAOT probe identified JsonArray.Add<string> requesting absent System.String metadata in ProjectGroup; use the existing AOT-safe StringNode helper, preserving full readingOrder rather than omitting children. Fixed-snapshot scene tests pass 39/39 (zero skipped); the diagnostic exception-detail change was reverted before building runtime-fixed. Real probe passes, and final tmp/officekit-native-scene-paint-3gbXxj/integration.json passes all four authored plus four snapshot-free source rotate/flipH/flipV/combined cases, exact readingOrder, source no-op/immutability, asymmetric orange/background pixels and bounded profile rule removal. Missing captures and unrelated rules remain errors/limits. Source PPTX and per-case source/candidate/scene hashes are retained; prior failures remain historical artifacts. Runtime is fixed 4b7cd9c6 plus the group primitive-node fix, not current whole-worktree C#: PPJ SHA-256 6b2c3f0c65624ef9f9fc81dc8e9fba721edbbd0ea9a674f05b172469ee3202b4, manifest 073333a05ec5a4a414d9f7a2e14631f41bab1af3c6241a5d1474fd12e6220653. Presentation 4/4, both generated checks, gate-policy and strict OpenSpec pass. No new double-build reproducibility, source transform edit/delete, full profile merge or production switch is claimed. 4.1 and full goal remain open, 9/15.

Historical failing stage (superseded only for the four AOT source-group failures by the verified fix above):

Task 4.1 transform mapping in progress (2026-09-10): native scene profile records actual successful SVG transform branches and matches each input rotation/flip field to every owner binding's native value before omitting that old factual error. Missing capture retains errors; unrelated limits remain. Exact 4b7cd9c6 run tmp/officekit-native-scene-paint-eIGtvx/integration.json is FAILED, exit 1: four authored rotate/flipH/flipV/combined group cases pass distinct orange/white pixels and profile assertions, but all four snapshot-free source projections return codec_failure before no-op/paint. The suite now retains these failures, executes independent regressions, persists failed status and throws AggregateError; no source success or task completion is claimed. Stage artifacts include PPJ/PPTX in TfErYC and eIGtvx. A temporary managed probe added only under the fixed snapshot's test directory projects the same rotate PPTX successfully both without and with IncludeNodeMap (TRX tmp/transform-probe-results/transform-probe*.trx). This is not a rebuilt NativeAOT pass. A separate diagnostic clone tmp/preview-transform-diagnostic-jqYdBe changes only exception detail in PpjCodecProtocol and is building an independent runtime to reveal the NativeAOT exception; installed/reference runtimes are unchanged. Presentation 4/4, metadata checks and whitespace pass. Full goal and 4.1 remain open, 9/15.

Task 4.1 first renderer-profile mapping (2026-09-10): assessPpjPreviewInput defaults to canonical-svg; explicit native-scene-svg requires a validated matching receipt, canonical input and actual painter result. Registry previewScene.factualMappings.visibility owns the all-owner-nodes-hidden handler. Only owners whose every binding is native-hidden and recorded by the painter's hidden branch omit the old visibility factual error; all other input/source/unknown/bounds/factual limits remain unchanged. Exact 4b7cd9c6 runtime report tmp/officekit-native-scene-paint-SYsUNl/integration.json passes authored and source no-op hidden input, visible orange versus hidden white center pixels, missing capture retention, mismatched input/scene and missing registry/unknown profile rejection, plus full prior regressions. The report now records ten implementation/binding identities; generated JS binding at this run is 1634f45e36dbca62488233b2dd454d4b423e6bca2350bafb099a0b5e584d60dc from concurrent work, not claimed identical to the C# snapshot. Presentation 4/4, both metadata generator checks, gate-policy, strict OpenSpec and whitespace pass. No C# rebuild, overall grade promotion or production/profile-assessment merge; remaining mappings and integration keep 4.1 open, 9/15.

Task 4.1 node assessment progress (2026-09-10): internal page assessments now contain the actual native group/diagram-child hierarchy. Each node retains semantic page/id/path and an independent scenePath; diagnostic fields resolve to the closest actual native node, not a shared generated owner. Unvisited descendants of hidden/failed/unavailable parents remain explicitly unassessed instead of receiving a visual pass. Synthetic regressions cover two generated nodes sharing one owner with distinct field diagnostics, plus hidden/failed parent traversal. Every savePaint in the real suite compares assessment node count and all identities to the compiler scene bindings. Exact fixed-4b7cd9c6 integration tmp/officekit-native-scene-paint-WbYaHv/integration.json passes, retaining 24 operational and 2 factual publication-failure cases; presentation 4/4 and gate-policy pass. No C# rebuild, grade promotion, factual-rule retirement or production switch. Registry/profile original-input assessment remains open; task 4.1 and full goal stay open, 9/15.

Task 4.2 completed (2026-09-10): exact fixed-4b7cd9c6 runtime run tmp/officekit-native-scene-paint-yuV8XP/integration.json passes. In addition to scene identity, non-overwrite, authored/source-candidate publication and brown warning pixels, all four real inputs run six isolated operational faults (24 cases): backend load, first-page raster, first SVG/PNG write, pending and final manifest write. Assertions preserve scene/source/candidate hashes, page/document identity, unaffected-page status, actual artifact hashes, valid remaining PNGs, pending/final lifecycle and unchanged input assessments/bytes. Missing/future scenes and required assets reject on actual receipts. Two additional real factual failures (authored connected scatter and imported SmartArt candidate) retain failed reliability, matching final receipts and red warning pixels. First matrix run incorrectly hashed empty authored source bytes instead of the contract's null; corrected the test and fully reran. Presentation 4/4, gate-policy and strict OpenSpec pass; no native rebuild. This completes the scene publisher contract, not the production route or renderer-profile input assessment (3.3/3.4/4.1 remain open). Current progress 9/15; full goal remains open.

Task 4.2 scene identity progress (2026-09-10): shared readPpjPreviewReceiptScene normalizes JS asset MIME names and validates program/candidate bytes, native scene digest and assets. Painter captures a compact identity; publisher matches version/origin/digest/program/candidate to compile evidence and source mode before reserving a directory or loading raster. Eleven injected missing/mismatched identity cases fail with preview.output.scene, failed reliability and no artifacts/directory. Real fixed-4b7cd9c6 integration tmp/officekit-native-scene-paint-dBwhGX/integration.json passes two authored plus source no-op/text-edit publications, returned/persisted identities, source versus candidate hashes, actual artifact hashes and PNG warning-color pixels. Old scene/new candidate, corrupt digest and missing assets are rejected. First real run IrFA1s failed on mimeType/contentType mismatch; extracted the existing view conversion into the shared validator and reran without weakening asset checks. Final presentation 4/4, gate-policy, strict OpenSpec and whitespace pass. No C# rebuild, production switch or full failure-matrix acceptance; 4.2 remains open, 8/15 unchanged.

Task 4.2 prerequisite (2026-09-10): publication failure replacement previously keyed only by semantic path; two scene pages at "$" caused both document children to become the last page after a page-local failure. A new regression first reproduced the wrong identities. Replacement now keys pageId/id/path/scenePath and failure diagnostics retain the affected page or global scene address. Five injected cases (first/last/both raster failures, global backend failure, PNG ENOSPC) verify page/document identity and status, returned/persisted equality, actual artifact hashes and unchanged input assessments. Focused publisher checks pass; presentation 4/4 passes. Exact 4b7cd9c6 runtime integration passes at tmp/officekit-native-scene-paint-OFGUGQ/integration.json, including existing authored publisher and source lifecycle regressions. Native C# was not rebuilt. Scene version/origin/digest publication binding, full source-candidate publication and production routing remain open; task 4.2 is not complete, 8/15 unchanged.

Task 5.2 completed (2026-09-10): native source frozen at 4b7cd9c6c56cd9af97b2e385428dc491b8aafc86; 3731 tracked blobs/symlinks match the exported snapshot (unexpanded legacy reference submodule is outside build inputs). Checked-in build with SDK 8.0.128 produces independent linux-x64 package tmp/preview-runtime-4b7cd9c6-ksr777, 106022896 generated bytes. PPJ SHA-256 86dd41600b3aa403a4d3111670ab6458b5ebb09395430deac2c3d615ab24efe2; Office dcf53e562b33ceb668cbf1b6d0c2f206835c509580db19752bc04f139239064c; manifest a297f27df7ef87bfff912e9460397b5983ff112442b8eac3d59399e5389e4c57. Exact package integration passes at tmp/officekit-native-scene-paint-NeJHpQ/integration.json; report validates manifest/binaries and five JS/binding identities across the run. Frozen-source PpjPreview tests pass 39/39, zero skipped (TRX under snapshot tmp/test-results); npm run verify:office-kit-build passes 9-file same-host reproducibility. Current-worktree proto:check passes and generated binding matches frozen source; presentation 4/4, gate-policy, syntax/import and strict spec/whitespace checks pass. First publish terminated with SIGBUS; TMPDIR on workspace storage, UseSharedCompilation=false and MSBUILDDISABLENODEREUSE=1 allowed the unchanged snapshot to build. Existing nullable/trim/AOT warnings remain. Current JS includes explicitly hashed later scatter/test changes; subsequent concurrent native changes are excluded. This closes the specified runtime gate, not full field paint, production routing, external fixtures or performance. Current progress 8/15; full goal remains open.

Task 3.4 numeric scatter (2026-09-10): internal painter consumes per-series numeric X/Y, canonical missing Y, explicit/reversed/log axes and bounded markers. Synthetic tests cover unequal X spacing, distinct series X counts, missing-domain exclusion, real zero, clipping, malformed inputs and immutability. Explicit 495daafc integration tmp/officekit-native-scene-paint-laY7Yi/integration.json passes authored X 10->25 pixel movement, exact source no-op, source Y 2->5 pixels/reprojection and source/non-target preservation (only chart1.xml changes). Source X edits remain explicitly rejected. Current writer forces scatter ln/noFill even for lineWithMarkers: real negative case verifies ChartML and requires scatter-line-unresolved with no invented SVG connection. Direct-line connected geometry has synthetic evidence only; smooth/bubble/full styles/current rebuild/production route remain open. Initial series-stroke rejection, canonical style-token correction and axis-overlap pixel correction are retained in this evidence; no compiler semantics were changed and 3.4 remains unchecked.

Task 3.4 further SmartArt investigation (2026-09-10, inspected HEAD 362dffe6): retained 2MsoJ4 source/candidate packages both have two cached shapes and zero cached connectors despite the authored scene connector. Current BuildCachedDrawing only writes semantic-node shapes. Candidate dataModelExt points to drawing2.xml, where source direct CC5500 fills became accent1 after semantic text editing; retained old drawing.xml is not current-target fidelity evidence. ReadCachedDrawing drops direct paint and Drawing also participates in projected detach authority, compiler Group cloning and codec equality checks. Repair therefore spans native write/import/edit/detach semantics, beyond this change's read-only scene boundary; implementation paused for an explicit planning-scope decision. No original artifact changed, no runtime rebuilt, no checkbox promoted. Detailed identities and additional acceptance requirements are in current-gaps G-07. Existing ownership assertions still pass their narrower scope; they do not establish target visual/style preservation.

Task 3.4 SmartArt ownership audit (2026-09-10): PptxCodec Diagram replacement intentionally builds a new exclusively owned part graph then deletes old target parts. Replaced the incorrect inventory-equality/directory-only expectation with actual graphicFrame/dataModelExt relationship traversal, five typed target roles, exact outside-graph file preservation, shared-slide sibling preservation, unrelated relationships/content types and unchanged layout/style/colors bytes. Shared slide normalization is restricted to attribute order and a known redundant same-URI root xmlns:a declaration; a sibling-text mutation is detected. Existing 495daafc integration tmp/officekit-native-scene-paint-2MsoJ4/integration.json passes with diagramFailures=[], saved source/candidate PPTX and resolved target map. Supersedes prior strict ownership assertion failures, not the missing imported cache paint/connections: candidate-import painting remains unavailable. Old-cache cleanup, complex/shared third-party graphs, rebuilt HEAD and production routing remain unverified; task 3.4 stays open.

Task 3.4 diagram-cache progress (2026-09-10): verified authored diagram drawing now routes through shared group painting, preserving child transforms/owners and actual shape/text/connector state. Synthetic cache/no-cache/geometry/immutability checks and real authored process RGB pass. Candidate-import diagrams explicitly fail unavailable: current PptxSmartArtCodec.ReadCachedDrawing only reconstructs semantic-node geometry/text, omitting complete cache paint/connections. Existing 495daafc integration tmp/officekit-native-scene-paint-XyIRJd/integration.json is FAILED (exit 1): source no-op and candidate text/reprojection pass, but TWO strict part-inventory/local-delta assertions fail on graphics part replacement, added drawing2.xml, slide/relationships/Content_Types changes. Failures remain fatal after independent regressions run. Audit SmartArt transaction ownership before deciding which changes violate preservation; no source-fidelity pass claimed. Presentation 4/4, synthetic, gate-policy, strict OpenSpec and whitespace pass. First real fixture errors were missing textStyleRef and wrong named-text-style nesting, corrected against existing schema. Native import completion, ownership regression, current rebuild and production route remain open; 3.4 unchecked.

Task 3.3 shape outlines (2026-09-10): shape/custom-path paint now reuses linePaint for native dash/cap/join/width/alpha; none, zero and custom stroke=false survive. Exact host dash metrics remain diagnosed. Synthetic patterns/zero/invalid/custom-path tests pass. Existing 495daafc integration tmp/officekit-native-scene-paint-chiuBR/integration.json passes authored/no-op/issued-leaf dash-cap-join edits, red/gap pixels, reprojection and source/non-target preservation (only slide1.xml changes). First fixture omitted required geometry.kind and was corrected before rerun. Presentation 4/4, gate-policy, strict OpenSpec and whitespace pass. Themes, compound outlines, path arrows and production routing remain open; task 3.3 unchecked.

Task 3.3 paragraph margins (2026-09-10): internal native marginLeftEmu and left-aligned signed indentEmu now determine paragraph/first-line x; explicit line breaks restore the paragraph start. Synthetic signed/zero/immutable and centered-indent residual checks pass. Existing 495daafc integration tmp/officekit-native-scene-paint-PHbQnD/integration.json passes 30pt margin/10pt hanging authored/no-op and issued-leaf zero edits, exact -20/-30/0 glyph shifts, reprojection and source/non-target ZIP preservation. Presentation 4/4, gate-policy, strict OpenSpec and whitespace pass. Only floating-point coordinate comparison uses 1e-9 tolerance; raster offsets remain exact. Lists, non-left first-line indentation, wrapping, current native rebuild and production route remain open; no checkbox promoted.

Task 3.3 point paragraph spacing (2026-09-10): lineSpacingPoints controls explicit-line baseline distance; spaceBeforePoints/spaceAfterPoints retain explicit zero. Font-derived/default layout and multiplier/removal inheritance remain limited. Synthetic multi-paragraph/line/zero/invalid/immutable/residual tests pass. Real 495daafc integration tmp/officekit-native-scene-paint-d2vSYk/integration.json passes authored/no-op/native-leaf edits and fresh projection: line 40->55, before 10->0, after 15->0 cause pixel offsets -10/+5/-10; only slide1.xml changes and source/non-target ZIP entries remain intact. Presentation 4/4, gate-policy, strict OpenSpec and whitespace pass. No native rebuild, production switch or full task completion; 3.3 remains open.

Task 3.3 letter-spacing progress (2026-09-10): native fontSpacingPoints maps to SVG letter-spacing with signed values, absent/default/run/fallback precedence and explicit zero. Nonfinite/out-of-native-range values fail. Synthetic inheritance/zero/bounds/immutability passes. Existing 495daafc native integration at tmp/officekit-native-scene-paint-b252Er/integration.json passes signed glyph-width deltas, source no-op/issued-leaf edit/reprojection, equivalent authored raster bounds/ink count, and source/non-target preservation. Presentation 4/4, gate-policy, strict OpenSpec and whitespace pass. No native rebuild, production route, kerning/shaping/wrapping or complete typography claim; task 3.3 remains unchecked.

Task 3.3 text-format progress (2026-09-10): internal run/default styles now paint single underline/strike and signed baseline percentages, preserving explicit none/noStrike/zero and resetting relative offsets across runs/lines. Unsupported decoration tokens fail explicitly; full typography remains limited. Synthetic inheritance/zero/size/newline/invalid/immutability cases pass. Real existing-495daafc integration at tmp/officekit-native-scene-paint-MUHC4L/integration.json passes 40pt +/-30% raster displacement, decoration ink, source no-op, issued-native-leaf edit, fresh projection, original/non-target ZIP preservation (only slide1.xml changes). Initial semantic rich-style edit was rejected; corrected to issued leaf capabilities, with projected single/sngStrike assertions aligned to the existing projector. Final presentation 4/4, gate-policy, strict OpenSpec and whitespace pass. Report now records time and painter/integration JS hashes. No current native rebuild, production switch, full typography or task completion claimed; 7/15 remains.

Task 3.3 image-border progress (2026-09-10): direct RGB/width/opacity/dash/cap/join paint follows the existing image mask outside its clipping group; custom-path stroke=false is retained. Synthetic ellipse/alpha/zero-width/zero-alpha tests pass; unresolved theme colors fail visibly and dash metrics retain their approximation diagnostic. Real authored/source-edited border RGB pixels, fresh width reprojection and unchanged source/non-target ZIP entries pass in tmp/officekit-native-scene-paint-Hyrhgw/integration.json against the existing 495daafc package. Presentation segment 4/4 passes. Themes/effects/tile and production routing remain open; no checkbox promoted.

Task 3.3 roundRect progress (2026-09-10): shape and image-mask painters share native roundRect radius mapping with defaults from the canonical preset profile; explicit zero and pin limits are preserved. Synthetic non-square/default/zero/signed/clamped cases pass. Real mask authored/source edit 50000->0, corner pixels and fresh projection pass in tmp/officekit-native-scene-paint-M9UI1y/integration.json using existing 495daafc package and current JS. Presentation 4/4, gate-policy and whitespace pass after moving TMPDIR to repository tmp: initial system /tmp run failed with ENOSPC and a codec failure. No current C# rebuild, production switch or complete preset coverage claimed; task 3.3 remains open.

Task 3.3 image-mask progress (2026-09-10): internal SVG clips rect/ellipse/diamond and literal custom paths in native frame coordinates, combined with crop and existing alpha/transform mapping. Clip IDs are unique per render and deterministic. Unknown presets, unimplemented adjustments, conflicting masks and unresolved paths fail visibly. Synthetic mask tests and presentation 4/4 pass. Explicit 495daafc package integration at /tmp/officekit-native-scene-paint-QMZO1r/integration.json passes authored and source-edited ellipse/diamond/custom masks with crop, center/corner pixels, fresh projection and unchanged source/non-target ZIP members. Remaining presets/adjustments, tile/effects and production integration keep task 3.3 open; no new C# build claimed.

Task 3.3 image crop progress (2026-09-10): internal painter maps signed native source rectangle edges into a clipped nested SVG viewport, preserving alpha and transparent negative-edge padding. Invalid ranges/empty source extents fail; tiled crops remain explicitly unavailable. Synthetic coverage checks directional/negative/zero/invalid crops and immutable scenes. Actual NativeAOT integration with existing 495daafc package passes at /tmp/officekit-native-scene-paint-Pxd6KU/integration.json: authored positive crop, source no-op, negative-padding edit, fresh projection, exact red/green pixels and underlying blue visible through padding, unchanged source and non-target ZIP entries (only slide1.xml changed). Presentation segment 4/4 and gate-policy pass. Initial blue-background fixture used a custom arc and was corrected to a rectangle before rerunning. Production route, masks/tile/effects and complete task 3.3 remain open; no current C# rebuild claimed.

Task 3.4 elbow bend progress (2026-09-10): internal SVG consumes signed literal bendAdjustment, including explicit zero and values outside 0..100000. Synthetic paths cover reversed endpoints and 0/25000/50000/100000/-50000/150000; straight-with-bend fails explicitly. Real integration `/tmp/officekit-native-scene-paint-90ZBTJ/integration.json` passed with the existing 495daafc NativeAOT package and current JS: authored 25000, exact source no-op, source edit to 75000, native/SVG/PNG/fresh projection assertions and unchanged source/non-target ZIP members; only ppt/slides/slide1.xml changes. No later C# rebuild or production route switch is claimed. Curved and full imported transform coverage remain open; 3.4 remains unchecked.

- [x] 1.1 Add the default-off compile scene option and versioned read-only result envelope using the existing native Presentation IR; preserve current PPJ JSON/hash fields, reject validation-only/projection misuse and regenerate bindings. Verify round-trip/presence tests, both request validators, `buf lint`, deterministic generation and the actual `npm run proto:check` outcome without staging unrelated generated work.
- [x] 1.2 Implement the native scene collector/envelope builder with deterministic ordering, semantic/native provenance, native asset references and byte/node budgets. Verify serialization/digest stability, absent optional values versus explicit zero/false, complete retained visual fields, no duplicated source-package/opaque payload transport, and explicit overflow failures in a focused `PpjPreviewSceneTests` suite.

## 2. Reuse actual compiled visual state

- [x] 2.1 Capture source-free slides from the writer's actual materialization pass, keeping catalog/master/layout context and vector/native chart results. Verify captured writer-input equality, one lowering/materialization per page, unchanged output bytes with scene on/off, and unchanged ordinary/Morph slide lifetime when the option is off.
- [x] 2.2 Generate source-bound scenes by importing the exact resulting candidate through the existing native codec, including physical no-op, native-leaf edit-plan, semantic export and file-backed source reuse. Verify a no-op plus text/frame leaf and semantic edits against fresh candidate imports, candidate/source hashes, unchanged original bytes and non-target ZIP entries; do not restore embedded authored snapshots as current visual state.
- [x] 2.3 Complete bindings for nested/repeated component nodes, writer-generated vector children, imported native nodes and native asset IDs. Verify stable page/semantic IDs, z-order, exact original owner paths with separate scene paths, repeated instance differentiation, correct asset bytes, and explicit unmapped/opaque attribution rather than fabricated edit paths.

## 3. Transport and actual local drawing

- [x] 3.1 Return the envelope through both general and PPJ-only native codec entrypoints; forward it through `native.mjs` and `compilePpjWorkspace` with default-off behavior. Verify both protocol profiles, no scene on ordinary build/check, and explicit missing/version/hash/asset mismatch failures rather than fallback to old canonical drawing.
- [x] 3.2 Add a dependency-light native scene adapter that preserves native fields and performs only mechanical unit, presence and asset/identity conversions. Verify all current native content cases have routing or an explicit limitation, unknown descendants fail conservative coverage, and neither the legacy Presentation facade nor raster/specialist dependencies load through root/in-memory usage.
- [ ] 3.3 Make SVG shape/text/image/group painting consume scene state, including actual compiler-generated vector paths and component placement; remove the label/value component heuristic. Verify distinguishable rendered geometry/style and stable IDs for paired component/explicit fixtures, real generated chart children, null/zero preservation, and explicit limits for remaining preset/text/effect painting gaps rather than equal-placeholder tests.
- [ ] 3.4 Route native charts, tables, connectors and remaining source/opaque content from scene fields without re-evaluating PPJ datasets, grammar or component layout. Verify actual data-channel, frame and ordering consumption; retain field-addressable failures for remaining G-02–G-10 painting defects, and prove no old topology guesses or lossy pseudo-PPJ conversion are used.

## 4. Reliability and publication integration

- [ ] 4.1 Extend registry-backed assessment with source/scene provenance and renderer-profile-sensitive factual rules. Verify generated-node attribution, unresolved inheritance, unknown native fields and each retained fact gate; retire a previous limitation only with a direct native-state-to-SVG regression showing that exact defect is resolved. Preserve G-11 supported-primitive mutation, nested IDs, warning and production-failure tests.
- [x] 4.2 Bind published evidence to scene version/origin/digest and actual candidate identity, while retaining G-12 non-overwrite, pending/final lifecycle, source hashes and actual artifact hashes. Verify returned/persisted evidence equality, labeled PNG pixels and scene/asset/raster/write failure paths using real authored and source-bound output.

## 5. Equivalence, rebuilt runtime and maintenance evidence

- [x] 5.1 Add fixed paired high-level/explicit cases for nested components, repeats/slots, named styles/grammar, dataset/encoding and vector/native charts. Each pair must include a relevant missing-data, complex-relation or source-bound edge. Verify equal effective scene geometry/data/style (only documented generated IDs normalized), actual SVG consumption and authoritative origin paths; compiling two files or rendering two placeholders is insufficient.
- [x] 5.2 Build the NativeAOT profile consumed by JavaScript using the checked-in build workflow and verify its manifest/version/digest. Run the focused native scene tests and real JS scene tests against that exact binary, `npm run verify:office-kit-build`, syntax/import checks, gate-policy and `npm run test:slow -- --segment presentation`; retain all G-11/G-12 preservation tests. Record full proto generation/check outcomes and any environment-dependent failures honestly.
- [ ] 5.3 Measure representative scene-on/off response size, stage timing and peak retained data, including a repeated/component-heavy case and a source-bound candidate import. Verify no preview allocation is retained by scene-disabled calls, limits fail explicitly and no input/output is overwritten. This is the G-01 regression baseline, not the unestablished full G-16 performance target.
- [ ] 5.4 Update registry/derived metadata, preview output documentation, affected presentation review guidance and the G-01 audit with actual implementation evidence. Verify generator drift checks, documentation links, Skill portability/reference-sync when changed, `git diff --check` and strict OpenSpec validation. Mark G-01 complete only after transport, authored/source-bound provenance, real scene consumption and equivalence all pass; keep unsolved G-02–G-10, G-13–G-16 and the full goal open.

Planning baseline: `0e1d8c8f` plus uncommitted G-11 and concurrent trendline-label work inspected on 2026-09-09. This checklist is planning, not implementation evidence; every task is initially unchecked. Re-read current shared proto/registry/compiler changes before implementation.

Current branch check (2026-09-10, HEAD `231467d8`, not rebuilt): `main` and `origin/main` are aligned. The branch includes custom-geometry connection sites and adjustment handles; the shared worktree also contains uncommitted source/schema/OpenSpec changes for `ppj-custom-path-references`. Production `svg-preview.mjs` still consumes `compiled.programJson`; the native scene painter remains an internal route. The changes after `495daafc` are not included in the package or report below and require a clean rebuild and rerun before they can count as evidence.

Last reproducible package evidence (2026-09-10, source snapshot `495daafc`): the checked-in build workflow with SDK 8.0.128 produced `/home/zenfun/mywork/OfficeKit/tmp/officekit-preview-runtime-495daafc` (7 files, 105,957,056 bytes; PPJ SHA-256 `d43dcf4ce8000aa50165277aabfa0ed906680184aa2797a16e9f6c6e13b37c17`; Office SHA-256 `22a1c33c67e3ed15fbf2f3224013c0f0005a053404ba1819b3e9d67b7c8f2703`; manifest SHA-256 `55611aac24a355df9f520cc957cfeabf950051d23846c747f203420c7493dec4`). The exact package passed `node test/ppj-preview-scene-native.mjs ...` with `status: passed`, `relationFailures: []`, `customArcPath: true`, generated Bézier paths 6, directed anchors 2/2, connector/table/chart/source-bound checks. A concurrent branch change invalidated the subsequent reproducibility comparison, so no reproducibility claim is made for this snapshot. The package proves only native scene transport/view/internal SVG/PNG; it does not prove production routing, complete field painting, third-party/workbook coverage, host behavior, human calibration or G-16 performance. G-01 remains 7/15; tasks 3.3, 3.4, 4.1, 4.2 and 5.1–5.4 remain open.

Task 3.4 nonnegative percentage-stack progress (2026-09-10): internal BAR now handles percent-stacked for complete nonnegative category totals. Divide by the category maximum before summation to avoid overflowing the raw total; retain raw values, fractions and cumulative endpoints, with percentage ticks. Missing categories remain unpositioned and visible; all-zero totals retain zero observations but explicitly undefined percentages, not zero/100% fabricated bars. Negative observations remain explicit failures pending a verified signed policy; full percentage support is not claimed. Synthetic tests pass both directions, 25/75 proportions, reversal, explicit bounds, real zero in a positive-total category, unknown versus zero totals, huge finite values, underflow/lost increments and negative/display-policy rejection.

Final real run `/tmp/officekit-native-scene-paint-l3j50c/integration.json` uses the same explicit temporary PPJ executable `9c97c6605c36c9e2b8218fe208a380745cc25b3b94742c4a620eb4ed21a496e9`. Both new percentage directions pass authored/no-op and original-source value 4->6 edits: fractions change from 4/12 to 6/14, geometry/RGB pixels and reprojected RAW values agree; only the target ChartPart changes, all non-target parts and original bytes are preserved. Previous ordinary stack and independent line/circular/bar/table/source checks also pass. Two object anchors still fail, 0/2, overall report failed and final exit 1. Source fixtures are project-generated literal data without workbook, not third-party coverage. An earlier result yqBN8Z was superseded after correcting a misleading test category label; no extra cases are counted from reruns.

Final presentation segment passes 4/4 at `/tmp/officekit-ppj-preview-BTDmLF`. Painter SHA-256 `859cf9cb003edf3114755dcff48b5052bd9710bdb0de3936affc79b3cfed40e2`, synthetic test `64176aa7e93ba07563f0c31c79f8d5b5668db039011368885194ba27a3dab92c`, native test `06d8c1a518d55f82abb9c2de4efb21f54bd47ce39f6bde0a3a151076cc0fe081`. Earlier clustered/ordinary-stack work was committed concurrently as `d7ddc46e`; this step preserves that snapshot and does not claim to have committed it. No C#/proto/compiler semantic change, NativeAOT rebuild, full-repository/smoke/host/human/performance acceptance or production switch. The registry/audit retain exact limitations and unchanged production grades. Task 3.4 and the full goal stay open, 7/15.

Percentage closing checks: gate-policy, both generated capability checks, strict OpenSpec, all 77 local audit links and whitespace pass. The final bar-percent-edited PNG was inspected after correcting the category label; this is Agent inspection, not human calibration.

Task 3.4 ordinary-stack progress (2026-09-10, base `1fe503cd` plus preserved changes): internal native BAR now maps grouping=stacked with independent positive/negative cumulative endpoints and cumulative domains, while honoring declared gap/overlap. Unknown categories retain missing/known observations, show Incomplete and do not fabricate cumulative positions. Explicit zero in a complete category has a review tick at its true cumulative baseline. Overflow and addition that loses a nonzero observation fail. Percent stacking, logarithmic bars, complete missing-display strategies, chart layout/labels/styles and production integration remain unfinished. No C#/proto/compiler semantics changed; task 3.4 remains unchecked, 7/15 overall.

The synthetic suite passes signed accumulations, both directions/reversal, overlap -100/0/100, incomplete categories with known zero, zero at a nonzero cumulative baseline, immutable input, overflow and precision-loss rejection. An initial synthetic reverse-axis fixture used untyped JS objects and failed protobuf serialization; it was corrected with the generated Axis schema, without weakening any assertion. Real run `node test/ppj-preview-scene-native.mjs /tmp/officekit-preview-runtime-xZSfO9` saved `/tmp/officekit-native-scene-paint-js6Kkn/integration.json`: both stack directions pass authored/no-op and original-source value 4->6 candidate geometry/RGB pixels/reprojection; only `ppt/slides/charts/chart1.xml` changes, original and all non-target parts remain unchanged. Inputs are project-generated literal data, not workbook/third-party cases. Existing independent regressions execute and pass; TWO object-anchor regressions still fail, report failed and final exit 1. No overall integration success is claimed.

Presentation 4/4 passes, artifacts `/tmp/officekit-ppj-preview-xTlM9i`; pure SVG and gate-policy pass. Column authored and bar edited PNGs were inspected, not human-calibrated. Painter SHA-256 `04ab1e744e51f9b812d5f8218da61e3e3001ea02d5a350cb7f75bc3514b52171`, SVG test `eaa0486a93dda55188b1936ca7b4940fd4ea35e31a12475b2f11216a4e4d9348`, native test `3dd8405e7a751d49cdf73ceb34983e858fc119b5ed24cbe67005aa6506dce3f3`. Reused PPJ binary remains `9c97c6605c36c9e2b8218fe208a380745cc25b3b94742c4a620eb4ed21a496e9`; no new runtime build, C# tests, full-repository/smoke/host/human/performance acceptance. Production grades stay unchanged.

Task 3.4 clustered-bar in-progress evidence (2026-09-09, base `1fe503cd`): internal `barChart()` now consumes native BAR with column/bar direction, category/value axes, independent series slots, native missing indexes, signed zero-baseline lengths, explicit min/max, reversed axes and gap/overlap presence. Missing observations reserve their slot without generating a zero bar; actual zero retains its identity and a labeled dashed review tick. Signed charts show a review zero reference, not an invented authored gridline. Point fill/outline overrides use the same helper as circular charts, retaining direct RGB/noFill/alpha and explicit zero pen width/opacity. Stacked/percent-stacked, log baselines, mixed/secondary axes and missing zero/span display strategies remain explicit failures, not ordinary-bar substitutions; labels/legend/theme/effects and host layout remain limited.

The full synthetic SVG suite passes, including exact bar/column geometry, missing versus zero, unequal signed lengths, reversal, clipping evidence, all-missing, hidden axes, gap 0/100/500 and overlap -100/0/50/100, point paint, immutable input and fatal unsupported/malformed cases. The existing circular/line regressions also pass after sharing paint helpers. A first real fixture incorrectly used a string for series.fill and was compiler-rejected (`ppj.schema.type`, artifacts vI7tSw); it was corrected to the existing solid-fill schema without changing the compiler or validator.

Final real run: `node test/ppj-preview-scene-native.mjs /tmp/officekit-preview-runtime-xZSfO9`, artifacts `/tmp/officekit-native-scene-paint-2he9uM`, persisted `integration.json`. Both directions pass authored, double-axis-reversed and source no-op geometry/RGB pixels. Each type then separately changes a value, sets gap to zero, sets overlap to -100, deletes gap and deletes overlap from the ORIGINAL source projection: all TEN candidates pass native/SVG/pixels/fresh-reprojection checks, retain missing indexes/zero, and change only `ppt/slides/charts/chart1.xml`; every non-target ZIP entry and the original source bytes are unchanged. Deletion is verified as absent native and reprojected fields. These are project-generated literal-data fixtures, not workbook/third-party or object/series deletion coverage. Existing line, circular (including ten explosion edits), table, generated paths, text and explicit-coordinate connector checks execute and pass. The TWO object-anchor regressions still fail, 0/2; final exit remains 1 and no aggregate success is claimed.

Final presentation segment passes 4/4, production old-route artifacts `/tmp/officekit-ppj-preview-dQGuM6`. Pure SVG, gate-policy and both generation drift checks pass. Final column-authored and bar-reversed PNGs were inspected, not human-calibrated. Reused PPJ executable SHA-256 `9c97c6605c36c9e2b8218fe208a380745cc25b3b94742c4a620eb4ed21a496e9`; no current C# rebuild or full task 5.2 acceptance is claimed. Painter SHA-256 `217aa21f1419468edf43f7c341209919aea7692ae9fc1fc12ae43a03f0f16327`, synthetic test `33864847b73ae7e8360c909749de16f36f350c955ba11b2198260a51fb8f748b`, native test `e9b05e35bc4383e9c260d50178be1274fdc3bd080ee8232296255743ea5a5680`. Registry and audit describe actual internal progress without changing production grades or task checkboxes. No C#/proto/installed-package/Skill-route change, full repository/smoke/host/human/performance acceptance; 3.3/3.4 remain open, 7/15 overall.

Task 3.4 circular in-progress evidence (2026-09-09, base `dc5e725c`): fixed the all-missing synthetic fixture by removing illegal missing-point styles from its positive case and adding separate fatal missing-style regressions for both pie/doughnut. No validator was relaxed. The entire internal SVG suite and presentation segment now pass again; the previous document audit's red result remains historical evidence, not the current suite result.

The internal single-series pie/doughnut painter now consumes series/point explosion in 0..400, preserves a point-level explicit zero over the series default and distinguishes deletion from zero. It uses a documented radial-review construction (ring thickness times percentage along the slice bisector), uniformly fits the plot to keep exploded geometry visible, and retains exact proportions, missing indexes and zero-area observations. It reports `chart-explosion-layout`: exact Office spacing, concentric layout, labels and theme are not claimed solved. Synthetic tests cover 0/25/100/400, point-zero precedence, rotation, true-zero nonmovement, unchanged input and fit bounds; invalid >400 and missing-point styles remain fatal. This is bounded preview progress, not complete explosion/host fidelity or native chart family coverage.

The explicit-package integration now passes normal circular ratios/angles/hole/point colors plus authored explosion and source no-op for both types. Each type separately edits point zero, deletes the point override, sets series zero, deletes series explosion, and deletes both, always rebuilding the request from the ORIGINAL source projection. All ten edits pass native/SVG/precise raster pixels/reprojection, preserve null versus zero, change only `ppt/slides/charts/chart1.xml`, and preserve all other ZIP entries and source bytes. Fixtures are project-generated literal-data inputs, not third-party/workbook coverage.

An initial expanded run at `/tmp/officekit-native-scene-paint-KC5nP4` hit `runtime_terminated` after retaining an independent full-wire client across the long suite; the native client retires after 1000 ms idle. The test now creates/releases a real client per independent comparison, without disabling production retirement or retrying/ignoring failed assertions. Final run `/tmp/officekit-native-scene-paint-e6xHTu` reaches and persists `integration.json`: all independent checks pass, but the same TWO authored object-anchor cases fail, anchors 0/2, final exit 1. The PPJ executable is still SHA-256 `9c97c6605c36c9e2b8218fe208a380745cc25b3b94742c4a620eb4ed21a496e9`, not a new HEAD build. Final presentation 4/4 passes at `/tmp/officekit-ppj-preview-FSn5Hk`; gate-policy, both generator checks and changed JS syntax pass. The authored pie and point-zero doughnut PNGs were inspected; this is not human calibration. Registry and audit now describe circular progress without promoting production grades. No C#/proto change, installed package update, production switch, full paired/runtime-build/whole-repository/host/human/performance acceptance is claimed. 3.3/3.4 remain unchecked; 7/15 overall and the complete goal remain open.

Circular closing checks: strict OpenSpec, Skill portability (255 files), reference-sync (333 files), audit local links (77 main + 2 historical) and whitespace pass. Painter SHA-256 `ffd4614d264979be819aeb672a464f8c1939c18a50af627b15fb7eb21dd2b5bd`; SVG test `dbdb343b50954cbe927bfa09393e17558cd0a5f89db415c30c368f95930a3aa7`; native integration test `68b171578fd66039875d77b82ec69088066879b8072bcd350b3f4a5d4c0e5bb9`. These checks do not change task completion or production support.

Task 3.4 native-line in-progress evidence (2026-09-09, base `d891a173` plus preserved working-tree line fixes): the internal painter now separates plot-clipped line paths from frame-bounded markers, so in-range observations on the plot edge remain whole. Out-of-axis observations retain their real value and an outside-plot attribute, without being clamped into invented edge values or claiming a visible review dot. Direct marker RGB outline/width/opacity/dash/cap/join consumes native fields with inherited/unsupported residuals; explicit zero width/opacity survive. This does not add a PPJ interpreter or change compiler semantics.

Synthetic SVG tests pass for leading/trailing/consecutive missing points, isolated observations, distinct two-series missing indexes/order/paths, hidden axes, axis-line versus tick visibility, category intervals, direct marker outline and zero paint, out-of-range evidence, plus the earlier real-zero/negative/log/reverse and malformed-data cases. The actual NativeAOT fixture now has two line series with different gaps and an explicit filled/outlined circle marker. Authored, source no-op, unrelated table move and source value-edit candidates pass native/SVG/marker-interior/outline/plot-edge/gap pixel assertions. Reprojection preserves the untouched second series and changes first-series `[2,null,0,4,5]` to `[2,null,3,4,5]`; the only changed ZIP member is `ppt/slides/charts/chart1.xml`. Every other member and the original source bytes are unchanged. This literal-data fixture explicitly has no workbook, so workbook-linked edits and deletion remain unverified.

Final explicit-package artifacts: `/tmp/officekit-native-scene-paint-rAFBXC`, with SVG/PNG, per-case diagnostics and `integration.json` including source/candidate/binary identities. The PPJ executable remains SHA-256 `9c97c6605c36c9e2b8218fe208a380745cc25b3b94742c4a620eb4ed21a496e9`, not a new HEAD build. Both authored object-anchor cases still fail, 0/2; the script executes the independent checks, persists `status: failed`, then throws AggregateError and exits 1. This is not a passing integration. The candidate image was inspected, not human-calibrated. The regular presentation segment passed 4/4 at `/tmp/officekit-ppj-preview-xQYFNQ`; final added synthetic edge checks passed separately, and gate-policy, both generator checks and syntax passed. Registry now records bounded native-line progress without promoting production grades. No C#/proto change, production route switch, runtime rebuild, full paired suite, full repository test, workbook/third-party/host/human or performance acceptance is claimed. Tasks 3.3/3.4 remain unchecked, 7/15 overall; native chart families, complete line layout/styles/display strategies and G-11/G-12 scene integration remain open.

Final native-line recheck in the same step: the shared line helper now attributes approximate dash metrics to actual `dashStyle` fields for chart/table lines and `lineStyle` for connectors; a marker dashed-outline SVG and exact diagnostic-path regression pass. Pure SVG and the full presentation segment passed again, with production artifacts `/tmp/officekit-ppj-preview-vl2H8D`. The last explicit-package run is `/tmp/officekit-native-scene-paint-4x0ZVv` (superseding rAFBXC above for code identity); it retains all independent passes and the same fatal 0/2 anchors, exit 1, persisted integration report. The painter SHA-256 is `2a73506b29f37b7b83d56d4ee052a1a672c649649a7aa12e5aba06acdf09eb92`. Strict OpenSpec, 70 local audit links and whitespace checks pass. No scope or checkbox was changed.

Task 3.4 in-progress evidence (2026-09-09, base `5837c51e`): the existing internal table/connector mappings now have real compiler-to-SVG/raster evidence. Native unequal column widths and row heights, the merged header, visible zero, direct right border and exact green/orange pixels are asserted for authored output, source-bound no-op and a table-frame candidate moved by 24 points. Fresh candidate reprojection gives x=64; the ZIP file inventory and every non-target entry are unchanged, as are original source bytes. An additional explicit-coordinate straight connector paints the reversed directed endpoints and arrow; its source-bound import and preservation across the unrelated table edit are verified. This separate coordinate fixture does not substitute for required object anchors.

The object-anchor regression remains fatal: both original and y+40 target cases compile to `(0,0.5)->(1,0.5)` instead of `(750,320)->(510,120/160)`. The integration now collects these two assertion failures, runs independent table/source checks, prints truthful passed counts (0/2 anchors), and throws AggregateError with exit 1. It does not skip the assertions, change their expected coordinates, loosen schema or report an overall pass. Compiler inspection identifies `BuildConnector`/`EndpointX/Y` ignoring `ElementId/Anchor`; the misleading painter comment claiming resolved anchors was corrected. Compiler semantic repair requires a planning-scope decision, requested but not assumed; no C# or planning contract was changed in this step.

Final explicit-package run: `/tmp/officekit-native-scene-paint-5vZYXO`, PPJ executable SHA-256 `9c97c6605c36c9e2b8218fe208a380745cc25b3b94742c4a620eb4ed21a496e9` (existing temporary build). All independent checks above executed, including source text and the six generated Bézier paths; overall integration failed on both anchors. The source-table-moved PNG was inspected visually, not human-calibrated. Pure native SVG test and `slow/presentation` 4/4 passed (production old-route artifacts `/tmp/officekit-ppj-preview-95r73S`); gate-policy, both generated capability checks and strict OpenSpec passed. Registry now describes bounded table/connector mappings without promoting any support grade. Production CLI is unchanged. No new runtime build, C#/proto validation, complete painter/paired suite, full repository test, external Office comparison or performance acceptance is claimed. Tasks 3.3/3.4 remain unchecked, 7/15 overall.

Task 3.3 in-progress evidence (2026-09-09, base `8b5b6771`): the new internal `preview-scene-svg.mjs` draws validated native shape/text/image/group state without reading canonical PPJ. Rect/textbox/process, ellipse and decision geometry paint alongside rich text; runs remain inline, explicit line breaks and zero text/opacity survive, and native font/color/bold/italic values are used with a persistent text-layout limitation. Groups consume actual child extents and outer rotations/flips; zero child extents fail visibly instead of being substituted. Validated native asset bytes enter image data URIs, without reloading files or guessing subject boundaries. Literal move/line/cubic/quadratic/close paths use their native viewports (omitted axes retain shape EMU semantics); unresolved references/arcs fail the whole path rather than reconnecting remaining segments. Unsupported presets, crop/masks, effects, richer text layout and unpainted content retain scene-addressed limits. The internal result always carries a visible pending-integration warning and does not claim a public G-11/G-12 receipt.

`test/ppj-preview-scene-svg.mjs` is registered in the shared registry and imported by the regular diagnostic gate. It verifies distinguishable geometry and inline run attributes, literal path coordinates, nested group matrices, directed flips/rotation, exact image bytes and zero opacity, hidden nodes, retained chart missing indexes, unknown fields, effect/path/zero-extent failures, immutable input and real in-memory drawing in a fresh process that forbids native/raster/specialist/facade loading. A fixture initially reused a protobuf path frozen by the preceding draw; the test now clones it per receipt rather than weakening scene immutability.

The explicit NativeAOT integration was extended to actually paint and rasterize minimum/canonical inputs, one component/explicit pair with a shared `[1,null,0]` chart risk, compiler-generated Sunburst/Sankey groups and a source-bound text-leaf candidate. The pair asserts native frames, RGB, text `0`, preserved chart values/missing indexes, actual SVG and the exact `[204,85,0]` interior raster pixel. Six actual generated Bézier paths are present in SVG with their native owners. Final run passed against the existing PPJ binary SHA-256 `9c97c6605c36c9e2b8218fe208a380745cc25b3b94742c4a620eb4ed21a496e9`, with SVG/PNG at `/tmp/officekit-native-scene-paint-yMtf47`; this is not a new build or proof of later codec changes. Early fixture errors (invalid color object, missing required vector style) were compiler rejections, corrected against the existing schema/contract without changing the compiler. Visual inspection found native `textbox` initially misclassified as an unknown preset; its codec-defined rect mapping was added and both unit and real runtime tests rerun. The resulting vector geometry and canonical page were visually inspected, not human-calibrated.

Final `slow/presentation` passed 4/4 (production old-route artifacts `/tmp/officekit-ppj-preview-8TMpOD`); gate-policy, both generated capability checks, syntax, strict OpenSpec and whitespace checks passed. No C#/proto change, NativeAOT rebuild, full smoke, full repository tests, host validation, human calibration or performance acceptance is claimed. Task 3.3 remains unchecked: production still uses canonical JSON and the old heuristic; full paired cases, remaining native drawing and G-11/G-12 integration must pass before the planned production switch. Progress remains 7/15, with a real internal drawing foundation rather than a completed G-01.

Task 3.2 implementation evidence (2026-09-09): `preview-scene-view.mjs` validates the compiled receipt and exposes frozen views over the complete native objects, not a copied/pseudo-PPJ visual schema. Generated `PresentationElement.content` descriptors own the vocabulary; all nine cases retain their native fields. Views provide point-space frames (12700 EMU per point), signed angle/opacity/font-unit helpers, optional transform/hidden presence, directed connector endpoints, separate group child coordinates, cached diagram drawing children, exact owner/scene paths, and asset lookup by native ID with the verified MIME/hash payload. Asset bytes are borrowed from the receipt, not reloaded or base64-encoded by the adapter. Unsafe integral conversion fails explicitly. Native chart channels/missing indexes, runs, effects and source/opaque descriptors are retained rather than interpreted or downgraded into a different data model.

The descriptor walk reports unknown JS/wire fields, unknown oneof cases and enum values at their exact scene paths with the real semantic owner when available. Empty or conflicting page ownership is explicitly limited instead of inferred from an ordinal or display name. No adapter result claims a paint grade: `paintAssessment` remains `unassessed`. The shared capability registry records the adapter/test and descriptor source without adding a second content list or promoting any preview type. The new test is imported by the existing diagnostic gate and covers all content cases, nested/generated ownership, distinct source/native asset IDs, unchanged protobuf bytes, optional false/zero/absent, chart missing indexes and X values, exact signed/fractional units, unknown descendants and separate fresh-process root/actual in-memory adapter import checks. Root's pre-existing JSZip dependency is not treated as a new adapter dependency; the adapter-only process forbids JSZip/native runtime as well as raster/specialist/facade imports.

Real `ppj-preview-scene-native.mjs` integration now also runs the adapter over minimum/canonical and source-bound no-op/text-edit receipts against the same explicit PPJ NativeAOT digest recorded in 3.1. It passes actual coordinates, candidate text, assets and native-object identity assertions. `slow/presentation` passed 4/4 with old-route artifacts `/tmp/officekit-ppj-preview-hMJ6U2`; gate-policy, both generated capability `--check` commands and adapter/test syntax checks passed. The last empty-page diagnostic addition was then covered by another adapter-only pass. Work began at `f3ebbb34`; independent chart kerning work became `4782e6e5` and was preserved. No new C# change or build is claimed in this task. SVG does not yet import the adapter: 3.3/3.4 actual drawing, 4.1/4.2 production evidence, 5.x equivalence/runtime/performance and the full G-01 remain open.

Final 3.2 recheck includes the empty-page limitation: presentation 4/4 again (same coverage, not additive; artifacts `/tmp/officekit-ppj-preview-O9lE46`), real NativeAOT scene-view integration again, strict OpenSpec, all 63 local audit links and whitespace checks passed. No files were committed or pushed.

Task 3.1 implementation evidence (2026-09-09): the checked-in `npm run build:office-kit -- --output /tmp/officekit-preview-runtime-xZSfO9` completed with SDK 8.0.128, producing an independent package rather than replacing the installed runtime. The explicit `test/ppj-preview-scene-native.mjs` run passed against its validated manifest and actual PPJ NativeAOT executable (SHA-256 `9c97c6605c36c9e2b8218fe208a380745cc25b3b94742c4a620eb4ed21a496e9`). Two authored fixtures exercise real component/asset scenes; source-bound no-op and text-leaf edits exercise exact source/candidate bytes and candidate assets. Lightweight and generated protobuf readers produce equal scenes, the cross-language deterministic scene digest verifies, and ordinary build/check responses have no scene. The public workspace/native path uses real process transport; only package resolution points to this explicit temporary build, no codec response is mocked.

The initial integration draft incorrectly routed PPJ compile into the packaged `office` executable and received `unsupported_operation`. Source inspection confirmed this is the intended Word/Excel-only profile, distinct from the general C# `CodecProtocol` library entrypoint. The test now asserts that rejection and compares both wire readers against the real `ppj` executable. No Presentation dependency was added to the Office package; the unnecessary scene recursion override in its JS decoder was removed. A Buffer-versus-Uint8Array assertion was corrected to compare exact bytes. A fresh native `FullyQualifiedName~PpjPreview` run with SDK 8.0.128 passed 39/39, including actual general `CodecProtocol` and `PpjCodecProtocol` entrypoints. Synthetic transport regressions remain explicit complementary negative tests for missing/future/malformed scenes, identities, binding coverage, asset corruption, forbidden source authority, preserved optional values and lazy defaults. `slow/presentation` passed 4/4 (old SVG route; artifacts `/tmp/officekit-ppj-preview-GmzKQ2`); gate-policy, both generated capability checks, integration-script syntax and strict OpenSpec passed. `npm run proto:check` completed lint, generation and its diff check with exit 0. These results complete transport only, not task 3.2 onward, scene-to-SVG drawing, or full task 5.2 runtime/equivalence acceptance. The temporary binary is identified by its digest, not claimed to contain later concurrent chart-text changes; the current native test run started from HEAD `6c4c841e` and the closing repository HEAD was `f3ebbb34`.

Task 2.3 implementation evidence (2026-09-09): source and actual-candidate imports collect opt-in physical `(part path, nonzero cNvPr ID)` identities. `PpjPreviewCandidateBindings` joins those identities to requested semantic nodes, rejects ambiguous identities, and never uses candidate ordinal IDs as source edit paths. Tests cover ordinary compile paths, page/element reordering, repeated numeric IDs on different parts, nested-group reading order distinct from input-array order, deletion and appended unknown overlays. Newly appended content with no authoritative source binding remains explicitly unmapped at its known page, not falsely associated with a removed or adjacent owner. Deletion and overlay append are tested separately: their combined request is rejected by the existing source-prefix constraint before preview, and that compiler boundary was not relaxed.

Authored provenance is captured during the original expansion only when preview is requested. `PpjPreviewOrigins` tracks typed-node and actual JSON-clone identities through the existing nested-slot replacement; it records original definition/instance-slot paths separately from legacy expansion/node-map paths and releases temporary bound-model entries. No annotations enter authored JSON and ordinary validation retains no preview origin map. The writer observer uses these paths, classifies component-expanded nodes as Generated, and gives actual vector children their real chart owner rather than fabricated PPJ children. A direct internal prevalidated component compile without captured origins fails explicitly instead of expanding again. Both codec profiles test two-level repeats, nested component definitions, one-to-many nested slot replacement, definition-supplied inner slots, resolvable original owner paths, separate actual scene paths/z-order, distinct instance/repeat identities, unchanged canonical/node-map bytes and exact candidate bytes with preview on/off. Existing vector/missing-index and native asset-byte assertions remain passing.

The combined focused run passed 45/45 with SDK 8.0.128: all preview cases plus six existing component/group/canonical expansion regressions. A subsequent `--no-build` preview-only run passed 39/39 (16 foundation, 12 authored, 11 candidate); these are the same cases, not 39 additional regressions. An additional filter named after the `PpjPresentationTests.cs` file matched no tests because its actual class is the partial `PptxCodecTests`; that empty run is not credited as validation. The six extra regressions above were selected by their actual method names and did execute. `slow/presentation` passed 4/4 (existing canonical-PPJ SVG route, real artifacts at `/tmp/officekit-ppj-preview-yxDA9B`); gate-policy, both generated capability drift checks, whitespace and strict OpenSpec checks passed. Work started at `ade46bd5`; concurrent chart text strike work was committed as `e092c458` during this turn and preserved. This completes native provenance task 2.3 only. JavaScript scene transport/consumption, runtime rebuild, paired actual SVG acceptance and new-scene G-11/G-12 integration remain unchecked; source-bound clone/complex third-party visual fidelity and full G-07/G-14 are not claimed complete.

Task 1.1 implementation evidence (2026-09-09): request field 7 is default-off `include_preview_scene`; result field 13 carries a versioned read-only `PresentationPreviewScene` reusing `PresentationArtifact`. Canonical receipt fields are unchanged. Shared validation is called by both general and PPJ-only entrypoints. `PpjPreviewSceneTests` passed 9/9 using SDK 8.0.128 at `/tmp/officekit-preview-sdk-hfWOmf/dotnet`, covering wire presence, both invalid-operation validators and ordinary scene-free build/check in both profiles. `node test/ppj-preview-scene-wire.mjs` passes and is included through the regular diagnostic test; `npm run test:slow -- --segment presentation` passes 4/4 at `/tmp/officekit-ppj-preview-vygd9W`, retaining G-11/G-12 tests. `node_modules/.bin/buf lint` passes. Two completed generations produced the same binding SHA-256 `2d157838cbb419935d401468115fc3d5eeafb2e074cbe17d7d3ab285f2471f03`. `npm run proto:check` exits 1 at its final git-diff check because the generated binding has uncommitted additions; lint and generation succeeded. No files were staged to alter that result. The previous temporary SDK was missing; the pinned version was restored in a new temporary directory without editing global.json. This proves the contract only: authored/source-bound collection, JS scene consumption and the rebuilt NativeAOT runtime remain pending.

Task 1.2 implementation evidence (2026-09-09): `PpjPreviewSceneBuilder` copies current native fields by descriptor, retaining optional presence and collection order, while stripping SourceBinding/Capability messages, deletion requests, opaque raw XML and OLE replacement requests. The opt-in collector copies each ordered materialized slide once and refuses duplicate, wrong-order, incomplete or repeated sealing; it is not yet connected to the writer. The envelope preserves explicit binding attribution and native asset identities, verifies asset bytes, serializes deterministically and hashes the complete scene with its digest field cleared. Budgets cover retained values and exact wire size (bounded by max_uncompressed_bytes and the absolute request limit), at most 100000 visual nodes and 128 nested message levels; overflow never returns a truncated scene. The focused native suite now passes 16/16, including a descriptor-driven fixture for all 9 current native content cases, separate zero/false/absent assertions, opaque text/frame preservation without its 1 MiB XML payload, caller immutability, digest/candidate changes, asset ordering/mismatch, generated-owner binding retention, and byte/node/depth rejection. These are collector/envelope unit results, not actual writer/source-bound provenance or NativeAOT acceptance; those remain 2.1–2.3 and 5.2. `buf lint`, JS scene wire regression, strict OpenSpec validation and `git diff --check` pass. Concurrent trendline-layout work was committed separately as `f3a67617`; its functionality is outside this scene verification.

Task 2.1 implementation evidence (2026-09-09): `PpjPreviewSourceFreeBuildPlan` observes the original writer plan, delegates materialization/previous-slide requirements and captures the exact validated slide in `RecordNativeBindings`. Ordinary compilation still passes the original plan. `PpjPreviewAuthoredSceneTests` passes 9/9 within the focused 30/30 preview run using SDK 8.0.128: minimum/canonical fixtures in both native protocol profiles preserve exact candidate bytes and canonical fields with scene on/off; compiler-produced heatmap groups and native line missing-value indexes remain distinct; actual master/layout backgrounds and explicitly declared grammar text precedence survive; the real writer sees each tracked page once, receives the same previous page for Morph, and releases the original slide graphs with the option both on and off. Uppercase asset SHA-256 remains accepted and immutable while scene evidence normalizes its digest. The foundation suite remains 16/16; five candidate-import cases also passed in this run, tracked under 2.2. Two test-fixture issues found during verification (native text style lives on runs; master/layout precedence must be explicitly declared) were corrected against the existing compiler contract, not by changing compiler semantics. This does not complete semantic provenance 2.3, JS scene consumption or NativeAOT runtime acceptance.

Task 2.2 implementation evidence (2026-09-09): `PpjPreviewCandidateScene.Attach` imports the exact final candidate via `PptxCodec.Import`, after source-bound no-op, native edit-plan or semantic export selection. A file-backed no-op remains unmaterialized and retains `ReuseSourceFile`; preview adds no re-export. Native asset references are verified against candidate-import bytes; existing projected payloads are reused by MIME/hash, metadata-only assets receive the exact candidate bytes, and lookup does not scan all assets for every reference. The final `FullyQualifiedName~PpjPreview` run passed 31/31: 16 foundation, 9 authored and 6 candidate cases. Candidate tests cover no-op, text leaf, frame fast path and semantic fill, exact scene equality against a separate fresh import, explicit changed text/frame/fill, original input immutability, unchanged non-target ZIP entries, opaque sibling order/raw XML preservation in the candidate with no raw XML in the scene, real SVG asset bytes, file-backed no-op asset availability, and a valid embedded-authored-snapshot case proving the preview importer does not recover that snapshot instead of native state. Full semantic/native bindings remain task 2.3: current candidate nodes are explicitly unmapped with native IDs, exact scene paths and the real program root, not invented editable PPJ child paths. This intermediate attribution is not the final G-01 completion state. The latest presentation segment passed 4/4 at `/tmp/officekit-ppj-preview-t0kuO7`; gate-policy, `buf lint`, both generated capability drift checks, strict OpenSpec validation and `git diff --check` passed. No NativeAOT rebuild or JS scene-consumption acceptance was performed. At closing, another workflow committed the tested scene implementation as `730071c6`, then began chart text language changes. A fresh `npm run proto:check` completed lint/generation but exited 1 on the generated diff for the concurrent optional chart text language field 12; scene wire and whitespace checks passed afterward. No staging was used to change that outcome. The native 31/31 result belongs to the tested scene snapshot, not a fresh validation of later concurrent language changes.

Publication verification (2026-09-09, base `654cb3e5`): the isolated publication snapshot passed all 39 `FullyQualifiedName~PpjPreview` native tests with SDK 8.0.128. `test/ppj-preview-diagnostics.mjs` passed, including the scene transport regression for both wire profiles, default-off forwarding, identity/digest/asset failures, optional presence and lazy loading. `npm run proto:check`, gate-policy, strict OpenSpec validation and whitespace checks passed. This snapshot includes the initial JavaScript transport implementation; its transport fixtures use synthetic receipt bytes and a stubbed native invocation. Task 3.1 remains unchecked pending real rebuilt-runtime integration evidence. No NativeAOT rebuild or scene-to-SVG acceptance was performed; G-01 remains open.

Task 3.4 literal arc progress (2026-09-10): the internal painter now maps resolved DrawingML arcTo to SVG elliptical arcs, including unequal radii, signed sweeps, full turns and close-path pen state. The focused SVG suite passes; unresolved references, invalid radii and angles fail explicitly. The native integration fixture now includes a custom arc, but that updated integration was not rerun during publication. Task 3.4 remains open.
