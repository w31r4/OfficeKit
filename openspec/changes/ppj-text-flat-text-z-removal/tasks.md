## 1. Flat-text depth lifecycle

- [x] 1.1 Implement additive deletion intent and all existing body-style owner paths; verify shared lifecycle, signed bounds, wire conflicts and canonical-child rejection tests.
- [x] 1.2 Update public field references and gap evidence; verify proto:check, reference generation/sync, portability, affected PPJ gates and strict OpenSpec validation.

Evidence: focused native selection 152/152 passed, zero skipped (SDK 8.0.128). C#/JS wire, proto:check, nativeLeaf schema bounds, preview input/capability/scene wire, generated reference/matrix, portability/reference sync and strict OpenSpec passed. No NativeAOT rebuild or host 3D rendering acceptance.
