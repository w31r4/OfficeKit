# Tasks

- [x] Add the source model field, native leaf schema, registry, coverage, and
      Skill reference.
- [x] Add direct child-free `a:sp3d/@extrusionH` projection and edit-plan proof
      with owner-local token splice.
- [x] Add the minimal source-bound/reprojection regression, then run build,
      focused tests, maintainer checks, JSON/OpenSpec gates, and
      `git diff --check`.
