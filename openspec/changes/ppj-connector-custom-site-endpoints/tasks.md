## 1. Endpoint binding

- [x] 1.1 Add the endpoint schema/model, custom-site resolver, authored binding, projection authority and source dependency lifecycle. Verify native target/index and transformed coordinates, fresh projection, original-source no-op, index/target switches, detach/frame-anchor switches, semantic geometry/frame and frame-leaf recomputation, unsupported geometry-leaf rejection and the existing preset authority boundary in focused connector/custom-geometry tests.

## 2. Public contract and verification

- [x] 2.1 Update Help, registry, shape reference, backlog and coverage for connectionSite, including explicit preview limitations. Regenerate associated docs and run preview input/capability, portability/reference-sync, focused native and strict OpenSpec checks; record exact scope and residuals.

Verification (2026-09-10): SDK 8.0.128 custom geometry/connector/authored-preview tests passed 130/130, zero skipped. Initial parallel MSBuild child exit produced no test result; single-process build with repository TMPDIR passed. Preview input/capability coverage, generated reference/matrix checks, portability (255 files with repository TMPDIR after default /tmp ENOSPC), reference sync (333 files), strict OpenSpec and whitespace checks passed. Public schema/model/compiler/projector/resolver and source dependency paths use existing native target/index wire fields. Preview explicitly diagnoses connectionSite; no NativeAOT rebuild or PowerPoint host/drag acceptance. Preset/opaque bindings and bound geometry native-leaf mutation limits remain explicit.
