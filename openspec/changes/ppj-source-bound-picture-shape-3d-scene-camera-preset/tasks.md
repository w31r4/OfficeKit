# Tasks

- [x] Preserve the additive picture source-model field and update the existing
      native leaf registry/reference contract.
- [x] Accept a strict direct picture
      `a:scene3d/a:camera/@prst` owner in projection and source-bound proof,
      then token-splice only its camera preset attribute.
- [x] Add the minimal picture camera-preset regression, run the product build,
      focused test, maintainer/reference/JSON/OpenSpec gates, and
      `git diff --check`.
