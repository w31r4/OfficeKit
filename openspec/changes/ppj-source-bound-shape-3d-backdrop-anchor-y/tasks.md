## 1. PPJ and native model

- [x] 1.1 Add the source model field, native leaf schema, registry, coverage,
  and Skill reference; verify generated/schema JSON parses.
- [x] 1.2 Add strict direct `a:scene3d/a:backdrop/a:anchor/@y` projection and
  edit-plan proof with an owner-local token splice; verify the presentation
  codec builds.

## 2. Regression and gates

- [x] 2.1 Add the minimal source-bound/reprojection regression, then run the
  focused test, Open XML validation, maintainer/reference-sync/JSON/OpenSpec
  gates, and `git diff --check`.
