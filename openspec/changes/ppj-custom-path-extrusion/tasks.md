## 1. Path field lifecycle

- [x] 1.1 Add optional extrusionOk schema/lowering/projection and verify authored true/false/absence, source add/change/delete/no-op and unrelated path edits with exact non-target preservation in a focused native regression.

## 2. Documentation and checks

- [x] 2.1 Document field and preview limits in Help, registry, shapes reference, backlog and coverage; regenerate references/matrix and pass focused preview, portability, reference-sync and strict OpenSpec checks.

Verification (2026-09-10): SDK 8.0.128 CustomGeometry/PpjPreviewAuthoredSceneTests passed 67/67, zero skipped. Preview input assessment, scene SVG, capability coverage, presentation Skill maintenance, capability-matrix freshness (53 boundaries/278 leaves), portability (255 files), reference sync (333 files), strict OpenSpec validation and diff whitespace checks passed. No wire change, NativeAOT rebuild or host appearance claim.
