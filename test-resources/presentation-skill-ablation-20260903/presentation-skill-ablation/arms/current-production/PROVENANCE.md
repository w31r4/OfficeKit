# Current production baseline

This arm is a byte-copied snapshot of the committed production
`skills/presentations/skills/presentations/` tree at the experiment branch
baseline. It is intentionally not routed through the experimental common
overlay and receives no arm-specific design brief.

The snapshot is the control condition. Do not edit it during a run. Any
production Skill change requires a new frozen experiment version rather than
silently changing this arm.
