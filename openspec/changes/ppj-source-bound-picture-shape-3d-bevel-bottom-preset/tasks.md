# Tasks

- [x] Add the additive picture source model field and preserve the existing
      native leaf/schema/registry/reference contract.
- [x] Accept a strict direct picture `a:sp3d/a:bevelB/@prst` owner in
      projection and source-bound proof, then token-splice only its preset
      attribute.
- [x] Add the minimal picture bottom-bevel-preset regression, run the product
      build, focused test, maintainer/reference/JSON/OpenSpec gates, and
      `git diff --check`.
