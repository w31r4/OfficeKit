interface:
  display_name: "Grid Layout Library"
  short_description: "Use the Grid Layout Library clean-room visual grammar"
  icon_large: "./assets/preview.png"
  default_prompt: "Create a new presentation using the Grid Layout Library visual grammar."
