## 1. Guide graph lifecycle

- [x] 1.1 Add geometry.guides to schema, authored/projection/source setGeometry paths and shape-only ownership; verify ordered formulas feeding a text rectangle, evaluated source changes, add/delete, reference failures and retained paths/text/frame/non-target ZIP bytes in one fixture.

## 2. Discoverability and checks

- [x] 2.1 Update Help, registry, shapes reference, backlog and coverage; regenerate docs and pass custom-geometry/preview regression, explicit preview limitations, portability/reference sync and strict OpenSpec checks.

Native evidence: SDK 8.0.128 custom-geometry/preview filter passes 65/65, zero skipped. Fixture verifies ordered guide/rectangle identity, actual inset evaluation 20pt -> 40pt, original-source add/edit/coordinated removal, unchanged path XML/text/frame and non-target ZIP parts; duplicate/forward/dangling references and non-shape owners reject.

Closing checks pass: generated PPJ manual and capability matrix, maintainer check, preview guide/formula assessment, native-scene SVG and capability coverage, portability (255), reference sync (333), strict OpenSpec and whitespace. No wire change, NativeAOT release build or PowerPoint layout acceptance.
