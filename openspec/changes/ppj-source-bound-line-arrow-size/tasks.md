# Tasks

- [x] Add four arrow-size native leaf kinds to normalization, schema enum,
      capability registry, and generated/reference coverage without a
      protocol change.
- [x] Add bounded shape/connector projection, source proof, readback, and
      one-attribute XML splice for existing endpoint width/length tokens.
- [x] Add the minimal shape/connector source-bound regression, including an
      absent-size refusal.
- [x] Run focused line tests, maintainer checks, JSON/OpenSpec gates, and
      `git diff --check`.
