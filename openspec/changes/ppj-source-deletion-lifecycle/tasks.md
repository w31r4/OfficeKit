## 1. 固定基线与独立回归入口

- [ ] 1.1 冻结实施时实际源码、SDK 和原生包身份，运行已有 frame、三个 paragraph spacing lifecycle 与背景源写回的窄托管测试；保存命令、摘要及结果，区分近期源码已修复项和旧 035472e9 包仍失败项。
- [ ] 1.2 将原 4 + 7 + 1 删除案例整理为可独立执行/汇总的正式回归，fixture 在测试中再生成，每份删除请求从同一原始源重新投影；验证失败仍能保留源、请求、候选或错误阶段，其他 locale 失败不会阻断这 12 项的收集。

## 2. Frame 属性真正删除

- [ ] 2.1 补充 shape/image/group 的旋转、水平翻转、垂直翻转及组合删除托管反例，包含显式零/false → 缺省和只删一项的混合请求；断言原生 xfrm 属性、新投影字段及源/非目标 XML，记录修复前实际结果。
- [ ] 2.2 修正 frame 存在性识别与快速写回路由，复用可清除属性的局部写回；验证 2.1、显式零/false、恢复、普通坐标修改通过，chart/table 既有行为及 connector 拒绝保持。
- [ ] 2.3 为 frame 删除补精确字段权限缺失、绑定篡改和不支持源内容的反例；验证全部拒绝且无候选，原输入不变，no-op 逐字节不变。

## 3. 段落间距组合删除

- [ ] 3.1 复用现有 paragraph spacing lifecycle helper，加入 text/shape 的七种倍数删除 mask、点值/混合单位、只含目标间距的 style 删除及恢复；逐项验证 XML 槽位真正消失、新投影单位和值、未改数值拼写、runs 和相邻段落保留。
- [ ] 3.2 对新源码中仍失败的组合，修正变化检测、能力检查或槽位局部写回；若已全部通过则记录无需生产代码变更。验证三个既有 lifecycle 测试、双单位/非法零值和 source-owned 未建模间距反例保持。

## 4. 共享图片背景删除

- [ ] 4.1 新增背景与前景共用同一图片关系的托管源编辑反例，断言原始删除能力及背景修改后的变化，验证错误来自原始绑定与候选状态混用；记录 no-op、请求和完整部件清单。
- [ ] 4.2 将受影响的源能力分析和绑定验证固定在背景修改之前，保留全部绑定检查和现有引用清理；验证删除后背景缺省、前景图片子树/媒体/关系逐字节保留，固定案例仅 slide1.xml 改变且 Open XML 检查通过。
- [ ] 4.3 覆盖缺少背景权限、伪造前景删除能力、错误源/摘要、opaque 背景及未授权组合删除；验证拒绝且无输出。复验已有不共享背景删除、裁切/透明度修改、图片替换与共享媒体保留行为。

## 5. 新原生包与交付证据

- [ ] 5.1 使用固定源码及仓库 `build:office-kit` 命令构建到新的独立目录，核验 PPJ/Office 两个 profile、manifest、notices、SBOM 与文件摘要；保留原默认包，记录构建命令和退出状态。
- [ ] 5.2 指定 5.1 新包运行 12 项独立 NativeAOT 回归，验证源/请求不变、候选结构、重新投影、非目标内容、scene 开/关候选相同及限定视觉参考；以逐项 12/12 和制品摘要作为原生修复验收，不以图像生成成功替代。
- [ ] 5.3 在同一冻结 JS/原生包上运行完整 scene-native 集成及无 preload 的 presentation 门禁，记录全套状态和全部其他失败；新增 locale 问题不得被清空、降级或并入本次授权。
- [ ] 5.4 更新相关 PPJ 能力/Skill 条目、差距报告和本任务状态，运行相应生成/参考同步检查、文档链接检查、`openspec validate ppj-source-deletion-lifecycle --strict` 与 `git diff --check`；每项结论注明托管源码、新构建包或默认包范围，保留未做的宿主/人类验收。
