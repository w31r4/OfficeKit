## 1. Rectangle lifecycle

- [x] 1.1 Add the PPJ rectangle schema, authored and projected state, source setGeometry authority and shape-only owner guard; verify numeric/reference/mixed presence, add/edit/delete, unchanged path/text/frame and non-target ZIP bytes plus invalid rectangle rejection in a focused regression.

## 2. Documentation and preview boundary

- [x] 2.1 Update Help, registry, shapes reference, backlog and coverage; regenerate docs and pass native custom-geometry tests, preview limitation checks, portability/reference sync and strict OpenSpec validation.

Native evidence: SDK 8.0.128 custom-geometry and authored-preview filter passes 64/64, zero skipped. Original-source fixture verifies numeric/reference/mixed rectangles, optional deletion and addition, path XML/text/frame and non-target ZIP preservation, invalid references/bounds, missing authority and shape-only lowering.

Closing checks pass: final native suite 64/64, preview input assessment including all rectangle edges, scene SVG regression, preview capability coverage, generated manual/matrix maintenance, portability (255), reference sync (333), strict OpenSpec and whitespace. Native wire is unchanged; no NativeAOT rebuild or PowerPoint layout acceptance was performed.
