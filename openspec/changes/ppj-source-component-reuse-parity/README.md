# PPJ source component reuse parity

Extend the finite `sourceClone` macro so an Agent can keep one exact top-level
element from a proven imported page, let the native clone writer delete every
independently proven sibling, and reimport the result as an ordinary editable
source-bound page.
