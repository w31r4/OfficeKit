# PPJ paired SVG asset parity

Expose the true SVG member of an imported Office fallback-picture pair so an
Agent can replace the editable vector source without losing its native PNG or
JPEG fallback.
