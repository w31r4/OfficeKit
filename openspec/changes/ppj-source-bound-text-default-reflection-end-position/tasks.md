## 1. Native projection and editing

- [x] 1.1 Add the `textDefaultReflectionEndPosition` leaf, bounded position validation, and paragraph default reflection proof/patch support; verify the focused source-bound regression passes.
- [x] 1.2 Extend the PPJ schema leaf enum and capability metadata; verify JSON/schema and capability checks pass.

## 2. Evidence and validation

- [x] 2.1 Update the presentation Skill, coverage, matrix, and gap backlog with the bounded field evidence; verify generated/reference synchronization checks pass.
- [x] 2.2 Run focused .NET tests, OpenSpec strict validation, and affected repository gates; verify the change is ready for an atomic commit and push.
