## 1. Contract and projection

- [x] 1.1 Add the PPJ schema, capability registry, generated matrix, Skill/reference, coverage, and backlog wording for `run.style.reflection.skewY` / `textReflectionSkewY`.
- [x] 1.2 Parse authored `skewY`, project the direct-run degree, and add the native leaf.

## 2. Source-bound lifecycle

- [x] 2.1 Extend direct-run topology proof and leaf allow-lists for one canonical `ky`.
- [x] 2.2 Add the token-splice edit and a focused authored/edit/reproject regression with non-target ZIP preservation and fail-closed cases.

## 3. Validation

- [x] 3.1 Run strict OpenSpec validation, focused codec tests, generated-surface checks, Skill portability/reference sync, and the NativeAOT build; record the environment-only `pdftoppm` skip from the broader reference-skills suite.
