# Tasks

- [x] Add the additive wire field, PPJ schema, registry, generated bindings,
      coverage, and Skill reference.
- [x] Recognize canonical direct `a:flatTx/@z` in authored build, import,
      projection, and source-bound text-body style paths.
- [x] Add `textBodyFlatTextZ` proof/readback and direct attribute token splice
      with fail-closed topology checks.
- [x] Add the minimal authored/imported/source-bound/reprojection regression,
      then run build, focused tests, maintainer checks, JSON/OpenSpec gates,
      and `git diff --check`.
