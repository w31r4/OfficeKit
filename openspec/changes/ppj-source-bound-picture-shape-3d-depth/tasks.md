# Tasks

- [x] Add the additive picture source model field and preserve the existing
      native leaf/schema/registry/reference contract.
- [x] Accept a strict direct picture `a:sp3d/@z` owner in projection and
      source-bound proof, then token-splice only its `z` attribute.
- [x] Add the minimal negative-coordinate picture regression, run the product
      build, focused test, maintainer/reference/JSON/OpenSpec gates, and
      `git diff --check`.
