## 1. Field and source lifecycle

- [x] 1.1 Add schema/wire/native mapping and authored precedence; regenerate bindings and verify true/false/absence, independent Latin/punctuation/wrap/break settings and invalid input in native experiments.
- [x] 1.2 Add exact source authority and per-paragraph lifecycle; verify text/shape removal/restoration, style-wrapper removal, boolean spelling, unknown-token refusal and non-target XML/ZIP preservation.

## 2. Discovery and integration

- [x] 2.1 Update Help, registry, generated manual/matrix, focused text guidance and F-03 backlog; verify explicit preview input/native diagnostics and documentation checks.
- [x] 2.2 Run proto:check, affected paragraph/list/master/table regressions, Skill and strict OpenSpec checks; record results and verify the scoped atomic publication.

## Validation evidence

- Focused native experiments: 4/4 passed (text/shape lifecycle, authored precedence and invalid inputs, unknown native token preservation/refusal), SDK 8.0.128.
- Preview input diagnostics for both booleans, scene wire, and generated Skill maintenance checks passed. The source edit compiler keeps all 41 paragraph diff masks independent.
- Delivery covers codec source and generated bindings. Measured East Asian line breaking, kinsoku evaluation and host layout remain explicitly partial; no NativeAOT rebuild or host acceptance is claimed.

- Related native paragraph/Latin/punctuation/direction/list/master/table regressions: 17/17 passed. proto:check, Skill portability/reference-source sync, preview capability coverage and strict OpenSpec validation passed.

- Isolated publication candidate: 18 field-related files. Generated manual/matrix, preview input/wire/capability checks and strict OpenSpec validation passed against the staged snapshot. Native source/test files match the versions used for the 4/4 and 17/17 runs; concurrent preview documentation is excluded.
