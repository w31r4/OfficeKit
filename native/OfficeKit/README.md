# OfficeKit

OfficeKit is this repository's clean-room C# codec for XLSX, DOCX, and PPTX. It uses the public Microsoft Open XML SDK and the independently defined protocol in `proto/office_kit/artifact/v1/office_artifact.proto`.

It is the only Office codec used by version 0.3. The JavaScript package supplies the public artifact model and wire adapter; it does not contain a fallback Office parser/writer. PDF is outside OfficeKit.

The public package exposes this implementation only through
`office-kit/codec` and `office-kit/codec/wire`. There is no alternate Office
codec, compatibility bridge, selector, or fallback.

## Projects

- `OfficeKit.Codec` implements package validation and the XLSX/DOCX/PPTX codecs.
- `OfficeKit.Runtime` exposes the byte-in/byte-out WebAssembly entry point.
- `OfficeKit.Codec.Tests` covers supported creation/import/edit/export profiles, opaque preservation, and fail-closed cases.

`OpenXmlChartSpaceCodec` is the package-agnostic DrawingML ChartSpace core used
by both worksheet and slide ChartParts. The XLSX and PPTX adapters keep their
format-specific relationship, source-binding, and edit policies around that one
semantic reader/writer/patcher instead of maintaining divergent chart XML
implementations.

`OfficeKit.Runtime` keeps its one `byte[] -> byte[]` JavaScript entry point
as a small explicit registration shim in `OfficeKitJavaScriptInterop.cs`. This avoids
the .NET 8 `[JSExport]` generator's process-random wrapper-name hash while
retaining the same public WASM ABI. The paired clean-build gate verifies the
generated runtime file inventory and hashes rather than merely trusting an
incremental build.

Protocol version 2 removes `allow_lossy`; the removed field name and number are reserved. Opaque content can be exported only from a validated, hash-bound source package. Unsupported edits, source-evidence mismatch, unsafe OPC paths, invalid relationships/content types, and missing runtime data return structured failures.

## Supported 0.3 slices

- XLSX: cells/formulas/styles, merges/dimensions/freeze panes, passwordless worksheet editing restrictions, tables, PNG/JPEG images, charts, standard Office 2010 sparklines, canonical one-/two-variable What-If data tables, a bounded native PivotTable profile, seven data-validation rule types with bounded prompt/error/drop-down UX, six conditional-format profiles, and bounded Office 2019 threaded comments. Worksheet protection exposes all 15 operation classes as an agent-facing allow-list while the codec owns SpreadsheetML's inverted lock attributes/defaults; password/hash/extension and disabled/partial profiles remain opaque/source-bound and fail closed on replacement. Protection is not encryption or access control. Conditional formatting covers cell/formula/text/color-scale rules plus standard gradient data bars and all 17 base SpreadsheetML icon sets with typed thresholds, `showValue`, and icon reversal. x14 solid/negative-axis data bars, x14-only/custom icons, exclusive/formula thresholds, multi-area/extension validation graphs, and richer rule graphs remain opaque/source-bound. Pivot authoring owns 1 through 8 ordered row fields, zero or one column field, 1 through 32 sum/count/average/min/max value fields, optional grand totals, exact typed include/exclude filters, and optional saved cache records; multiple values use the canonical `x=-2` data-layout axis. Recognized imports are source/hash/topology-bound and read-only when editing cannot be proven safe. Compact/subtotal-bearing multi-row and multi-column graphs, non-marker scatter, noncanonical bubble profiles, richer Pivots/data tables, nested comment replies, mentions, and extended identity graphs remain opaque/source-bound.
- DOCX: styles, paragraph/run formatting, pages/sections, canonical 1–45 equal-width or explicit-width section columns, headers/footers, PAGE/simple fields, canonical inline five-run SEQ/REF/PAGEREF fields with an optional narrow bookmark around a SEQ cached result, one canonical one-paragraph complex TOC placeholder plus `updateFields` intent, writable `trackRevisions` intent, canonical facing-page `mirrorMargins`, per-section binding gutters plus canonical `gutterAtTop`, passwordless `documentProtection` (`none`, `readOnly`, `comments`, `trackedChanges`, or `forms`) with explicit enforcement/formatting flags, PNG/JPEG images, lists, links, classic comments plus bounded modern root/direct-reply threads with resolved/durable/UTC/person metadata, fixed-geometry tables, bounded whole-block bookmarks/internal hyperlinks, plain-text paragraph/list-item footnotes/endnotes, standalone whole-paragraph `w:ins`/`w:del` revisions, canonical inline plain-text `w:sdt`, Word 2010+ `w14:checkbox`, `w:dropDownList`, `w:comboBox`, and strict ISO/Gregorian `w:date` controls, plus one bounded bibliography `b:Sources` catalog with whole-paragraph `CITATION` fields and one source-free canonical switch-free `BIBLIOGRAPHY` output placeholder. The output requires at least one modeled source, preserves only its cached display after canonical import, and relies on a compatible Word host to calculate formatted entries. Explicit-width columns own an ordered width/spacing-after definition per native `w:col`; ignored equal-width root attributes, missing widths, unknown children, extensions, duplicates, and other irregular column/page-margin graphs remain source-owned, fail closed on semantic replacement, and make affected section geometry read-only. Protection is an editing restriction, not encryption; password verifiers, cryptographic/extension attributes, IRM, and permission exceptions remain source-owned and fail closed on replacement. The date profile owns exact `YYYY-MM-DD` visible text, UTC-midnight `w:fullDate`, `yyyy-MM-dd`, `en-US`, `date` mapping, and Gregorian calendar metadata; other date profiles remain opaque. A file-level transaction adds one exact in-paragraph replacement to original hash-bound bytes as adjacent native `w:del`/`w:ins` wrappers; it accepts a structured direct-body paragraph or bounded physical table-cell selector and requires the full paragraph/cell snapshot plus one unique literal. The literal may occupy one ordinary `w:r/w:t` or adjacent non-empty ordinary runs with identical `w:rPr`; the deletion retains every matched fragment, the insertion uses the shared formatting, and `matched_source_run_count` audits the span. Mixed formatting and broader graphs fail closed. Only `word/document.xml` changes, with target/byte/element/text/native-ID evidence returned. The table profile requires a direct body table, stable grid, non-continuation cell, and exactly one direct paragraph. Revision finalization accepts or rejects both exact-pair profiles—including same-format fragmented deletions—and the direct whole-paragraph/one-run profile, reports exact changed parts and tracking state, and rejects other story parts or complex graphs before returning output. A separately advertised `text_patchable` capability permits one source-hash-bound untracked literal replacement inside one ordinary direct `w:r/w:t` or across adjacent non-empty direct runs with byte-identical `w:rPr` in an otherwise read-only paragraph or complex table cell. The shared literal-span resolver keeps native run/text topology fixed and is also used by tracked replacement. Duplicate matches, mixed formatting, empty-run gaps, paragraph boundaries, hyperlinks, fields, controls, revisions, capability tampering, residual-topology changes, and whole-paragraph/cell replacement fail closed. Imported recognized modern threads permit text and resolved-state edits while paragraph/durable/person/UTC identity, root anchors, and topology remain source-bound. Generated modern-comment paragraph IDs and explicit inputs are constrained to the Open XML `00000001`–`7FFFFFFF` range. Imported canonical inline-field cached results, TOC placeholders, note bodies, typed content-control values, bibliography source content, citation display text, and one canonical bibliography-output cached display permit fixed-topology edits while field positions/instructions/bookmark identity, anchors, native identity, control type/symbols/list choices/native date profile, source order, and tags remain source-bound; imported whole-block bookmarks remain read-only. Nested/irregular modern comments, automatic field materialization, refreshed cross-paragraph TOCs, refreshed/multi-paragraph bibliography output, and other rich or irregular content-control/bibliography/field/bookmark/note/revision graphs remain opaque/source-bound or explicit package workflows.
- DOCX note paragraphs: source-free or recognized source-bound footnotes/endnotes have 1 through 16 physical plain-text paragraphs. The first owns the native `w:footnoteRef`/`w:endnoteRef` marker plus one text run; each continuation has one text run. The wire carries the exact `paragraphs` array plus an LF-joined `text` projection. Imported edits may change only text in the fixed count; anchor, native ID, formatting, and topology stay source-bound. Bodies beyond 16 paragraphs, rich/multi-run paragraphs, reused references, custom note graphs, and anchor movement remain opaque or fail closed.
- DOCX section page numbering: `pageNumbering` authors/imports one canonical `w:pgNumType` with an optional integer start from 0 through 2147483647 and/or decimal, upper/lower Roman, or upper/lower letter format. Omitted start continues the prior sequence; PAGE-field insertion and refresh remain separate. Recognized source-bound leaves support fixed-topology edits/removal, while duplicate, empty, child-bearing, extension-bearing, chapter-numbered, unsupported-format, or otherwise irregular graphs remain source-owned and fail closed on semantic replacement.
- DOCX paragraph shading, pagination, contextual spacing, outline, and line numbering: `paragraphFormat.shadingFill` authors/imports one canonical direct or source-free named-style `w:shd` with a six-digit RGB fill, `w:val="clear"`, and `w:color="auto"`; a recognized ordinary direct paragraph can add, change, or clear that fixed fill as part of its modeled direct-formatting profile, while patterns, theme colors, and imported style catalogs remain source-bound. `paragraphFormat.keepNext`, presence-aware boolean `keepLinesTogether`, presence-aware boolean `widowControl`, and `pageBreakBefore` author/import canonical direct or named-style `w:keepNext`, `w:keepLines`, `w:widowControl`, and `w:pageBreakBefore`; they remain independent host pagination directives rather than a layout engine. `paragraphFormat.contextualSpacing` authors/imports presence-aware canonical direct or named-style `w:contextualSpacing`: true suppresses before/after spacing between adjacent paragraphs with the same style, false explicitly overrides an inherited value, and omission inherits. `paragraphFormat.outlineLevel` authors/imports canonical direct or named-style `w:outlineLvl`: `0` through `8` are native outline levels, `9` explicitly removes an inherited level, and omission inherits. `lineNumbering` authors/imports one canonical `w:lnNumType` with a 1-through-32767 display increment, optional zero-based 0-through-32767 start, optional 0-through-31680-twip distance, and optional new-page/new-section/continuous restart. `{}` normalizes to every-line numbering. `paragraphFormat.suppressLineNumbers` authors/imports a presence-aware canonical direct or named-style `w:suppressLineNumbers`: true excludes the paragraph from display/calculation, false explicitly overrides inherited style suppression, and omission inherits. Recognized source-bound leaves support fixed-topology edits/removal; duplicates, children, extensions, invalid lexical/numeric values, unknown restart modes, and other irregular graphs remain source-owned and fail closed on semantic replacement.
- DOCX paragraph borders: `paragraphFormat.borders` is one nonempty `top`/`left`/`bottom`/`right`/`between`/`bar` profile. Each edge owns a six-digit RGB color, `2..96` eighth-point `size`, and optional `0..31`-point `space`; the codec writes exactly `w:val="single"`, `w:color`, `w:sz`, and `w:space` beneath canonical `w:pBdr`. Recognized ordinary direct paragraphs can add, replace, or clear the complete profile. Source-free paragraph styles can author it, while imported style catalogs, themes, patterns, frame/shadow flags, other border styles, duplicate edges, child-bearing/extension markup, and irregular attributes remain source-owned and fail closed on semantic replacement.
- DOCX floating images: absence of a placement record remains canonical inline flow. The bounded `wp:anchor` profile is foreground, non-overlapping, layout-in-cell, absolute-offset only, and supports horizontal margin/page/column plus vertical margin/page/paragraph references, square or top-and-bottom wrap, and text distances. Recognized imports permit fixed-topology placement edits; inline/floating transitions, behind-text/overlap, tight/through wrap, aligned/percentage positions, relative sizing, effects, external images, and irregular drawing graphs remain opaque/source-bound and fail closed on replacement.
- DOCX picture bullets: source-free lists may author one canonical VML `w:numPicBullet` resource per equal embedded PNG/JPEG/GIF or external HTTP(S) marker and reference it from a numbering level. Recognized imports expose the marker semantically and preserve exact bytes when unchanged. A source-bound edit requires every list item sharing the native instance and level to change coherently, keeps the embedded/external source kind fixed, and writes a new instance-local level override rather than mutating a potentially shared original resource. Partial edits, source-kind transitions, inherited/irregular numbering, DrawingML, transforms, and unknown VML stay opaque/read-only or fail closed.
- DOCX text watermarks: source-free documents may author one canonical VML `v:textpath` watermark paragraph per section/default-first-even Header scope. An imported Header part is editable only when it is referenced by exactly one scope and contains exactly one exclusive recognized watermark paragraph. Source-bound edits may change only the text-path string or remove that whole paragraph; element, semantic, shape, and residual-header hashes are re-proved and only the Header part is marked mutated. Adding to imported relationship topology, changing scope/order, shared Headers, multiple watermarks, DrawingML/images, and irregular VML stay opaque/source-bound or fail closed.
- DOCX block plain-text controls: one canonical body-level `w:sdt` wraps exactly one modeled `w:p` containing one ordinary `w:r/w:t`. The public paragraph carries separate block-control metadata, so JS inspection reports block placement instead of inventing a run index. Text, tag, alias, and supported paragraph/run formatting may change after import; native ID, wrapper placement, and one-paragraph/one-run topology remain source-bound. Multi-paragraph, table/cell, nested, rich-text, locked, placeholder, repeating-section, data-bound, or extension-bearing block SDTs stay opaque and fail closed on semantic replacement.
- DOCX table-cell content controls: one canonical cell-level `w:sdt` wraps the only `w:p/w:r/w:t` in a source-free rectangular physical cell and may be plain text, Word 2010+ checkbox, standard drop-down, standard combo box, or the strict ISO/Gregorian date profile. Public handles report `placement: "tableCell"` with row/column coordinates and participate in the same typed inspect/resolve/fill/set/verify contract as inline controls. Recognized imports preserve unchanged bytes and permit only type-appropriate state plus tag/alias edits; native ID, type, cell placement, choice/symbol/date profile, and wrapper/table topology remain source-bound. Adding/removing imported controls, continuation/irregular cells, and rich, inline-within-cell, nested, multi-paragraph, data-bound, locked, placeholder, repeating-section, or extension-bearing cell SDTs stay opaque and fail closed.
- DOCX canonical list controls: source-free and recognized inline `w:dropDownList` and `w:comboBox` SDTs expose an ordered display/value table. Drop-downs accept only a declared `selectedValue`; combo boxes accept a declared choice value or bounded custom `value`. The codec derives visible text, permits only typed value/tag/alias edits after import, and source-binds choice order, values, display text, native ID, type, and run topology. Irregular list-control graphs and controls outside body runs remain opaque/source-bound.
- PPTX: direct RGB/theme solid and native style-reference slide backgrounds, text boxes/round rectangles, basic fill/line/shadow, straight/elbow/curved connectors with target connection-site indexes, bounded line ends, caps, and joins, source-free literal bar/line/pie/standard-area/fixed-50%-doughnut/marker-scatter/2D-bubble charts plus the bounded clustered bar+line combo, bounded embedded rectangular images with signed DrawingML `a:srcRect` crop, fixed-grid tables with canonical non-overlapping rectangular merges, recursive native `p:grpSp` trees with local `chOff/chExt` coordinates, rich text/lists/links, canonical plain-text speaker notes, native custom shows with relationship-free run hyperlinks, legacy slide annotations, bounded Office 2021 modern root/direct-reply threads with top-level element or shape-text-range anchors, source-bound Master/Layout preservation, and presentation view metadata (grid spacing, snap flags, and horizontal/vertical guides). A relationship-free imported fixed-topology `viewProps.xml` profile may change only values of existing grid/snap attributes and existing guide positions; it preserves guide count/order/orientation, `showGuides`, relationships/extensions, and a hash of every other XML leaf. Notes import exposes a re-proven addability claim for a notes-absent SlidePart only when its presentation has one reusable NotesMaster or a canonical first SlideMaster ThemePart. Export re-proves that graph, reuses the existing master byte-for-byte or creates one bounded master, adds one NotesSlide with only its master and owner back-reference, allocates collision-free IDs across every relationship family, and validates the written graph; inconsistent/multiple masters, missing themes, and capability tampering fail closed. A separately advertised legacy-comment addability claim is true only when the complete imported presentation has no legacy or Office 2021 comment graph. The bounded transaction creates one canonical shared `CommentAuthorsPart` plus closed numbered `SlideCommentsPart` leaves, allocates collision-free owner relationships, preserves every SlidePart byte, and validates author/index semantics after write; an existing/mixed/connected graph, a second add, or capability tampering fails closed. Existing imported legacy comments are unchanged-only outside the separate bounded root-text profile. Table merges use explicit public ranges and native `gridSpan`/`rowSpan`/`hMerge`/`vMerge`; covered cells stay empty/read-only, and recognized imports may edit only visible text and complete geometry without changing merge topology. Canonical imported custom shows permit fixed-topology name and ordered retained-slide membership edits while show count/order, native identity, and source hashes remain fixed; canonical `ppaction://customshow?id=...` run links bind to that stable identity across a rename and support presence-aware `return=true|false`. The bounded imported-slide clone copies those canonical relationship-free actions to the new SlidePart after resolving the native ID, while leaving the presentation-wide show identity and membership unchanged. Extension-bearing, unresolved, duplicate-identity, and otherwise irregular lists and malformed, relationship-bearing, or dangling custom-show actions remain opaque-preserved and fail closed on replacement or clone. Recognized imported modern threads permit existing text/status edits while author/date identity, anchor/range, position, topology, relationships, and source hashes remain fixed; reactions/task fields, nested replies/anchors, rich text, mixed families, and connected comment parts remain opaque/source-bound. Recognized imported direct backgrounds, source rectangles, canonical fixed-topology groups, and the presence-aware `p:sp/@useBgFill` flag are projected without flattening inheritance or surrounding package graphs; `useBgFill` is preview-visible but read-only and source-bound. Eligible top-level OLE objects also allow one source/hash/relationship-bound XLSX payload replacement: the replacement workbook is budgeted, opened by the Open XML SDK, and Office 2021 validated while the OLE shell, preview, relationship topology, and unrelated native graph remain unchanged. JavaScript computes `contain`/`cover` into the native source rectangle before crossing the wire and owns local gridline/guide visibility; the native `viewProps.xml` graph remains source/hash-bound except for the re-proven fixed-topology leaf-value profile. Custom-show membership/topology mutation during clone and delete identity references, advanced background/group/view graphs, shared or ambiguous OLE payloads, external or masked/effect-bearing pictures, and rich or irregular notes remain opaque-preserved or fail closed on mutation.
The bounded PPTX custom-geometry profile supports literal paths plus one
ordered `a:avLst`/`a:gdLst` graph. Its source-built formula module validates and
evaluates all 17 ECMA-376 guide operators under fixed name/count/length and
signed-32-bit budgets. Formula operands may use integer literals, DrawingML
built-ins, or earlier guides; path points and arc radii/angles may use only a
literal or one declared adjustment/guide name. Forward references, reserved or
duplicate names, undefined path references, invalid arithmetic, and invalid
evaluated arc bounds fail closed. Path `w`/`h` remain positive literals. The
same graph crosses the public protobuf wire and survives top-level/grouped
authoring, import, fixed-topology edit, export, and second import.

The profile also preserves the presence of each `a:path` `fill`, `stroke`, and
`extrusionOk` attribute. It owns explicit `norm`/`none` fill modes and booleans;
omitted attributes remain omitted. One optional shape-local text rectangle
carries the custom shape's text bounds. The codec reads native numeric `a:rect`
coordinates and emits one exact four-guide scaling profile so PowerPoint and
LibreOffice resolve the same EMUs; absence retains the full-shape default.
Unknown formula syntax, non-empty handles/connection sites, every other
formula-valued text rectangle, and child-bearing leaves keep the entire shape
opaque. Relative lighten/darken fills and actual 3D geometry remain opaque or
fail closed. `extrusionOk` is eligibility metadata, not a 3D authoring or
preview claim.

- PPTX legacy comment source edit: a recognized existing closed legacy `SlideCommentsPart` may be source-bound text-editable only when its presentation has exactly one relationship-free `CommentAuthorsPart`, the concrete comments leaf is relationship-free, and every comment's author, timestamp, coordinate, native author/index, order, and count re-proves. Export changes only existing `p:cm/p:text` values; all metadata, relationships, and other package parts remain fixed. This is the sole exception to legacy comments' otherwise source-bound read-only treatment; mixed, connected, rich, or otherwise irregular graphs remain opaque and fail closed.
- PPTX PowerPoint sections: source-free export writes one canonical Office 2010 presentation extension with URI `{521415D9-36F7-43E2-AB2F-B90AF26B5E84}` and one `p14:sectionLst`. Each named section owns a unique brace-delimited GUID plus one or more native `p:sldId/@id` values, and the complete list must partition the current deck in order. Canonical imports surface fixed section identities and permit only name and boundary edits that retain the same count/order/GUIDs and complete partition; the codec rewrites only `ppt/presentation.xml`, re-reads its output, and verifies the profile. Missing/duplicate/unknown extension nodes, invalid names or GUIDs, unresolved/empty/repeated/out-of-order memberships, section topology mutation, and combinations with pending clone/delete stay opaque or fail closed rather than being reconstructed.
- PPTX imported placeholder text: a concrete SlidePart placeholder keeps `editable=false` for the surrounding shape and carries a separate `text_editable` source claim only when its local `p:txBody` is fully recognized. Export accepts textual-content deltas with identical paragraph/inline kinds and formatting, then applies them in place through the Open XML SDK. Placeholder identity, geometry, style, layout binding, and unmodeled XML remain hash-bound; capability tampering, formatting changes, topology changes, fields outside the bounded request, and unsupported native text graphs fail closed.
- PPTX imported slide clone: the bounded unchanged SlidePart leaf accepts canonical run-level clicks for an external absolute URI, an internal jump to a retained SlidePart, or one of five relative slide actions. Preflight requires every relationship-backed click to use a recognized inline run property and every hyperlink/slide relationship to be consumed; execution copies the exact `r:id` and target onto the fresh SlidePart without rewriting source XML. Postwrite re-proves that external URIs and internal target parts match exactly. The same leaf profile accepts recognized literal-data chart frames only when each uniquely consumes one internal numbered `ChartPart` relationship whose child, external, hyperlink, and data relationship sets are empty; execution byte-copies each payload into a distinct clone-local ChartPart and re-proves exact relationship use, source bytes, and allowed new parts. Eligible top-level embedded-XLSX OLE frames may likewise consume one uniquely inbound closed package edge plus one preview-image edge apiece; execution creates distinct byte-identical packages, shares the previews, and rejects nested/shared/external/non-XLSX/relationship-bearing/replacement-pending graphs. Formula/external-data/embedded-workbook/connected/orphan charts, shape-level and hover clicks, unknown actions, orphan hyperlink relationships, links in non-shape leaves, and jumps to a removed slide remain fail-closed.
- PPTX SmartArt clone leaf and plain-node text edit: one top-level `p:graphicFrame` is accepted for cloning only when a single canonical `dgm:relIds` root uniquely binds the standard `dm/lo/qs/cs` relationship roles to four non-empty, relationship-free diagram data/layout/quick-style/colors parts with matching content types. Execution preserves the slide-local IDs, byte-copies all four into distinct SDK-typed clone-local parts, and postwrite admits only the numbered SDK targets. Second import proves disjoint part paths with equal hashes. Separately, a source-bound DiagramDataPart may expose text-only updates only when each document point has one direct `dgm:t > a:p > a:r > a:t` profile. The codec preserves node IDs/order, frame, relationships, layout/style/colors, and all non-data parts while rewriting only that verified data part. SmartArt authoring, graph/layout/geometry edits, rich or multi-run node text, and nested, incomplete, mistyped, external, duplicated-binding, or connected graphs remain opaque/source-bound and fail closed.
- PPTX InkML content-part clone leaf: one direct top-level `p:contentPart` is accepted only when its exact internal standard/strict `customXml` relationship uniquely binds a non-empty, fully well-formed, relationship-free `application/inkml+xml` CustomXmlPart whose document element is `ink` in the standard namespace. Execution preserves the slide-local relationship ID, uses the Open XML SDK typed part API, and byte-copies the payload into a distinct `ppt/customXml/itemN.xml` clone part. Second import proves disjoint paths with equal hashes. The object remains opaque/source-bound and read-only; nested, extension-bearing, ambiguous, mistyped, malformed/multi-root, wrong-root, or connected content parts fail closed.
- PPTX embedded-MP4 clone leaf: one direct top-level canonical media `p:pic` is accepted only when its empty `ppaction://media` sentinel, `a:videoFile`, `p14:media`, and poster blip consume exactly one video relationship, one Office media relationship, and one image relationship. The video/media pair must uniquely own the same non-empty, relationship-free `video/mp4` MediaDataPart. Execution preserves both slide-local IDs, creates a distinct Open XML SDK MediaDataPart, byte-copies the MP4, and shares the immutable poster ImagePart. Postwrite and second import prove exact bytes, distinct MP4 paths, and one shared poster path. The media object stays opaque/source-bound and read-only; linked, shared, nested, non-MP4, audio, timing-rich, multi-binding, or connected graphs fail closed. Native render checks validate the poster, not playback.

The additive embedded Office-package leaf is equally narrow. Existing eligible
XLSX OLE frames retain their workbook-specific API and clone profile. The
generic `PresentationOleOfficePackage` wire record currently recognizes only a
top-level `p:graphicFrame`/one-`p:oleObj` DOCX package with its exact internal
relationship, standard WordprocessingML content type, exclusive inbound owner,
and source hash. A replacement asset must retain that kind/MIME; the codec opens
it as a body-bearing `WordprocessingDocument`, applies package budgets and
Office 2021 validation, re-proves the original shell/relationship binding, and
replaces only the package bytes. It never authors OLE, accepts arbitrary binary
payloads, updates previews, or admits DOCX OLE frames to the XLSX clone profile.

The PPTX combo profile is intentionally small: literal clustered bars plus literal
lines, globally ordered series, duplicated chart-level data labels, and either
one shared primary category/value axis pair or one canonical secondary top/right
pair for every line. Bars stay primary; all lines must share one axis group.
External data, mixed line groups, secondary bars, per-series labels/point
overrides, smooth lines, trendlines, error bars, and irregular mixed plots remain
opaque-preserved or fail closed.

The other canonical PPTX chart families are equally bounded. Bar, line, pie,
standard area, and 50%-hole doughnut use literal categories and finite caches;
pie/doughnut have no axes and alone may show percentage labels. Marker-only
scatter and 2D bubble use aligned numeric X/Y caches and two value axes; bubble
sizes are positive and area-based. PPTX formula references, external or embedded
workbooks, stacked area, connected/smooth scatter, non-50% doughnut geometry,
bubble 3D/negative/custom-scale variants, and topology mutation remain
opaque/source-bound or fail closed.

Imported objects outside these modeled profiles remain opaque and unchanged. They do not become permission for lossy rewriting.

## Build and test

```sh
dotnet test native/OfficeKit/OfficeKit.sln --configuration Release
npm run build:office-kit
npm run verify:office-kit-build
```

The bundled consumer runtime is written to `runtime/office-kit`. The npm package includes that runtime, manifest, SBOM, and license notices, but excludes this C# source tree and build output. Installed consumers therefore do not need a local .NET SDK.
