# Tasks

- [x] Add the native leaf schema, registry, coverage, and Skill reference.
- [x] Recognize canonical non-literal custom-geometry guide formulas in the
      native projection and source-bound proof/readback path.
- [x] Add formula-graph validation and direct `a:gd/@fmla` token splicing.
- [x] Add the minimal authored/imported/source-bound/reprojection regression,
      then run build, focused tests, maintainer checks, JSON/OpenSpec gates,
      and `git diff --check`.
