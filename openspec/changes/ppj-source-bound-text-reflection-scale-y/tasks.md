## 1. Contract and projection

- [x] 1.1 Add the PPJ schema, capability registry, generated matrix, Skill/reference, coverage, and backlog wording for `run.style.reflection.scaleY` / `textReflectionScaleY`.
- [x] 1.2 Parse authored `scaleY`, project the direct-run ratio, and add the native leaf scale.

## 2. Source-bound lifecycle

- [x] 2.1 Extend direct-run topology proof and leaf allow-lists for one canonical `sy`.
- [x] 2.2 Add the token-splice edit and a focused authored/edit/reproject regression with non-target ZIP preservation and fail-closed cases.

## 3. Validation

- [x] 3.1 Run strict OpenSpec validation, focused codec tests, generated-surface checks, Skill portability/reference sync, and the NativeAOT build; record environment-only skips.
