# Tasks

- [x] Add the additive wire message/field, PPJ schema, registry, generated
      bindings, coverage, and Skill reference.
- [x] Recognize ordered literal `a:prstTxWarp/a:avLst/a:gd` values in authored
      build, import, projection, and source-bound apply paths.
- [x] Add indexed `textBodyWarpAdjustment` proof/readback and direct formula
      value token splice with fail-closed topology checks.
- [x] Add the minimal authored/imported/source-bound/reprojection regression,
      then run build, focused tests, maintainer checks, JSON/OpenSpec gates,
      and `git diff --check`.
