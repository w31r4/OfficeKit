## 1. Handle lifecycle

- [x] 1.1 Add typed schema, lowering/projection and source authority for both handle kinds; verify literal/reference units, absent ranges, source paired-bound add/change/remove and positions, topology/range/reference/owner rejection and unchanged geometry/text/frame/non-target ZIP members in focused native tests.

## 2. Discoverability and checks

- [x] 2.1 Update Help, registry, shapes reference, backlog and coverage; regenerate docs and pass focused preview diagnostics/scene, portability/reference sync and strict OpenSpec checks.

Verification (2026-09-10): SDK 8.0.128 CustomGeometry/Connector/PpjPreviewAuthoredSceneTests passed 126/126, zero skipped. Preview input assessment, scene SVG, capability coverage, presentation Skill maintenance, matrix freshness (55 boundaries/278 leaves), portability (255 files), reference sync (333 files), strict OpenSpec validation and diff checks passed. ImageMaskShape copies paths only, keeping handles shape-owned. No wire change, NativeAOT rebuild or host dragging claim.
