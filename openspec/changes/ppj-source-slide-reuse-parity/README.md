# PPJ source slide reuse parity

Expose the mature source-preserving slide clone as a finite PPJ page macro so
an Agent can reuse one exact imported page, build it through the native graph
writer, and reimport before continuing edits.
