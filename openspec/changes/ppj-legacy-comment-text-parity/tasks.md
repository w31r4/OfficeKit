## 1. Projection and lowering

- [x] 1.1 Retain comment raw state and project a comment nativeRef.
- [x] 1.2 Issue `replaceText` only from the native editable legacy profile.
- [x] 1.3 Lower fixed-topology comment text changes through the existing codec.

## 2. Agent surface

- [x] 2.1 Regenerate `ppj.md` and update review guidance and coverage.

## 3. Lean verification and integration

- [x] 3.1 Extend the existing chart edit with one comment text change and second
      projection assertion.
- [x] 3.2 Run the focused PPJ contract, C# build, Skill-maintainer, and strict
      OpenSpec checks.
- [x] 3.3 Commit atomically and fast-forward main without force pushing.
