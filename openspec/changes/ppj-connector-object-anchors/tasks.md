## 1. Coordinate semantics

- [x] 1.1 Implement explicit/auto object endpoint resolution in authored lowering, with affine group/target transforms and identifiable rejection. Verify directed/moved, all anchors, tie ordering, transformed groups and unsupported targets in focused native tests.
- [x] 1.2 Correct component literal-endpoint and group-child coordinate expansion. Verify translated/scaled component equivalence, local ID rewriting and existing component/group regressions.

## 2. Binding lifecycle

- [x] 2.1 Add bounded native frame-anchor metadata independent of geometry connection sites, including optional wire fields if needed. Verify malformed metadata remains source-owned and fresh projection without embedded PPJ preserves exact target/anchor identity.
- [x] 2.2 Support source-bound endpoint edits and dependent target moves without bypass through native-leaf shortcuts. Verify no-op bytes, original-source edit/delete/reprojection and non-target ZIP preservation.

## 3. Integration and documentation

- [x] 3.1 Verify authored compile with/without preview produces equivalent endpoint geometry and original program remains unchanged. Keep the two existing native preview counterexamples intact; distinguish source-library results from installed NativeAOT execution.
- [x] 3.2 Update the F-04 backlog, capability entry and focused reference with tested behavior and remaining boundaries. Run narrow affected native/JS checks, protocol checks when changed, documentation generation checks and strict OpenSpec validation before atomic publication.

Coordinate-stage evidence (2026-09-09, isolated base 1fe503cd): focused native run passed 30/30, zero skipped: PpjConnectorObjectAnchorTests, PpjPreviewAuthoredSceneTests, PpjV1ValidatesAndExpandsCanonicalPresentationProgram and PpjGroupReadingOrder. Both directed/moved cases inspect actual exported connector XML as well as the preview scene; preview on/off yields identical PPTX bytes. The first fixture used incorrect shape geometry/style keys and was corrected; a rotated cross-group endpoint outside the current nonnegative local-coordinate profile remains an explicit rejection test alongside the supported positive case. This is source-library evidence, not a new NativeAOT run. Binding persistence and source-bound dependency edits remain incomplete; this change is not ready for publication as a completed field.

Binding-stage evidence (2026-09-09): focused native run now passes 38/38, zero skipped. Six requested anchors survive export, removal of the embedded PPJ, fresh projection and byte-identical source no-op. The native XML carries bounded frame-anchor metadata without invented stCxn/endCxn sites; duplicate/unknown/extra-field/missing-target/self-target/native-site-conflict metadata is rejected. An invalid anchor in an actual PPTX projects as opaque and preserves its bytes on no-op. A referenced target does not receive deletion capability. proto:check, JS preview scene wire/view tests, strict OpenSpec and whitespace checks pass. Source-bound target moves and endpoint edits (2.2), final coverage/reference updates and publication (3.2) are still outstanding.


Source-edit evidence (2026-09-10): 69/69 focused native tests pass, zero skipped, including Connector, PpjPreviewAuthoredSceneTests, canonical PPJ expansion, PpjGroupReadingOrder and PpjComponent filters. Endpoint edit/retarget/detach/attach, semantic target movement, issued frame/child-frame leaf movement, auto recomputation and conflicting edits are covered. Original-source no-op bytes, non-target ZIP members and exact target/anchor reprojection remain asserted. Native geometry-site bindings receive no setConnectorEndpoints authority. The first source-edit run exposed missing from/to capability-field enum entries; both schema and semantic vocabulary now agree. One existing broad connector/chart regression still expected literal error-bar deletion to fail; it now verifies the implemented deletion and retained trendlines. This is source-library evidence; no NativeAOT rebuild or host acceptance is claimed. Documentation and publication remain outstanding.

Documentation/check evidence (2026-09-10): F-04 and coverage describe the bounded lifecycle and remaining routing/host limits; connectorEndpoint schema documentation, capability boundary, Help distinction between native sites and PPJ frame anchors, shapes reference and generated PPJ/capability matrix agree. Maintainer check, matrix check, preview capability coverage, scene wire/view/SVG, Skill portability (255 files), root reference sync (333 files), proto:check and strict OpenSpec pass. Updated isolated base to d7ddc46e before publication; regenerated PPJ reference after its registry contract changed. No production renderer or concurrent percentage-stack edits belong to this change. Full P0/P1 backlog remains unfinished.
