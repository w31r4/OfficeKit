## 1. Native Contract

- [x] 1.1 Add an additive table-cell image-paint field to the Office wire and regenerate bindings.
- [x] 1.2 Build and validate native DrawingML table-cell `a:blipFill` through the slide asset context.

## 2. PPJ Compiler

- [x] 2.1 Lower explicit and inherited PPJ table-cell image fills using each cell's computed physical dimensions.
- [x] 2.2 Preserve the existing fail-closed imported-table boundary and path-specific asset diagnostics.

## 3. Evidence and Agent Surface

- [x] 3.1 Extend the existing comprehensive authored PPJ round-trip contract with native image-fill and exact-recovery evidence.
- [x] 3.2 Update the capability registry, charts/tables guidance, generated `ppj.md`, coverage, and OpenSpec state.
- [x] 3.3 Run only the affected C# contract, proto consistency, Skill registry, and strict OpenSpec checks.
