## 1. Single support contract

- [x] 1.1 Add registry-owned preview rules for actual element/chart types, field owners, variants and source-bound boundaries; derive the existing capability summary and matrix data without duplicate grade membership. Verify schema type alignment, invalid mappings/obsolete names, deterministic generated output and read-only drift rejection in `test/ppj-preview-capability-coverage.mjs`.
- [x] 1.2 Define the structured diagnostic and element/page/result assessment contract in a dependency-light leaf module; verify severity/status validation, bounded escaped value summaries, exact paths and deterministic `unavailable > opaque > partial > supported` aggregation, including supported informational records and empty/unassessed state, in a new focused diagnostic test.

## 2. Actual input assessment

- [x] 2.1 Walk real program/page/nested element fields and classify proven mappings, explicit metadata, source-owned state, known limits and unclassified descendants; verify nested groups/components, inherited theme/style/master/layout state, zero/false/null/defaults, unknown descendants and duplicate local IDs across pages without mutating inputs or expanding binary/source payloads.
- [x] 2.2 Add field-addressable limits for text, geometry, transforms/hidden state, fill/theme/effects, images, tables, connectors, charts, source previews and unresolved bounds; incorporate concurrent errorBars diagnostics. Verify representative actual paths in every family, page/global propagation, and one genuinely supported primitive with geometry/style assertions plus a one-field mutation that ceases to be supported.
- [x] 2.3 Add error-severity rules and failed reliability for every factual-error family specified in the design: text-bearing shape loss, connector topology, missing coercion/bridging, proportions, hierarchy/flow, OHLC/X/Y/size channels, cumulative totals and contradictory axes/transforms/visibility. Verify a direct regression for each family, distinguish unassessed limitations from known contradictions, and prove successful publication or styling does not clear the failure.

## 3. Rendering and publication integration

- [x] 3.1 Route all drawing-branch diagnostics through the shared assessment and expose identical element/page/global support and reliability in memory; add visible unreliable/review-needed indications to affected SVG pages before rasterization. Verify warning/diagnostic linkage, original IDs, canvas dimensions, unchanged input, supported-page behavior and absence of new eager root/raster dependencies.
- [x] 3.2 Preserve assessment and reliability in `preview-output.mjs` receipts and failure receipts without changing G-12 publication completion meaning; verify retained artifacts, hash equality for labeled output, failure dominance, original source bytes and equality of returned/persisted evidence through actual codec+sharp authored/source-bound tests and injected dependency/write failures.

## 4. Gates, documentation and completion evidence

- [x] 4.1 Register the dependency-light diagnostic regression in package/regular gates, replace historical full-support membership assertions with actual-state assertions, and verify `node test/gate-policy.mjs`, the new diagnostic test, `node test/ppj-preview-capability-coverage.mjs`, `node test/ppj-preview-output-evidence.mjs` and the updated `npm run test:slow -- --segment presentation` all pass without dropping previous preservation tests.
- [x] 4.2 Document support/reliability/publication distinctions, visible warning meaning, corrected capability summaries and unchanged publication-only CLI exit behavior; update affected presentation guidance and only verified G-11/G-14/G-15 audit progress. Verify documentation links, generated metadata drift checks, `node test/skill-portability.mjs` and `node test/reference-skill-sync.mjs` if their sources change, `git diff --check`, and strict OpenSpec validation. Leave G-01–G-10 drawing work, G-13 delivery integration and the full renderer goal open.

Planning baseline: `6620f353` plus concurrent errorBars work inspected on 2026-09-09. None of these implementation tasks has been completed by creating this checklist. Re-read the current shared-file diff before applying.

Implementation evidence (2026-09-09): task 1.2 is verified by `node test/ppj-preview-diagnostics.mjs` and `node --check src/ppj/preview-diagnostics.mjs`. This establishes pure evidence primitives only; runtime input assessment and integration remain unchecked below their respective tasks.

Task 1.1 evidence: `node test/ppj-preview-capability-coverage.mjs`, both capability generators and their `--check` modes pass. The existing slow/presentation segment also passes all three steps with real authored/source-bound codec+sharp output at `/tmp/officekit-ppj-preview-e4Ygx5`. Family declarations are now conservative and registry-derived; remaining drawing-branch assessments are not yet replaced, so this is not G-11 completion.

Early gate integration for 4.1: the new diagnostic suite is registered and the updated slow/presentation segment passes 4/4 with real output at `/tmp/officekit-ppj-preview-FNOSeQ`; `node test/gate-policy.mjs` and both generators' `--check` modes also pass. Task 4.1 remains unchecked until actual input assessment/integration and their assertions are implemented; no existing preservation regression was removed.

Task 2.1 evidence: `node test/ppj-preview-input-assessment.mjs` passes for schema-directed field traversal, metadata exclusion with unknown-child rejection, nested group/component slots and definitions, duplicate local IDs, program/page/group inheritance, explicit zero/false/null, canonical input immutability, binary omission and nativeRef/source payload non-expansion. The suite is included by the regular diagnostic test; actual drawing integration remains task 3.1.

Tasks 2.3/3.1/3.2 evidence (2026-09-09): `node test/ppj-preview-diagnostics.mjs` exercises every factual registry family through actual SVG drawing and publication both before/after a style edit, and confirms matching axis bounds/unresolved tokens require review rather than inventing factual contradictions. Nested duplicate IDs, an actual scatter/null drawing exception, continued later-page output, bounded placeholders and correctly classified retained failures pass. `node test/ppj-preview-output-evidence.mjs` retains G-12 preservation/failure tests and now asserts failed reliability for all production failures. `npm run test:slow -- --segment presentation` passes 4/4 with real codec+sharp output at `/tmp/officekit-ppj-preview-NYPhga`: authored output is partial/failed with complete publication, source-bound output is opaque/requires-review with complete publication. Tests compare persisted/returned assessments, original source bytes, image hashes/dimensions and actual warning-banner PNG pixels. The authored claim PNG was also visually inspected; this verifies warning presence, not full drawing fidelity.

Final G-11 verification (2026-09-09): updated slow/presentation passes 4/4 at `/tmp/officekit-ppj-preview-itVihO`, now also exercising the real CLI's exit-0 / failed-reliability combination. Gate policy, both generated metadata checks, JavaScript preflight (292 files), Skill portability (255 files), reference sync (333 files), 43 local documentation links, `git diff --check` and strict OpenSpec validation pass. The portability test previously forced REPL guidance on the typed PowerPoint Live adapter; it now checks that adapter's install/doctor/sessions/execute/disconnect route while retaining host-neutral safety assertions. The generic Skill template validator still rejects the existing uppercase `Presentations` name; this reference-only change does not rename it or claim that generic validator passed. Concurrent trendline-label registry/schema work caused a matrix drift failure; the checked-in generator was rerun and its resulting counts/rules reviewed, then --check passed.

The full preview smoke was rerun: 42 inputs, 4 rendered (all partial), 38 compilerRejected, 0 rendererFailed. Its coarse success condition and fixture deficiencies remain G-14. G-11 is complete for its support/diagnostic contract; G-01–G-10 drawing fidelity, G-13 delivery, broader G-14/G-15 coverage, G-16 performance and the full user goal remain open. This change is not archived or committed by this workflow.
