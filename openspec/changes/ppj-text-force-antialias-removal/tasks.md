## 1. Anti-alias hint presence

- [x] 1.1 Add validated native deletion and PPJ inference/normalization; extend shared boolean/wire experiments and run focused lifecycle/body/AutoFit/placeholder/TableText regressions including authority and other-field guards.
- [x] 1.2 Regenerate bindings and field descriptions; run proto:check, JS wire experiment, preview input/capability/scene-wire, generated reference/matrix, Skill portability/reference sync, strict OpenSpec and whitespace checks; record evidence and limits.

Verification (2026-09-10): focused native selection 120/120, zero skipped, SDK 8.0.128. C#/JS setter bytes and deletion round-trip pass. proto:check passes against regenerated/staged binding. Scene wire, preview input/capability, maintenance, generated reference/matrix, portability (255 files), reference sync (333 files), strict OpenSpec and whitespace pass. Updated codec required; no NativeAOT rebuild or host rendering acceptance. Full backlog remains open.
