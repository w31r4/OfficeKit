## 1. Paragraph default language

- [ ] 1.1 Implement exact field authority, presence mutation and targeted lang writing; verify lifecycle, spelling/bounds, run/altLang preservation, missing authority and unmodeled-source handling in focused native fixtures.
- [ ] 1.2 Update schema/Help/registry/references and backlog evidence; verify related native tests, generated metadata, preview input/capability, portability/reference sync and strict OpenSpec checks.

Publication checkpoint (2026-09-10): current implementation and minimal regressions are saved at the user's request; neither task is complete. The combined `PpjTextBodyPropertyLifecycleTests|PpjPreview` filter on SDK 8.0.128 ran 221 cases: 219 passed, 2 failed, zero skipped. Both failures are the new paragraph-default language lifecycle cases with `otherDefaults=true` (text and shape): deletion fails with `presentation_unmodeled_shape_content_changed`. Keep these failing reproductions for the next increment; do not claim language lifecycle closure. Language-only deletion/restoration, case preservation, invalid tokens, direct-run and altLang preservation are exercised by the added cases. Unmodeled-source language coverage and public Help/registry/reference updates remain pending. Scene SVG, scene wire, preview input assessment, preview capability coverage, integration-script syntax and both strict OpenSpec validations passed. Full NativeAOT integration was not rerun; its separately documented failures remain open.
