# Tasks

- [x] Add the additive `last_row` table field to the wire model, PPJ table
      style, schema, registry, and generated/reference coverage.
- [x] Add bounded table projection, source proof, readback, and one-attribute
      XML token-splice compilation for `a:tblPr/@lastRow`.
- [x] Add the minimal authored/imported/source-bound/reprojection regression,
      including an absent-flag refusal.
- [x] Run the focused table tests, maintainer checks, JSON/OpenSpec gates, and
      `git diff --check`.
