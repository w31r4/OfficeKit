# Evidence

## Contract

- PPJ owns bounded native data-label defaults at plot and series scope plus
  sparse zero-based point overrides.
- The canonical profile covers visibility flags, native position, direct
  number format and bounded chart typography.
- Imported editable ChartParts expose `setChartLabels`; the source-bound
  lowerer may add, change or remove only this semantic label state.
- Custom label text, manual layout, shape/effect state, leader-line graphs,
  extensions and source-linked formats remain source-owned and fail closed.

## Focused verification

- NativeAOT codec build: passed with 0 warnings and 0 errors.
- Existing comprehensive PPJ contract:
  `PpjV1CompilesCanonicalPresentationProgramDeterministically` passed 1/1.
- The contract covers authored ChartML, import projection, a source-bound
  series/point label edit, second projection and deterministic semantics.
- Presentation Skill maintenance sync/check: passed with 151 Help APIs, 73
  native leaves and 13 host-only operations.
- `npm run proto:check`: passed.
- `npx openspec validate ppj-chart-label-overrides --strict`: passed.

## Honest boundary

No full `npm test`, package/release gate, renderer comparison or desktop
PowerPoint playback was run for this bounded primitive slice. Those remain
release-level evidence, not prerequisites for this focused contract.
