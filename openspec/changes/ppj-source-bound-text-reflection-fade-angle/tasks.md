## 1. Native projection and editing

- [x] 1.1 Add the direct-run `textReflectionFadeAngleDegrees` leaf, strict
  fade-only reflection profile, proof/patch support, and source-bound regression.
- [x] 1.2 Extend the PPJ schema leaf enum and capability metadata; verify
  JSON/schema and capability checks pass.

## 2. Evidence and validation

- [x] 2.1 Update the presentation Skill, coverage, matrix, and gap backlog with
  the bounded field evidence; verify generated/reference synchronization checks
  pass.
- [x] 2.2 Run focused .NET tests, OpenSpec strict validation, and affected
  repository gates; verify the change is ready for an atomic commit and push.
