## 1. Language contract

- [ ] 1.1 Add optional `sourceClone.retainElement` to schema and typed C# model.
- [ ] 1.2 Validate a direct source-page element and every sibling delete proof.

## 2. Source-bound lowering

- [ ] 2.1 Retain one exact source element in the clone wire projection.
- [ ] 2.2 Lower every sibling through existing element-deletion bindings.
- [ ] 2.3 Preserve full-page clone behavior when the selector is absent.

## 3. Agent surface and lean verification

- [ ] 3.1 Regenerate `ppj.md` and update continuation guidance and coverage.
- [ ] 3.2 Extend one existing clone round-trip with component reuse and reimport.
- [ ] 3.3 Run the focused test, Skill maintainer, and strict OpenSpec check once.
- [ ] 3.4 Commit atomically and fast-forward main without force pushing.

## Evidence

Pending implementation.
