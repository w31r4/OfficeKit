## 1. Freeze the experiment boundary

- [x] 1.1 Record execution-time `origin/main`, worktree path, Node/Codex/OfficeKit versions, and dirty-main preservation note.
- [x] 1.2 Create the experiment directory and ignored raw-run policy without changing production package globs.
- [x] 1.3 Inventory and hash the six scenario inputs, complex source pages, fonts, reference images, and OfficeKit package snapshot.
- [x] 1.4 Freeze the seeded 12-case manifest with audience, job, delivery mode, carrier, source, edit steps, image need, and acceptance contract.
- [x] 1.5 Freeze the capability coverage ledger, rubric, judge prompt, and randomized arm schedule before author runs.

## 2. Build the two clean-room Skill arms

- [x] 2.1 Write the shared invariants and capability manifest covering evidence, relation-first composition, occlusion, sparse data, render repair, PPJ/source-bound/opaque, and image rights.
- [x] 2.2 Assemble the Shared What / What-kind / How overlay with concise route files and on-demand implementation references.
- [x] 2.3 Assemble the Kimi-style concise overlay with route selection, one scenario guide, task-local style brief, and PPJ handoff.
- [x] 2.4 Verify both overlays expose identical common references and do not include Kimi source text, code, private templates, or copied assets.
- [x] 2.5 Add a route smoke that proves both arms reach all mandatory invariants and the same PPJ/image/review surfaces.

## 3. Add the disposable runner and evidence ledger

- [x] 3.1 Implement one one-off runner for fresh author sessions with model/effort/timeout, arm, case, seed, and environment identity recorded.
- [x] 3.2 Capture JSONL model usage, file reads, tool calls, image searches, retries, check/build/render/review passes, and output paths.
- [x] 3.3 Add source-bound package/hash checks for 1-to-10 outputs and stable-ID/reimport checks for all outputs.
- [x] 3.4 Add anonymization and seeded A/B order randomization for two blind judge rounds.
- [x] 3.5 Keep raw runs outside the package or under ignored `runs/`; emit only normalized evidence records for analysis.

## 4. Run the frozen study

- [ ] 4.1 Run 12 author cases for the Shared arm with `gpt-5.6-luna` and `model_reasoning_effort=max`.
- [ ] 4.2 Run the same 12 author cases for the Kimi-style arm in the seeded paired order.
- [ ] 4.3 Run two independent blind Luna Max judge rounds over all valid A/B pairs.
- [ ] 4.4 Collect four human calibration pairs without revealing arm identity or using them for tuning.
- [ ] 4.5 Re-run only incomparable provider failures or hard environment failures, preserving their original records and reason.

## 5. Analyze and deliver the report

- [ ] 5.1 Compute hard-gate status, quality dimensions, win/tie/loss, per-route and per-scenario deltas, and judge agreement.
- [ ] 5.2 Compute exact paired tests, bootstrap intervals, image/non-image strata, token/time/tool-call metrics, and quality/cost frontiers.
- [ ] 5.3 Write the scope-limited report with raw evidence links, conclusions, limitations, and next falsifiable hypotheses.
- [x] 5.4 Confirm no default route, production Skill, PPJ/compiler/API, template, or package behavior changed.
- [x] 5.5 Run only the narrow manifest/route/evidence checks required for this research change and record unverified host playback boundaries.
