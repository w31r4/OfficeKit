## 1. Column count lifecycle

- [x] 1.1 Implement deletion and guarded style removal using NoColumns; extend the shared numeric lifecycle regression and run the focused lifecycle, TextBody, placeholder and TableText tests.
- [x] 1.2 Update schema, Help, registry, text reference and F-03/F-06 coverage; verify preview columns 1 diagnostic, generated reference/matrix checks, Skill portability/reference sync, strict OpenSpec validation and whitespace checks.

Verification (2026-09-10): focused native selection 89/89, zero skipped, SDK 8.0.128. Preview input/capability, maintenance, matrix, portability (255 files), reference sync (333 files), strict OpenSpec and whitespace checks passed. No wire change, NativeAOT rebuild or host layout acceptance. Full backlog remains open.
