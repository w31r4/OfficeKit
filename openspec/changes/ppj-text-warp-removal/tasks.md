## 1. Warp lifecycle

- [x] 1.1 Implement deletion, guide clearing and normalization across existing owners; verify shared native lifecycle and wire/malformed-source guards.
- [x] 1.2 Synchronize schema, Help, registry, reference and gap evidence; verify proto:check, preview diagnostics, reference/matrix checks, portability/reference sync and strict OpenSpec validation.

Evidence: focused native selection 160/160 passed, zero skipped (SDK 8.0.128), including full signed guide bounds. C#/JS wire, proto:check, nativeLeaf schema bounds, preview input/capability/scene wire, generated reference/matrix, portability/reference sync and strict OpenSpec passed. No NativeAOT rebuild or full WordArt rendering acceptance.
