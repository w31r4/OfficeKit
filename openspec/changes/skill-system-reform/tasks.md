## 1. Freeze the surface map

- [x] 1.1 Record the Kimi-observed layer map and clean-room boundaries in the
  change design without copying proprietary text or assets.
- [x] 1.2 Inventory OfficeKit Presentation Help names, route files, examples,
  focused tests, and release evidence needed by the impact manifest.

## 2. Publish the semantic references

- [x] 2.1 Add the concise Presentation primitive index and link each family to
  authoritative Help/API and advanced boundary references.
- [x] 2.2 Add role-based typography guidance with CJK/Latin fallback and
  render-review rules.
- [x] 2.3 Add the versioned primitive-impact manifest and keep it limited to
  stable families and maintenance surfaces.

## 3. Build the update Skill

- [x] 3.1 Add the host-neutral `skill-update` plugin manifest, entry Skill, and
  agent metadata.
- [x] 3.2 Implement deterministic `impact` and `check` commands without
  runtime/provider initialization or file mutation.
- [x] 3.3 Add the plugin to package inclusion, Claude marketplace metadata, and
  the default `officekit init` Skill set if the installer owns that list.

## 4. Converge the Presentation routes

- [x] 4.1 Shorten the main Presentation entry to routing, invariants, and the
  common spine; point primitive and typography decisions to the new references.
- [x] 4.2 Audit duplicated workflow prose. The source-bound starter/edit
  contract remains in its dedicated reference because executable helper scripts
  and existing users depend on its exact preconditions; route-level prose now
  points to it instead of copying those mechanics.
- [x] 4.3 Make the Creator calibration route load the shared references and
  distinguish capability evidence from published style guidance.

## 5. Verify and deliver

- [x] 5.1 Run the new checker, existing Skill/reference-sync smoke, and one
  syntax/import check; add no broad benchmark or duplicate test harness.
- [x] 5.2 Review the diff for Kimi paths, private identifiers, stale links, and
  accidental user-WIP inclusion.
- [ ] 5.3 Commit the reform by subsystem, push normally, and fast-forward merge
  into `main` after verifying the remote ancestor relationship.
