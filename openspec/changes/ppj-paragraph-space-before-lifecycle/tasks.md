## 1. Paragraph space before

- [x] 1.1 Connect both unit fields, precedence, exact authority and targeted native editing; verify precision/zero, switching, removal/restoration and wrappers, unchanged XML/ZIP and rejected input/source cases through focused lifecycle and related native tests.
- [x] 1.2 Synchronize Help, registry, text guidance, generated references and backlog; verify preview diagnostics, maintainer/matrix checks, portability/reference-sync and strict OpenSpec validation.

Focused native lifecycle tests passed 4/4. Related regressions passed 335/336 initially; the existing rich-text fixture explicitly writes distributed alignment, which the existing reader recognizes. After correcting its stale unmodeled-alignment assertion, that test and the new lifecycle tests passed 5/5. The previously documented whole-default-style baseline exclusion remains. Preview input/coverage, maintainer/matrix, portability, reference-sync, strict OpenSpec and staged-only reference/input-assessment checks passed. NativeAOT was not rebuilt and host layout remains unverified.
