## 1. Source contract

- [x] 1.1 Add the additive picture source-model field, extend the native leaf
      registry and refresh the PPJ reference/backlog; verify protobuf
      generation, JSON parsing, and reference synchronization.

## 2. Projection and edit

- [x] 2.1 Accept the strict complete picture backdrop owner in projection and
      source-bound proof, then token-splice only `norm/@dy`; verify with the
      focused picture regression.

## 3. Verification

- [x] 3.1 Run the product build, focused runner, maintainer/review/reference/
      JSON/OpenSpec gates, and `git diff --check`; verify all tasks are
      complete.
