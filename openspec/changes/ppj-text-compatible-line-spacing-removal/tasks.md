## 1. Compatible line-spacing hint presence

- [x] 1.1 Add validated native deletion, PPJ inference and normalization; extend shared boolean/wire experiments with paragraph line-spacing preservation and run focused lifecycle/body/AutoFit/placeholder/TableText regressions.
- [x] 1.2 Regenerate bindings and public descriptions; verify proto:check, JS wire, preview input/capability/scene-wire, generated reference/matrix, Skill portability/reference sync, strict OpenSpec and whitespace checks; record evidence and limits.

Verification (2026-09-10): focused native selection 136/136, zero skipped, SDK 8.0.128; explicit line/before/after spacing included. C#/JS wire and proto:check pass against regenerated/staged binding. Scene wire, preview input/capability, maintenance, generated reference/matrix, portability (255 files), reference sync (333 files), strict OpenSpec and whitespace pass. Waited for observed concurrent native build to end before editing. Updated codec required; no NativeAOT rebuild or host metrics acceptance. Full backlog remains open.
