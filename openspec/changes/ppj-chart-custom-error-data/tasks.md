## 1. Literal custom error fields

- [x] 1.1 Add the PPJ data schema, semantic validation, compiler and projector mappings and formula-free native lifecycle; verify authored/source-bound point amounts, formats/tokens and side changes with `PpjCustomErrorData` tests.
- [x] 1.2 Verify malformed caches, invalid PPJ data and formula owners are rejected or preserved with focused regressions; rerun the shared error-bar and scalar lifecycle tests.

## 2. Discoverability and evidence

- [x] 2.1 Update the registry, F-07 and chart reference, regenerate reference/matrix, extend the existing partial preview diagnostic regression, and run maintenance/preview/reference checks.
- [x] 2.2 Run strict OpenSpec and diff validation and record exact test results plus remaining formula/visual limits in evidence.md.
