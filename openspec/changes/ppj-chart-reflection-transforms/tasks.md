## 1. Contract and implementation

- [x] 1.1 Extend chart reflection schema and additive wire fields; regenerate bindings and pass proto check and JS wire preservation regression.
- [x] 1.2 Connect native validation/read/write, chart compilation and projection; verify optional presence and ordinary imported guards with focused native tests.

## 2. Minimal experiment and documentation

- [x] 2.1 Extend existing line/combo lifecycle, bounds and style/vector tests; pass the focused effect suite with native attributes, fresh projection, no-op and non-target ZIP assertions.
- [x] 2.2 Update registry, focused reference, coverage, backlog and generated docs; pass maintenance, capability, portability and strict OpenSpec checks, recording exact scope.

Verification: SDK 8.0.128 focused effect/chart/ordinary reflection suite passed 69/69, zero skips. Existing line/combo lifecycles now include all seven transforms, native attribute presence, clearing/deletion/recreation, independent effect retention, unchanged text, non-target ZIP bytes and exact fresh no-op. Bounds tests cover signed Int32 extremes, skew limits and precision, malformed attributes, every alignment and ordinary imported guards; style/vector and JS wire regressions pass. Proto, Skill maintenance, capability matrix/coverage, portability and strict OpenSpec checks pass. No full repository test, NativeAOT rebuild or host display acceptance was performed.
