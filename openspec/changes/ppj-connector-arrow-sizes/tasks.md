## 1. Field lifecycle

- [x] 1.1 Add the four connector size fields to schema, authored compilation, fresh projection and setConnectorArrows source authority; verify a focused native fixture covers presence, edit/delete, arrow removal conflicts, no-op bytes and preserved bindings/non-target ZIP members.

## 2. Documentation and checks

- [x] 2.1 Update Help, registry, shapes reference, backlog and coverage; regenerate documentation and pass focused native/SVG, reference and strict OpenSpec checks before atomic publication.

Native evidence: connector and PpjPreviewAuthoredSceneTests filter passes 68/68, zero skipped, using SDK 8.0.128. The new fixture covers all four authored sizes, omitted native attribute presence, original-source independent mutations, no-op bytes, orphan/conflict and authority rejection, bindings and SlidePart-only changes.

Closing checks: generated PPJ manual and capability matrix synchronized; maintainer check, matrix check, preview input assessment (including four explicit size diagnostics), native-scene SVG regression, preview capability coverage, portability (255 files), reference sync (333 files), strict OpenSpec and whitespace checks pass. No wire change, NativeAOT rebuild or host appearance acceptance.
