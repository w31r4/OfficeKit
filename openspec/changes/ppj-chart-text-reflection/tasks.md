## 1. Reflection lifecycle

- [x] 1.1 Add schema/wire, shared native optional-value codec and ordered chart effects; verify native presence/order/bounds, invalid graph rejection and JS wire preservation.
- [x] 1.2 Connect authored/source-bound compilation, projection, named precedence and vector overrides; verify line/combo create/change/clear/delete/recreate, exact no-op and non-target ZIP preservation, and fix the verified pre-existing reflection-angle leaf scaling failure using the ordinary text fractional-angle regression.

## 2. Documentation and verification

- [x] 2.1 Update capability registry, chart reference, coverage/backlog and generated references; verify maintenance, capability and portability checks.
- [x] 2.2 Run focused reflection/effect regressions, proto and strict OpenSpec checks; record actual evidence and remaining boundaries before scoped atomic publication.

Evidence (2026-09-09, base e75423c8): the final focused native suite passed 69/69 with zero skips using SDK 8.0.128. It includes all four new chart reflection cases and existing inner/outer shadow, glow, soft edge, reflection, highlight and trendline rich-text/label tests. Line/combo edits retain optional presence and the other four effects, ordered native children, literal text, exact no-op, fresh projection and every non-target ZIP entry; native cases cover empty/zero styles, full order under OpenXmlValidator, bounds/tokens/precision, named precedence, vector overrides and eleven malformed source-owned reflection graphs.

The first suite had 68 passes and one existing ordinary-text reflection failure. Running that exact old test on the prior inner-shadow assembly reproduced the same stale-baseline failure. Removing textReflectionDirectionDegrees from the conflicting integer classification repaired its declared degree-to-native conversion; the updated original regression proves 45.5 to 90.25 degree edits and reprojects 5415000 native units correctly. No source-proof check was weakened.

JS worksheet-chart retention and wire field 23 presence passed. Proto lint/generation/drift, Skill maintenance, portability (255 files), root reference sync (333 files), capability matrix/coverage, strict OpenSpec and whitespace checks passed. No NativeAOT package rebuild, production preview or host appearance acceptance is claimed; transformed/partial-span reflection and full F-07 remain open.
