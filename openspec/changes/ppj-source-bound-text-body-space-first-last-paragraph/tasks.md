# Tasks

- [x] Add the additive `space_first_last_paragraph` body-property field to the
      wire model, PPJ text-box style, schema, registry, and generated/reference
      coverage.
- [x] Add bounded body-property projection, source proof, readback, and one
      attribute XML token-splice compilation for `a:bodyPr/@spcFirstLastPara`.
- [x] Add the minimal authored/imported/source-bound/reprojection regression,
      including an absent-attribute refusal.
- [x] Run the focused text-body test, maintainer checks, JSON/OpenSpec gates,
      and `git diff --check`.
