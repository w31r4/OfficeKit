## 1. Numbered paragraph start value

- [x] 1.1 Add exact startAt authorization and field-local lowering, preserve the same-scheme native marker and parse malformed starts safely; verify text/shape authored, source no-op, set/delete/restore, exact XML/ZIP preservation and negative requests in the focused lifecycle regression.

## 2. Discoverability and compatibility

- [x] 2.1 Synchronize schema/Help/registry, generated PPJ reference, focused text guidance and F-03 backlog; verify automatic-number preview diagnostics, affected native bullet/list regressions, generated checks, portability/reference sync and strict OpenSpec validation.

Validation (2026-09-10): the focused lifecycle first reproduced rejected source edits and malformed-token projection failure; after the fix it passes 3/3. The related paragraph, bullet, list/master and table-picture-bullet filter passes 19/19, zero skips. Input/paint diagnostics, capability coverage, generated manual/matrix, portability/reference sync and strict OpenSpec pass. The emitted 33-field capability exposed the old array bound; 64 now accommodates it while retaining the closed vocabulary. No NativeAOT rebuild, full repository test or host numbering/layout acceptance was performed.
