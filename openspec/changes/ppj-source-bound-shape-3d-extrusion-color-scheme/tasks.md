## 1. Source contract

- [x] 1.1 Add additive shape and picture source-model fields, generated wire
      bindings, native leaf registry/schema/reference entries, and refresh the
      Skill/backlog; verify protobuf generation and JSON parsing.

## 2. Projection and edit

- [x] 2.1 Accept strict bare `schemeClr` extrusion owners for shapes and
      pictures, project the theme token, and token-splice only its `@val`;
      verify focused shape and picture regressions.

## 3. Verification

- [x] 3.1 Run product builds, focused tests, maintainer/review/reference/
      JSON/OpenSpec gates, and `git diff --check`; verify all tasks are
      complete.
