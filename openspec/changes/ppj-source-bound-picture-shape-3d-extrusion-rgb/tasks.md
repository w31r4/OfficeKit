# Tasks

- [x] Preserve the additive picture source-model field and update the existing
      native leaf registry/reference contract.
- [x] Accept a strict direct picture
      `a:sp3d/a:extrusionClr/a:srgbClr/@val` owner in projection and
      source-bound proof, then token-splice only its RGB attribute.
- [x] Add the minimal picture extrusion-RGB regression, run the product build,
      focused test, maintainer/reference/JSON/OpenSpec gates, and
      `git diff --check`.
