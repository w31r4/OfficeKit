# Reference Skill compatibility

The 0.3 source tree publishes the reference file-type layout, the project-native OfficeKit router, a local template utility, and a retained MIT template library as seven native plugin bundles, not as the earlier flat project-specific Skills:

```text
skills/
  documents/{.codex-plugin,README.md,assets,skills/documents}
  spreadsheets/{.codex-plugin,.app.json,README.md,assets,skills/{spreadsheets,excel-live-control}}
  presentations/{.codex-plugin,README.md,assets,skills/presentations}
  pdf/{.codex-plugin,README.md,assets,skills/pdf}
  office-kit/{.codex-plugin,README.md,skills/office-kit}
  template-creator/{.codex-plugin,manifest.json,README.md,assets,skills/template-creator}
  default-template-library/{.codex-plugin,LICENSE.md,manifest.json,README.md,integrity.json,assets,scripts,skills/...}
```

There are seven plugin packages and twenty-seven Skills in the source tree. Six packages and seven Skills are distributed in npm; `default-template-library` is a seventh, repository-only package with twenty nested template Skills and is deliberately excluded from the npm tarball. The old fixture runners remain under `test/skill-harness`; they are development tests and are excluded from the npm package.

## Verified compatibility

| Surface | Status | Evidence and remaining boundary |
| --- | --- | --- |
| Plugin manifests and discovery | done | The six npm-distributed native manifests pass the official plugin validator. OfficeKit intentionally omits invented brand icons while retaining valid interface and Agent metadata. The seventh repository-only manifest, its 20 nested Skills, source `agent.yaml` metadata, retained MIT license, canonical file inventory, and integrity record are checked structurally; every declared path resolves inside its plugin bundle. |
| Project-local initialization | done | The npm package exposes `officekit init` and `officekit update`. It detects supported Agent project markers or accepts `--tools`, installs all seven distributed Skills into each selected project-local Skill root, and records path ownership plus exact tree hashes in `.office-kit/skills.json`. Clean-install package tests invoke the packed CLI from `node_modules`, install the complete set into a fresh project, and verify every `SKILL.md` plus Template Creator's self-contained icon. |
| Reference source synchronization | done | [`skills/reference-sync.json`](../skills/reference-sync.json) records the pinned `0f501d1` source commit plus deterministic file count, byte count, and tree SHA-256 for each of the six reference plugin bundles: 345 files in total. The repository-only checker fails if that source changes without review or if any reference path is missing from this project's compatibility superset. Adapted files may add public OfficeKit workflows and honest fail-closed boundaries; the gate does not confuse path completeness with byte identity. The Presentation `style_guidelines.md` and `routing/google_slides.md` references are now present at their native paths. Canonical template binaries keep their stricter independent byte-comparison gate. |
| Published PNG identity and package budget | done | All 40 PNGs required by the four npm file-type plugins remain published. The 39 paths shared with the pinned reference plus the Excel live-control compatibility icon retain byte-identical inflated scanline streams and byte-identical non-IDAT chunks; only their IDAT zlib representation is deterministically recompressed. The repository gate validates PNG CRC/structure, source identity, idempotence, the exact 40-file inventory, and a 3,550,000-byte category ceiling. This recovers 848,504 unpacked bytes without changing pixels, EXIF/XMP/ICC, density, plugin paths, or the separately byte-identical repository-only template assets. |
| Private-package imports | done | Published JavaScript examples and runners import `office-kit`; the package test rejects imports from `office-artifact-tool`. |
| Presentations built-in template | done | The unflattened 26-slide `grid-layout-library` runs through its shipped `create-presentation.mjs`, canonical OfficeKit export, and second import. Text runs, body properties, absolute frames, 11 literal custom geometries, one prompt placeholder image, and two connectors survive. |
| Presentation workspace helper | done | The shipped setup helper resolves the public package root or bundled runtime, creates a module workspace, and links/imports `office-kit` without a private runtime path. |
| OfficeKit | done | The project-native top-level Skill inventories inputs and outputs, assigns exactly one domain owner per artifact, orders cross-format work as a dependency graph, and loads only the selected installed domain Skills. Existing-file edits and explicit user/domain/template choices take precedence. Its goal-clear/template-specified matrix searches only for a clear goal with no chosen template; unclear goals are clarified first or elicited from an uploaded/named template, and a known goal plus known template goes directly to feasibility. Uploaded DOCX/XLSX/PPTX references remain immutable and task-scoped unless reusable registration is explicitly routed to Template Creator. New DOCX/XLSX/PPTX work has an explicit `selected`, `ask`, or `none` result; `none` is valid, PDF-only work never queries the Office catalog, and missing Skills fail as a broken install rather than falling through to raw APIs. The deterministic local BM25F query validates schema v2, safe paths, retained-file hashes, duplicate-ID root precedence, budgets, and untrusted catalog text; it field-weights purpose/audience/content/visual evidence, hard-rejects `avoidWhen` conflicts or missing verified operations, and returns scores, matches, review flags, and rejected reasons with `selectionMade: false`. The Agent retains semantic and visual judgment, loads only final previews/instructions, and uses no vector database or hidden model call. |
| Template Creator | done | The shipped local-only utility creates numbered `artifact-template-*` Skills from one structurally valid DOCX/PPTX/XLSX reference plus a structurally valid PNG preview, retains source bytes, and explicitly updates only one named same-kind template. Before it acquires a lock or writes a tree, the canonical bounded facade inspector verifies ZIP CRCs, the family-specific main part/content type, and exactly one root `officeDocument` relationship, so renamed text, cross-family, corrupt, and broken-root inputs fail closed. It emits schema-v2 selection metadata and exact retained-asset hashes, defaults unverified templates to opinionated `copy-only`, accepts a complete strict `--selection-json`, preserves existing v2 metadata on update, and migrates explicitly owned v1 templates. It enforces input budgets, rejects symbolic-link update trees, serializes writers, recovers interrupted replacements, preserves extra template-owned files, rolls back placement failure, and leaves no staging/backup/lock residue. The pack gate additionally installs the production tarball offline, invokes the packaged creator, and checks the generated sidecar plus byte-identical retained XLSX/PNG assets. |
| Default Template Library | partial | This repository-only library directly retains the 20 Office template Skills committed under MIT at [`office-artifact-tool` `256cb31`](https://github.com/w31r4/office-artifact-tool/commit/256cb31bfe0a07b3cef0051b6b159342be381378): 7 DOCX, 7 PPTX, and 6 XLSX. The current reference submodule retains those unchanged source assets at `0f501d1`, while the original asset provenance stays pinned to `256cb31`. Every Office/PNG asset is byte-accounted in `integrity.json` and compared byte-for-byte with that checkout; each local schema-v2 sidecar adds intended/avoid uses, audience, content shape, visual traits/commitment, verified edit profile, provenance, and the same exact asset hashes for compact OfficeKit discovery. A transactional materializer verifies the source hash, creates a distinct working copy, writes a provenance audit, and refuses overwrite. All 20 templates pass public-facade import, unchanged export, second import, and LibreOffice/Poppler source-and-output rendering when those tools are available. All seven PPTX templates additionally perform a public `TextFrame.set()` edit on one visible recognized SlidePart-placeholder title, preserve native identity/geometry plus paragraph/run formatting, reimport, and render the edited file; a changed newline topology fails closed. All seven DOCX templates also perform a public capability-routed text edit: six replace a unique literal in an ordinary paragraph and Minimal Letterhead patches its complex greeting table cell. The DOCX route distinguishes `textEditable` from source-bound `textPatchable`, rejects unsafe assignment, preserves native formatting/topology, reimports, re-proves capability evidence, and native-renders every edited output. Native codec tests exercise both Office families independently. A non-formula XLSX string cell remains the bounded spreadsheet edit. Rich objects and unsupported topology remain source-bound; the Financial Budget partial shared-formula range is deliberately preserved but read-only. The library is not in npm and has no source-free generator or visual fallback. See [template-library provenance](template-library-provenance.md). |
| Spreadsheet core example | done | The reference-style create/value/formula/fill/style/chart/SVG-render/export/import example, `Workbook.fromCSV`, and the shipped OfficeKit range, data-validation, worksheet-protection, sparkline, What-If, Pivot, finance, bubble-chart, and threaded-comment workflows run against the public package. The data-validation workflow authors list/whole/custom rules with input prompts, error alerts/styles, blank policy, and list-arrow visibility, imports them twice through C# WASM, edits the fixed rule while preserving native semantics, and enters LibreOffice/Poppler QA. The worksheet-protection workflow separately marks unlocked inputs and locked/hidden formulas, authors a passwordless agent-facing permission profile, edits that source-bound profile, imports twice, and enters native render QA; it explicitly treats protection as an editing restriction rather than encryption. The runnable `conditional-format-visuals` fixture additionally authors typed standard data bars plus normal/reversed icon sets, imports them twice through C# WASM, checks inspect/layout/SVG semantics, and visually reviews the one-page workbook through LibreOffice/Poppler. The broader suite covers R1C1 formulas, block writes, formula evidence, current-region/navigation, formatting, charting, direct threaded replies, standard sparklines, one-/two-variable What-If tables, a native multi-axis/multi-value PivotTable, and three separate financial models with formula-driven checks. Unsupported protection/validation/comment/conditional-format extension graphs fail closed rather than being flattened. |
| Spreadsheet imported Pivot cache safety | done | `officekit-pivot-refresh-hardening-workflow.mjs` is a direct no-overwrite imported-XLSX transaction: it binds one worksheet/Pivot name, requires `sourceCapabilities.refreshOnLoadHardenable` to prove one uniquely owned cache with an explicit source `refreshOnLoad=true`, and invokes `pivot.disableRefreshOnLoad()` only then. It audits every package part, permits only the cache definition to change, proves the XML residual/second import/model render, and returns the capability as false. It neither refreshes data nor promises to suppress manual, macro, external-data, or other host-triggered refreshes; all other imported Pivot fields, source values, cached output, topology, and shared/ambiguous cache graphs remain fail-closed. |
| Spreadsheet bounded imported formula update | done | `officekit-growth-assumption-edit-workflow.mjs` provides a deliberately narrow two-sheet operating-plan transaction: it changes only a named growth input, recalculates the dependent formula chain, protects a separate gross-margin input, sheet order/identity, and a byte-stable `Approved Baseline` canary, then reimports and all-sheet SVG-renders before atomic output/audit promotion. The independent Agent evaluator additionally permits only the target worksheet plus workbook metadata package parts to change and requires LibreOffice/Poppler target-page change with an unchanged baseline page. It is not a generic imported-workbook rewrite or global calculation claim. |
| Full Spreadsheet Quick API | partial | The high-value Range slice is compatible: `formulasR1C1`, `displayFormulas`, `formulaInfos`, `Range.write/writeValues`, clear/copy modes, relative formula translation, even tiling, used/current regions, navigation aliases, scalar/matrix number formats, seven validation rule types with bounded admission UX, all 15 passwordless worksheet-protection operation classes through an intuitive allow-list, and six conditional-format rule types are documented and tested. Standard data bars and all 17 base SpreadsheetML icon sets accept typed thresholds plus `showValue`/`reverse`, cross the public model/protobuf/Open XML path, and emit agent-visible inspect/layout/SVG hints with native render QA. `thread.addReply()`, `containsText`, formula-bound charts, standard sparklines, bounded What-If tables, and bounded native Pivots likewise cross the canonical path. Remaining reference breadth includes password/cryptographic and irregular worksheet-protection graphs, richer Pivot axes/grouping/filter/edit graphs, multi-area/extension validation graphs, non-reversible sparkline/data-table graphs, scatter/modern chart variants, x14/custom/stop-if-true conditional-format graphs, and formulas outside the public Help catalog. |
| Excel live control | partial | Routing content, Skill metadata, icon, and `.app.json` connector declaration are present. Execution requires the host-provided connected-document app plus an active Excel add-in session; the npm package does not implement that service. |
| Documents | partial | The ordinary native workflow runs a shipped `DocumentModel` create → OfficeKit export → import/edit → export → second-import example with semantic verification, fresh `document.fontFamilies`, and render-backed QA. Its source-free footer demonstrates one bounded ordered literal/simple-field paragraph (`Page PAGE of NUMPAGES`); the fallback displays are not a claim that JavaScript calculated pagination. The imported counterpart exposes its exact segments for inspect/resolve and no-op preservation but remains source-bound/read-only. The reference checklist remains available for explicit Python package helpers, not as a replacement for the public path. Imported paragraphs/table cells expose separate `textEditable` and `textPatchable` evidence; one unique ordinary literal can be replaced without flattening same-format run boundaries, while ambiguous, mixed-format, field/control/revision, or cross-boundary edits fail closed. Dedicated source-text, source-bound note-paragraph, tracked-replacement, revision-finalization, classic-comment, modern-comment, and canonical text-watermark workflows protect immutable inputs, bind source identity/topology, audit exact package scope, reimport, verify, render, and publish without overwrite; all seven retained DOCX templates traverse the bounded source-text path. The note workflow changes one canonical physical paragraph only after binding the semantic/native/anchor IDs, index, and exact old text; it permits only `word/footnotes.xml` or `word/endnotes.xml` and proves the remaining raw XML unchanged. The watermark workflow edits or removes one recognized exclusive VML text-path paragraph and permits exactly one changed Header part; shared/multiple, DrawingML/image, irregular VML, and imported relationship/scope changes stay opaque or fail closed. The public model also covers whole-block bookmarks/internal hyperlinks, plain-text footnotes/endnotes, bounded revisions with writable tracking intent, canonical inline `SEQ`/`REF`/`PAGEREF`, bibliography-backed citations plus one source-free switch-free `BIBLIOGRAPHY` output placeholder, one canonical complex TOC placeholder, passwordless `none`/`readOnly`/`comments`/`trackedChanges`/`forms` document-protection state through `document.setSettings()`, bounded section line numbering, and direct/named-style `paragraphFormat.suppressLineNumbers` with presence-aware inheritance. The bibliography placeholder requires a modeled source, writes the update-on-open intent, permits only cached display-text edits after canonical import, and leaves formatted entry generation to a compatible Word host. Protection is treated as an editing restriction, not encryption: password/cryptographic variants stay source-owned and replacement fails closed. Canonical suppression true/false is editable after import; duplicate, extension-bearing, child-bearing, or invalid lexical markup remains source-owned and fails closed. Content controls include strict string fill-by-tag for inline and table-cell plain text plus one canonical body-level plain-text `w:sdt`; boolean state for Word 2010+ checkboxes; declared `selectedValue` for canonical `w:dropDownList`; declared-or-custom typed `value` for canonical `w:comboBox`; and a real Gregorian `YYYY-MM-DD` `dateValue` for the canonical `w:date` profile. All five typed profiles can own either one inline run or one complete canonical table cell. Handles expose block/inline/tableCell placement, with row/column on cell controls; native ID, type, wrapper placement, table coordinates, drop-down/combo-box choices and order, symbols, fixed date-format/language/mapping/calendar metadata, and topology remain source-bound. Typed setters never silently coerce across control kinds. The shipped content-control fixture authors block text plus all five inline and all five table-cell profiles, edits each through its typed primitive, imports twice, inspects native markup, and enters LibreOffice/Poppler QA. The protection fixture separately proves author/edit/import state, native settings markup, and zero-pixel layout change through LibreOffice/Poppler. Python/OOXML helpers remain explicit for PAGEREF/page calculation, bulk caption discovery, arbitrary-package wrapping, header/footer controls and controls outside the bounded body/table profiles, refreshed cross-paragraph TOCs, refreshed/multi-paragraph or irregular bibliography output, and irregular bibliography/note/bookmark/revision/field/comment/protection/watermark graphs. Full status remains partial because rich/multi-paragraph/inline-within-cell/nested/data-bound/locked/placeholder/repeating-section cell SDTs, other irregular SDTs, irregular lists, localized dates, custom-symbol checkboxes, rich note bodies, password/IRM permission semantics, complex background objects, and other source-bound features still exceed the semantic model. |
| Full Presentation API guide | partial | The built-in source-free template and Agent-facing create/inspect/resolve/render/verify flows are compatible. The runnable chart-family workflow now authors standard area, fixed 50%-hole doughnut, marker-only scatter, and bounded 2D bubble ChartParts, edits all four after import, imports twice, inspects/verifies, and produces an explicit Playwright PNG plus native LibreOffice/Poppler QA when available; formula/external data and advanced plot topology fail closed. Native table authoring closes the reference `table.merge(...)` primitive for multiple non-overlapping rectangular ranges: origin/covered semantics are inspectable, canonical DrawingML merges survive two imports, and imported merge topology is fixed/fail-closed. Imported `slide.name`, `moveTo`, isolated delete, custom-show edits, title/plain-notes edits, fixed-topology rich speaker-notes run edits, relationship-free rich speaker-notes paragraph/run edits with fixed imported topology, notes-absent plain-text notes creation, comment-free legacy review-comment creation, a plain-node SmartArt text edit, and `slide.duplicate()` use explicit source-bound transactions. The notes-add workflow gates on inspectable capability evidence, independently re-proves the source graph, reuses one NotesMaster byte-for-byte or creates a canonical master over an existing theme, audits exact NotesSlide relationships, reimports, and requires source/output render identity. The legacy-comment-add workflow separately requires the complete source package to have no legacy or Office 2021 comment graph, creates one canonical shared author catalog plus one closed slide-local comments part, allocates collision-free relationships, audits the exact changed/added OPC scope, reimports exact author/text semantics, publishes without overwrite, and proves source/output model plus LibreOffice/Poppler render identity. Existing legacy graphs outside the separate bounded root-text edit profile remain unchanged-only. The clone profile accepts canonical inline leaves including merged tables, closed ChartPart/notes/comments leaves, eligible embedded-XLSX OLE, canonical top-level four-part SmartArt, canonical top-level closed InkML content parts, canonical closed embedded-MP4 pictures, supported run links, and stable-ID custom-show actions. It creates independent slide/chart/XLSX/SmartArt/InkML/MP4 parts, shares only proven immutable resources, preserves retained source bytes, and requires export/reimport. After that boundary, a distinct SmartArt profile may replace only existing one-run document-node text in its bound DiagramDataPart; all other SmartArt, InkML, and media remain opaque/read-only. Connected/noncanonical chart, OLE, SmartArt, contentPart, media, comments, link, notes-master, template, section/extension, and broad OPC mutations fail closed. Shipped chart-family, name, duplicate, custom-show, speaker-notes add, legacy-comment add, title/plain-notes, rich-notes fixed-run, modern-comments, OLE, SmartArt, InkML, media, render, and PromptBench workflows expose the bounds without a hidden fallback codec. |
| Presentation legacy comment text edit | partial | `slide.comments.capability.editable` is true only for one existing closed legacy `SlideCommentsPart` with one relationship-free presentation author catalog and a re-proven fixed comment topology. The `officekit-legacy-comment-edit-workflow.mjs` transaction binds one slide name, stable comment ID, and exact old text; it changes only the selected root `p:cm/p:text`, rejects author/time/coordinate/native-author-index/order/count/reply/relationship changes, permits exactly that existing comments XML part to differ, reimports full semantics, preserves model/native rendering, protects the input, and emits a no-overwrite audit. Mixed, connected, rich, or otherwise irregular comment graphs remain read-only/fail-closed. |
| Presentation slide transitions | partial | `slide.setTransition(...)` and `clearTransition()` provide a direct, source-free `fade`/directional-`push` profile with explicit speed, click advancement, and a bounded timer. `inspect({ kind: "transition" })` and `${slide.id}/transition` resolve a capability-aware facade. An imported slide may replace or remove only one existing canonical direct transition; a transition-absent source may add one only when `transition.capability.addable` proves the Slide root contains only `p:cSld` plus optional `p:clrMapOvr`, with no `p:transition`, `p:timing`, or extension leaf. Addition is re-proved during export and changes only that SlidePart. For an Agent-facing replacement of an existing canonical transition, `officekit-transition-edit-workflow.mjs` requires one unique slide name plus complete expected/replacement states, protects source bytes, permits exactly the target SlidePart to differ, reimports, compares static model render and non-transition semantics, and emits a no-overwrite audit. Timing trees, sound actions, `p14` duration, extension lists, other effects, and irregular XML remain opaque-preserved and fail closed. The strict slide-clone contract can carry one unchanged canonical direct transition. Export/reimport and Open XML semantic checks verify the package metadata; static render QA checks only visible-slide stability and does not claim slideshow playback fidelity. |
| Presentation section-name transaction | done | `officekit-section-rename-workflow.mjs` is the narrow Agent-facing route for one canonical imported section label. It requires one exact current name, rejects duplicate/missing/case-conflicting names and opaque or section-free packages, retains the count/order/facade ID/native GUID/ordered membership, and changes only `ppt/presentation.xml`. It protects the source, reimports the full section snapshot, proves non-section semantics and static slide renders stable, calls `verify()`, and writes a no-overwrite source/output audit. It deliberately does not expose boundary movement, catalog topology changes, slide topology work, or a native navigation-pane claim. |
| Imported SlidePart placeholder text | partial | A concrete imported `p:sp/p:ph` with a fully recognized owner-local `p:txBody` now exposes a component-level text capability without making the surrounding placeholder editable. `text.replace(...)` changes one existing run; `text.set(...)` preserves native paragraph/run formatting when newline-delimited segments map unambiguously onto the fixed source topology. Type/index/name, direct or inherited geometry, formatting, layout binding, Master/Layout graphs, fields, and unmodeled XML remain source/hash-bound; changed newline counts, ambiguous styled spans, unsupported text graphs, or any non-text mutation fail closed. The shipped title/notes workflow uses the same public path, and all seven retained PPTX templates pass visible title edit, second import, formatting/identity checks, and native render. |
| Presentation clone leaf updates | done | The bounded `slide.duplicate()` profile accepts recursively canonical inline groups, closed literal ChartParts, eligible top-level embedded-XLSX OLE frames, canonical top-level SmartArt frames, bounded connectors, run links, notes, and legacy comments. Each SmartArt frame must have one exact `dgm:relIds` root whose `dm/lo/qs/cs` relationships bind four standard, non-empty, relationship-free parts. OfficeKit copies those data/layout/quick-style/colors bytes into four distinct typed clone-local parts under the same slide-local IDs. C# and JS tests prove origin byte preservation, SDK validation, disjoint reimport paths/equal hashes, and fail-closed connected or non-top-level graphs. Clone preservation does not itself grant editing; the separate plain-node profile below is the only SmartArt text exception. SmartArt semantic authoring, graph/layout editing, unsupported connectors/comments, and general template graph cloning remain unavailable. |
| Presentation SmartArt plain-node text | partial | `nativeObject.diagramText` and `setDiagramNodeText(modelId, text)` are available only for a source-bound top-level four-part SmartArt frame whose DiagramDataPart has one direct `dgm:dataModel` / `dgm:ptLst`, unique document-point IDs, and exactly one direct `dgm:t > a:p > a:r > a:t` chain per exposed point. The public workflow protects input bytes, resolves one object/node/expected-text triple, permits only the bound DiagramDataPart to change, audits provenance/package scope, reimports exact node order/IDs/text, and fails closed for rich/multi-run, connected, nested, or ambiguous graphs. This is not authoring, graph/layout/style/color/geometry editing, raw XML access, or native render proof. |
| Presentation InkML content-part clone | done | The same bounded clone profile accepts one direct top-level `p:contentPart` only when it uniquely consumes an internal standard/strict `customXml` relationship to a non-empty, fully well-formed, relationship-free `application/inkml+xml` part with an `ink` root in the standard namespace. OfficeKit uses the Open XML SDK typed CustomXmlPart API, retains the slide-local ID, copies exact bytes into a distinct clone part, and validates the package. C# and JS tests prove source immutability, second-import disjoint paths/equal hashes, prefixed InkML namespace handling, and fail-closed connected, wrong-root, multi-root, or non-top-level inputs. This is opaque clone preservation, not ink authoring, stroke editing, or arbitrary Custom XML support. |
| Presentation source-bound duplicate workflow | done | `officekit-slide-duplicate-workflow.mjs` independently inspects the source package before semantic import and before invoking `slide.duplicate()`. It inventories each canonical SmartArt frame's four roles, each InkML content part's exact relationship/root/content/hash, and each embedded-MP4 picture's media action, video/media pair, unique inbound ownership, content type, bytes/hash, and poster binding. After export it requires distinct byte-identical clone targets, shared immutable posters, exact allowed package scope, second-import model agreement, independent source/clone bindings, and native poster-render equality when available. Its chart/OLE/link/notes/comments/custom-show checks remain. Connected, nested, incomplete, mistyped, external, wrong-root, shared, or otherwise noncanonical SmartArt/InkML/media fails before output/audit promotion. No broad graph clone, fallback, SmartArt/ink/media authoring, playback claim, or lexical clone-slide XML claim is implied; the separate SmartArt plain-node workflow is the only text-edit exception. |
| PDF | partial | The native guide is a reference-compatible capability router. Its public `./pdf/providers` control plane resolves one declared task to `ready`, `installable`, or `blocked` from a single versioned provider→pack catalog and a default-disabled project policy; it permits only policy-authorized, hash-pinned managed assets or a separately selected `system-only` runtime, with no automatic route switch. qpdf `12.3.2-oat.1`, `python-foundation` `3.13.14-oat.1`, qpdf-dependent `python-specialists` `3.13.14-oat.1`, veraPDF/JRE `1.30.2-oat.1`, OCR core `17.8.1-oat.1`, and `eng`/`chi_sim` language packs `4.1.0-oat.1` are published, attested managed packs for `darwin-arm64` and `linux-x64`; the foundation provides isolated CPython with ReportLab/pdfplumber/pypdf/Pillow, specialists provide PyMuPDF/pikepdf/pyHanko/certificate validation after explicit AGPL-or-commercial acknowledgement, veraPDF brings its own managed JRE, and OCR installs qpdf plus the isolated OCRmyPDF/Tesseract 5/Ghostscript/`pdftotext` core and only requested language packs. Only Poppler QA remains intentionally unpublished, so that managed route blocks honestly today. Required lazy MuPDF.js drives default arbitrary-file inspect/render and bounded direct-original edits; its source-bound Text-note, unique-text Highlight, and safe-link placement primitives now use the inspect-returned `mupdf-page-space` bbox after the current 0/90/180/270-degree page rotation, preserve raw unrotated box facts separately, expose native appearance geometry, and fail closed on stale/clipped placement. Its source-bound `duplicate_page` primitive copies one ordinary non-interactive untagged right-angle page to a declared 1-based output position as a single-operation rewrite, rejects ambiguous page/object graphs, and requires second inspection plus mapped Poppler pixel identity; it does not synthesize navigation or broaden the complete-source pypdf merge contract. The pypdf form route supports explicit interactive fills and a separate verified static delivery: flattening is rewrite-only, paints requested and untouched canonical fields before removing every Widget and the `/AcroForm` tree, reopens to prove no live field tree remains, reports `formValidation`, and rejects incomplete/orphan/unsupported form graphs rather than guessing how to reattach them. Explicit specialist routes cover ReportLab/pdfplumber/pypdf, PyMuPDF sanitize including typed exact raster-term `redact_ocr_text`, qpdf repair/linearize plus a source-bound unencrypted-input-to-AES-256 delivery-copy primitive with private password-file transport, pikepdf active/auxiliary cleanup, veraPDF conformance, and OCRmyPDF searchable-layer OCR. The qpdf encryption route refuses decrypt/re-encrypt, permission editing, owner-password bypass, plaintext secret transport, unsigned signature-policy assumptions, and public password-bearing inspect. OCR redaction binds one page plus its exact 0/90/180/270-degree rotation, expected match count, Tesseract language/DPI, native image placement, full rewrite, rotation-aware residue proof, and Poppler display-space QA without fallback. The shipped `scripts/pyhanko_sign_provider.py` adds stable `probe`/`inspect`/`sign` contracts for one source-bound local-PKCS#12 approval or first-document certification signature: exact PDF/credential hashes, trusted/caller-isolated declaration, stdin/no-passphrase, existing/invisible/visible field modes, unrotated CropBox guard, expected signature count, explicit DocMDP, exact-prefix incremental output, internal all-signature integrity/DocMDP validation, and collision-safe promotion. `scripts/pyhanko_provider.py` independently supplies explicit-root trust, revision/ByteRange coverage, timestamps, difference analysis, DocMDP/FieldMDP, and delivery gates. The workflow requires qpdf reinspection and Poppler review and never claims that an older signer endorses a later revision. P12/private keys, TSA/LTV/DSS, PKCS#11/HSM, remote signing, complete PAdES claims, fuzzy/approximate OCR-redaction intent, OCR preprocessing/page subsets, broader corpus QA, and human PDF/UA review remain caller-provided or open. |
| Presentation modern comments | partial | The native Presentation Skill now ships `officekit-modern-comment-workflow.mjs`: it authors a source-free Office 2021 root plus one direct reply against an exact shape text match, exports through OfficeKit, imports the native `p188:authorLst`/`p188:cmLst` graph, changes existing root/reply text and root resolved status, then performs second import, package inspection, model render, CLI execution, and a byte-bound audit. Public targets accept a top-level element, text range, `{ element }`, `{ textRange }`, or `{ textMatch: { element, query, occurrence? } }`. Imported author/person/date identity, GUIDs, anchor/range, position, reply topology, relationships, and source hashes remain fixed. Reactions/task fields/extensions, rich text, nested replies or group-child moniker chains, connected parts, mixed legacy/modern families, and modern-comment slide cloning remain opaque/source-bound and fail closed. Full status remains partial because those advanced native graphs and Microsoft PowerPoint host validation are still outstanding. |

| Presentation section-boundary transaction | done | `officekit-section-boundary-edit-workflow.mjs` is the separate Agent-facing route for one or more imported PowerPoint section boundary moves. It accepts exact expected and replacement arrays containing every section's ID/name/native GUID/ordered membership; the expected array must exactly bind the current canonical source, while the replacement retains fixed section identity/name/catalog order and explicitly partitions every retained slide once in deck order. It refuses a partial update, rename, GUID/catalog change, empty/no-op group, duplicate/omitted/reordered slide, opaque/section-free source, or concurrent slide topology work. It protects the source, allows only `ppt/presentation.xml` to differ, reimports the exact target partition, proves non-section semantics and static model renders stable, calls `verify()`, and writes a no-overwrite provenance audit. Page renders do not claim to exercise the PowerPoint navigation pane. |

### Documents canonical note bodies

The runnable Documents profile now accepts a source-free or recognized
source-bound footnote/endnote body with 1 through 16 physical plain-text
paragraphs. The first paragraph carries its native marker plus one text run;
each continuation carries exactly one text run. Inspection exposes the exact
`paragraphs` array and the LF-joined `text` projection. Replacing complete
fixed-count arrays is supported after import; changing paragraph count,
anchors, native IDs, run/paragraph formatting, or note topology fails closed.
For an Agent task that changes only one existing physical paragraph, the
published `officekit-note-text-edit-workflow.mjs` binds that note's semantic ID,
native ID, anchor ID, index, and exact old text, permits only the matching note
part to differ, and records a raw-XML residual plus no-overwrite audit.
The packaged end-to-end example and multi-paragraph fixture cover export,
import, edit, second import, Open XML SDK validation, and native render QA when
LibreOffice/Poppler are available. Rich/multi-run bodies and bodies beyond the
16-paragraph profile remain opaque/source-bound.

### Documents canonical section-margin transaction

The Documents Skill also publishes
`officekit-section-margin-edit-workflow.mjs` for one imported editable
canonical section. It binds the inspected block index and all five source twip
values, writes only the selected `w:pgMar` body margins, preserves raw
`w:header`/`w:footer` distance canaries, permits only `word/document.xml`, and
then proves the normalized residual plus full section projection on second
import. The input and audit/output paths are distinct and protected from
overwrite. This is a fixed-source page-geometry correction; irregular or
unrecognized section markup remains source-bound.

### Presentation embedded DOCX OLE evidence

`officekit-ole-office-package-workflow.mjs` is the only generic embedded
Office-package edit route currently shipped by the Presentation Skill. It finds
one named top-level OLE object with the unique DOCX binding, imports its
defensive package copy with `DocumentFile`, changes one exact one-run paragraph,
then writes a no-overwrite PPTX and audit. It verifies source immutability,
one-part-only package scope, exact DOCX replacement bytes, rebound
path/relationship/content-type/source-hash evidence, second DOCX import, and
unchanged model SVG. Legacy XLSX keeps its specialized workbook API and the
generic API remains compatible with it. DOCX OLE clone/authoring, general OLE
bytes, preview regeneration, rich/fragmented text, shared/external/malformed
graphs, and fallback patching are explicitly rejected.

### Documents floating-image evidence

The public Documents path now authors one bounded foreground `wp:anchor`,
imports and edits only its placement from square to top-and-bottom wrap, imports
again, inspects the native graph, and completes LibreOffice/Poppler page review.
Unsupported anchors remain source-owned, and imported inline/floating
transitions fail closed. Broader Word drawing placement remains outside the
semantic model.

### Documents text-watermark evidence

The public Documents path now authors one canonical section/header-scoped VML
text watermark, imports it as a separately inspectable/resolvable object, edits
only its text path, imports again, and enters LibreOffice/Poppler page review.
The shipped transaction protects the source, refuses overwrite, proves that
exactly one `word/headerN.xml` part changed, verifies and model-renders the
result, and records an audit. Whole-object removal uses the same exact paragraph
and residual-header hashes. Shared headers, multiple objects, DrawingML/image
backgrounds, irregular VML, and new imported relationship topology remain
opaque or fail closed rather than being heuristically flattened.

### Documents imported header/footer evidence

Imported page furniture is not rebuilt through a generic document model. A
header or footer exposes `sourceBound` plus `editable` only when it is a
direct unformatted text paragraph in a uniquely used source part. A transaction
may change one such paragraph per part: OfficeKit re-proves the relationship,
section scope, source semantics, paragraph element/residual hash, and residual
part hash, then permits exactly that Header/Footer part to change. PAGE/simple
fields, rich or multi-run paragraphs, shared or inherited parts, multiple edits
to one part, and any relationship/style/scope/topology change are read-only and
fail closed. A watermark edit may share the same canonical Header part because
both codecs exclude the other's proven object from their residual check; the
result still changes only that one part and must be reimported and rendered
before delivery.

Source-free page furniture has a separate narrow authoring profile: 2 through
32 ordered literal or simple-field segments in one header/footer paragraph, at
least one field, no formatting, and a visible text value derived from the cached
segment displays. This supports a native `Page PAGE of NUMPAGES` paragraph
without a hidden placeholder convention. A recognized imported multi-segment
paragraph reports the same sequence but is intentionally not editable; exact
no-op export preserves it rather than rebuilding its field graph.

| Document source-bound page geometry | done | `officekit-section-page-geometry-edit-workflow.mjs` edits one imported canonical Word section only when its block index and complete `{ orientation, pageSize }` source value match. It treats `w:pgSz` as an atomic leaf, permits only `word/document.xml`, checks the raw residual/package scope, reimports the full section projection, verifies/model-renders, protects the source, and writes a no-overwrite audit. It does not rescale surrounding content; paper-code, extension, duplicate/non-Word, stale, partial, or noncanonical geometry fails closed before publication. |
| Document source-bound line numbering | done | `officekit-section-line-numbering-edit-workflow.mjs` edits one imported canonical Word section only when its block index and complete normalized `lineNumbering` source value match. It permits only `word/document.xml`, masks only the selected `w:lnNumType` for residual/package checks, reimports the full section projection, verifies/model-renders, protects the source, and writes a no-overwrite audit. It never adds/removes numbering, changes paragraph suppression, or claims to calculate displayed line numbers; duplicate, child/extension-bearing, unknown/non-Word, noncanonical-numeric, stale, or opaque leaves fail closed before publication. |
| Document source-bound columns | done | `officekit-section-columns-edit-workflow.mjs` edits one imported canonical Word section only when its block index and complete normalized equal-width or custom-width `columns` source profile match. It permits only `word/document.xml`, retains the source profile shape, masks only the selected `w:cols` element for residual/package checks, reimports the full section projection, verifies/model-renders, protects the source, and writes a no-overwrite audit. It never adds/removes columns, converts profile shape, or claims to calculate visible column flow; duplicate, child/extension-bearing, unknown/non-Word, noncanonical-numeric, stale, or opaque graphs fail closed before publication. |

## Compatibility discipline

Plugin packaging and workflow compatibility are separate gates. A plugin is not marked fully compatible merely because its manifest validates or its files appear in the tarball.

For each remaining Skill, convergence requires:

1. run the published instructions from a clean npm install;
2. exercise the public package rather than repository-only test harnesses;
3. keep DOCX/XLSX/PPTX ordinary I/O on the single OfficeKit facade;
4. retain direct OOXML patching only as an explicit, user-selected low-level operation;
5. render and inspect representative output where the environment supports it;
6. record unsupported reference calls and either implement them or narrow the published instruction honestly;
7. add the workflow to `test/reference-skills.mjs` before promoting its coverage status.

For PDF, “compatible” means preserving the reference foundation while routing advanced work to explicit mature providers. It does not mean forcing every operation through `PdfArtifact`: arbitrary-file extraction models are never exported as fidelity-preserving edits, and provider absence or unsupported capability fails closed.

`test/reference-skills.mjs` is the publication-contract smoke test. The older format-specific tests under `test/*-skill.mjs` continue to exercise the deeper development fixtures under `test/skill-harness`.
