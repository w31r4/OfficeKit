# Tasks

- [x] Add `lineOpacityThousandthPercent` to native-leaf normalization, schema
      enum, capability registry, and generated/reference coverage without a
      protocol change.
- [x] Add bounded shape/connector projection, source proof, readback, and
      one-token XML splice for an existing direct line alpha.
- [x] Add the minimal authored/imported/source-bound/reprojection regression
      for a shape and a connector, including the missing-alpha refusal.
- [x] Run the focused codec test, related line regressions, maintainer checks,
      JSON/OpenSpec gates, and `git diff --check`.
