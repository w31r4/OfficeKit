## 1. Field lifecycle

- [x] 1.1 Implement verticalText source omission and simple style removal with the existing marker; verify supported owners, native presence, all three restored values, no-op bytes, surrounding XML/non-target ZIP and retained capability/removal guards in the shared lifecycle tests.

## 2. Public contract

- [x] 2.1 Update schema/Help/registry/text reference and backlog/coverage, regenerate derived reference/matrix and verify preview input/capability, portability/reference sync and strict OpenSpec checks with exact scope recorded.

Verification (2026-09-10): related body/placeholder/table-text native selection passed 47/47, zero skipped (SDK 8.0.128, repository TMPDIR, single-process build). The shared direction experiment gained 7 verticalText cases and retains prior column-direction/removal/authority checks. Preview input/capability, Skill maintenance, matrix, portability (255 files), reference sync (333 files) and strict OpenSpec checks passed. No wire revision, NativeAOT rebuild or host-layout acceptance. Other body deletions/inheritance remain open; the known broader table failure was not rerun.
