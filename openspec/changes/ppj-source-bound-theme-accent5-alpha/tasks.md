## 1. Contract

- [x] 1.1 Extend the source-bound accent5 capability description, schema coverage, registry, backlog, and generated presentation references for canonical direct alpha; keep the wire field and protocol version unchanged.
- [x] 1.2 Validate this OpenSpec change and all generated JSON artifacts.

## 2. Codec

- [x] 2.1 Project a canonical direct accent5 `a:alpha` together with the existing six accent colors and retain strict source ownership for non-canonical topology.
- [x] 2.2 Compile only the existing accent5 RGB and alpha attributes, preserving alpha presence and the rest of the ThemePart.

## 3. Regression

- [x] 3.1 Add an authored -> source projection -> RGBA edit -> second projection test covering no-op bytes, changed-part scope, non-target ZIP preservation, Open XML validity, and capability/alpha-topology rejection.
- [x] 3.2 Run focused native tests, OpenSpec strict validation, schema/registry/protocol checks, and Presentation Skill maintenance.
