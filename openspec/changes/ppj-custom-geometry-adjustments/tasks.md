## 1. Adjustment graph lifecycle

- [x] 1.1 Add kind-specific custom adjustments to schema, authored/projected/source geometry and diagram validation; verify adjustment-to-guide-to-rectangle evaluation, list add/edit/delete, cross-list errors, preset compatibility and unchanged paths/text/frame/non-target ZIP bytes.

## 2. Discoverability and checks

- [x] 2.1 Update Help, registry, shapes reference, backlog and coverage; regenerate docs and pass focused native geometry/preview tests, explicit preview diagnostics, portability/reference sync and strict OpenSpec validation.

Verification (2026-09-10): SDK 8.0.128 native CustomGeometry/PresetGeometry/PpjPreviewAuthoredSceneTests passed 68/68, zero skipped. Preview input assessment, scene SVG, capability coverage, presentation Skill maintenance, capability-matrix freshness, portability (255 files), reference sync (333 files), strict OpenSpec validation and git diff --check passed. No wire change or new host-appearance acceptance.
