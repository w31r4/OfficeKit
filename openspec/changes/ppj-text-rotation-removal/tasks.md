## 1. Rotation lifecycle

- [x] 1.1 Add rotation omission handling and bounded native deletion admission for existing text owners, including removable rotation/upright-only style objects. Verify native attribute presence, no-op bytes, zero/signed restoration, table compact-text restoration, unchanged surrounding XML/non-target ZIP and retained guards in the shared lifecycle fixture and related text-body tests.

## 2. Public contract

- [x] 2.1 Update schema descriptions, Help, registry, text reference, backlog and coverage; regenerate references/matrix and run preview input/capability, portability/reference-sync and strict OpenSpec checks. Record the exact test scope and remaining layout/other-deletion boundaries.

Verification: related native body/placeholder/table-text selection passed 33/33, zero skipped, including 7 new rotation cases and 7 retained upright cases. Skill maintenance, capability matrix, preview input/capability checks, portability (255 files), reference sync (333 files) and strict OpenSpec validation passed. Other text-body deletions and full layout remain bounded; no wire revision, NativeAOT rebuild or host acceptance. The previously documented broader table failure was not rerun.
